# 2026-03-29 00:52 A4.0 统一内容契约入口与装载基线自测报告

## 1. 自测范围

本轮聚焦 `#53 WBS L3: A4.0 统一内容契约入口与装载基线`，只验证统一内容注册表、运行时装载边界与回归入口，不扩展到 skill/buff/level 编辑器业务实现。

验证对象：

1. [config.json](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/config.json)
2. [DataManagerV2.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js)
3. [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)
4. [mock_ui_v11.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html)
5. [05-mock_ui_v11主流程测试基线.md](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CodexAnylyse/05-mock_ui_v11主流程测试基线.md)

## 2. 执行的检查

1. 检查内容入口配置
   - 结果：`assets/data/config.json` 已新增 `contentRegistry`
   - 结果：已覆盖 `player/items/skills/enemies/levels/buffs/slotLayouts`
   - 结果：`skills.byTree.melee_v4_5` 与 `buffs.schemaVersion = buffs_v2_1_wrapped` 已显式声明
2. 检查 DataManager 装载边界
   - 结果：[DataManagerV2.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js) 已新增 `_buildContentRegistry`
   - 结果：`loadConfigs()` 已优先走 `contentRegistry`，并把已装载包记录到 `gameConfig.contentPacks`
   - 结果：保留 `sources` 兼容层，未强行打断既有配置
3. 检查回归测试入口
   - 结果：[codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html) 已新增 `config registry 与 DataManager 装载边界统一` 回归项
   - 结果：回归页辅助函数已改为按运行时同口径解析 `basePath`
   - 结果：浏览器中 `22/22` 项回归通过，无 `FAIL`
4. 检查主流程消费入口
   - 结果：按 [05-mock_ui_v11主流程测试基线.md](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CodexAnylyse/05-mock_ui_v11主流程测试基线.md) 执行真实点击流程
   - 结果：`开始冒险 -> 关卡选择 -> 幽暗森林边缘 -> 选择技能 -> 部署技能槽 -> 提交规划 -> 执行` 可达
   - 结果：执行后成功进入第 `2` 回合，页面回到 `PLANNING`
   - 结果：打开技能树成功，模态窗显示 `可用 KP: 30`，关闭按钮可用
5. 检查静态质量
   - 结果：执行 `git diff --check` 通过

## 3. 关键证据

1. 浏览器回归页
   - 地址：`http://127.0.0.1:3101/test/codex_regression_runner.html`
   - 结果：`22/22 PASS`
   - 关键新增项：`PASS config registry 与 DataManager 装载边界统一`
2. 浏览器主流程页
   - 地址：`http://127.0.0.1:3101/mock_ui_v11.html`
   - 点击流程：`开始冒险 -> 关卡选择 -> 幽暗森林边缘 -> 重锤 -> 敌方胸部槽 -> 提交规划 -> 执行`
   - 结果：进入第 `2` 回合后，玩家为 `HP 90 / 100`，敌人为 `HP 48 / 50`，敌方胸部护甲为 `0 / 18`
3. 运行时日志
   - `mock_ui_v11.html` 控制台已出现 `? [DataManager] Configs successfully loaded from JSON files.`
   - 未出现新的数据加载失败日志

## 4. 自测结论

结论：`通过`

已确认：

1. `contentRegistry` 已成为统一内容入口，且未破坏当前主流程消费链
2. `DataManagerV2` 的装载边界已具备三条内容链可复用的共享基线
3. 回归页与真实主流程页对同一数据入口的验证口径已对齐

## 5. 未覆盖项

1. 本轮未实现 skill/buff/level 编辑器页面的新功能
2. 本轮未调整各内容域的 schema 细化规则，只处理统一装载入口与元信息
3. `mock_ui_v11.html` 控制台仍存在一个既有表单可访问性提示：`A form field element should have an id or name attribute`
   - 该提示不属于本轮装载边界改动引入的问题
