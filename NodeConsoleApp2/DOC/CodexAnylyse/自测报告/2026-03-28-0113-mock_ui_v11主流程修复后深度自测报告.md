# mock_ui_v11 主流程修复后深度自测报告

记录时间：2026-03-28 01:13

状态：`待人工验收` / `待用户确认`

## 1. 本轮目标

验证今晚压缩计划 4 个阶段是否已经落地到真实页面：

1. 宿主与引擎实例唯一性
2. 模态生命周期与焦点管理
3. 技能目标契约、战斗内保存入口、多选技能执行一致性
4. `mock_ui_v11.html` 主流程回归

## 2. 测试依据

- 主入口：`http://127.0.0.1:3101/mock_ui_v11.html`
- 用户确认主流程：
  1. `开始冒险`
  2. `关卡选择`
  3. 选择唯一关卡
  4. 进入战斗主界面
  5. 选技能并放入技能槽
  6. `提交规划`
  7. `执行`
  8. 自动执行并进入下一回合
- 技能树子流程：
  - `×` 只关闭
  - `提交并关闭` 才学习并保存

## 3. 测试环境

- 工程目录：`/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2`
- 本地服务：`PORT=3101 node app.js`
- 浏览器驱动：MCP Chrome DevTools
- 预处理：清空 `localStorage.save_game` 后刷新页面
- 自动回归补充入口：`http://127.0.0.1:3101/test/codex_regression_runner.html`

## 4. 自动回归结果

本轮新增浏览器回归脚本覆盖以下断言，全部通过：

1. `mock_ui_v11.html` 改为接入默认引擎单例，不再直接 `new CoreEngine()`
2. `CoreEngine.saveBattleState()` 同步 `runtime.currentScene` / `runtime.battleState`
3. `TurnPlanner.planMany()` 多选技能按槽位分发目标部位
4. `UI_SkillPanel.getSkillTarget()` 接受 `SCOPE_MULTI_PARTS`
5. `UI_SkillPanel.highlightValidSlots()` 只高亮 `candidateParts`
6. `UI_SkillTreeModal` 改走 `engine.saveGame()` 保存入口

## 5. 真实页面回归结果

### 5.1 宿主与引擎实例

实测结果：

- 页面刷新后控制台只出现 1 次 `Engine initializing...`
- 配置资源不再重复请求
- 登录 -> 主菜单 -> 选关 -> 战斗 仍可正常进入

结论：

- `F-UI-01` 双引擎初始化与重复加载已修复

### 5.2 模态生命周期与焦点管理

实测结果：

- 从 `关卡选择` 点击唯一关卡进入战斗后，页面上不再残留 `选择关卡` 模态内容
- 打开技能树后点击 `×`，焦点回到 `打开技能树` 按钮
- 控制台未再出现 `aria-hidden` 焦点告警

结论：

- `F-UI-02` 选关模态残留已修复
- `F-UI-03` 技能树关闭焦点问题已修复

### 5.3 技能树学习与战斗内保存一致性

执行步骤：

1. 回合 1 打开技能树
2. 学习 `裂颅一击`
3. 学习 `多攻测试`
4. 点击 `提交并关闭`
5. 读取 `localStorage.save_game`

实测结果：

- 技能池新增：
  - `裂颅一击`
  - `多攻测试`
- `save_game.global.player.skills.learned` 包含：
  - `skill_skull_cracker`
  - `skill_1771952147024`
- `save_game.global.player.skills.skillPoints = 28`
- 同次保存内运行态同步为：
  - `runtime.currentScene = BATTLE_LOOP`
  - `runtime.turn = 1`
  - `runtime.phase = PLANNING`
  - `runtime.battleState.active = true`
  - `runtime.levelData` 存在

结论：

- `F-RUNTIME-05` 技能树学习路径写出陈旧战斗存档的问题已修复

说明：

- 本轮验证的是“学习触发的保存入口会同步当下战斗态”
- 本轮未把“每回合自动存档”作为既定目标

### 5.4 单选技能回归：裂颅一击

执行步骤：

1. 回合 2 选择 `裂颅一击`
2. 检查高亮槽位
3. 将技能放入敌方 `head`
4. `提交规划`
5. `执行`
6. 等待进入回合 3

实测结果：

- 高亮结果：
  - `head = 3`
  - `chest = 0`
  - `abdomen = 0`
  - `arm = 0`
  - `leg = 0`
- 时间线执行日志出现：
  - `[enemy] skill_bite -> DONE`
  - `[self] skill_skull_cracker -> DONE`
- 玩家 HP：`90 -> 60`
- 敌人头部护甲：`7 -> 0`
- 敌人 HP：保持 `50`

结论：

- `F-UI-04` 目标部位约束未映射到槽位高亮的问题已修复
- 单选技能学习后实战链路正常

### 5.5 多选技能回归：多攻测试

执行步骤：

1. 回合 1 选择 `多攻测试`
2. 放入敌方 `head / chest / abdomen / arm`
3. 尝试放入第 5 个 `leg`
4. `提交规划`
5. 检查时间线条目数
6. `执行`
7. 等待进入回合 2

实测结果：

- 第 5 个槽位被正确拦截，已填充槽位保持 `4`
- 时间线条目数为 `5`：
  - `1` 个敌方动作
  - `4` 个我方 `多攻测试` 动作
- 执行日志出现：
  - `[enemy] skill_bite -> DONE`
  - `[self] skill_1771952147024 -> DONE` 共 `4` 条
- 执行后战果：
  - 玩家 HP：`100 -> 90`
  - 敌人 `head`：`10 -> 7`
  - 敌人 `chest`：`18 -> 15`
  - 敌人 `abdomen`：`10 -> 7`
  - 敌人 `arm`：`10 -> 7`
  - 敌人 `leg`：保持 `10`

结论：

- `F-RUNTIME-06` 多选技能“规划正确但执行不完整/目标错位”的问题已修复

### 5.6 敌人自动行为回归

实测结果：

- 回合 1、回合 2 都自动出现敌方 `skill_bite`
- 敌方动作稳定进入时间线并执行完成

结论：

- 敌人自动规划与执行主链路稳定

## 6. 本轮通过项

1. 双引擎与重复资源加载问题已收敛
2. 选关模态残留与技能树焦点问题已收敛
3. 技能树学习保存路径已统一到引擎入口
4. `裂颅一击` 只高亮头部并能真实执行
5. `多攻测试` 4 槽位规划、上限控制、时间线执行、目标部位结算全部通过
6. 敌方自动行为 `skill_bite` 在多回合中稳定执行

## 7. 本轮未纳入阻塞项

1. 时间线 `开始 / 单步 / 直接结算`
2. 战斗内齿轮设置流程
3. 更深层 Buff 分支矩阵
4. 更多敌人策略类型与多敌人协同
5. 自动存档策略专项

## 8. 非阻塞观察

页面仍存在 1 条 Chrome issue：

- 登录输入框缺少 `id` 或 `name` 属性

该项不影响本轮主流程、技能学习、多选技能执行、敌人行为的验收结论。

## 9. 当前阶段判定

本轮可以判定为：

- `今晚压缩计划已完成实现与回归`
- `主流程底座已形成可验收版本`
- `待人工验收`
