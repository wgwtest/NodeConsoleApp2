# 反击 Buff 与受击后置事件记录

创建时间：2026-03-26 00:26

## 1. 本轮目标

在上一轮“技能/buff/敌人行为主链已打通”的基础上，补齐仍阻塞主流程闭环的真实缺口：

- 伤害落地后的事件缺口
- 防御结算后的事件缺口
- `ATTACK` Buff 仍是占位导致 `buff_counter` 无法实战生效

## 2. 本轮已完成项

### 2.1 主伤害链路补事件

已完成：

- `CoreEngine._applyBattleDamage()` 现在会额外发出：
  - `BATTLE_TAKE_DAMAGE`
  - `BATTLE_DEFEND_POST`
- 上述事件携带运行态伤害上下文，可被 BuffSystem 真实消费

### 2.2 反击 Buff 桥接

已完成：

- `BuffSystem._act_attack()` 不再为空实现
- `BuffSystem` 会发出 `BUFF_ATTACK_REQUEST`
- `CoreEngine` 已新增桥接接收逻辑
- `buff_counter` 可基于受击上下文对攻击者发起一次最小反击

### 2.3 测试页可观察性增强

已完成：

- `test/engine_test_v2.html` 已暴露：
  - `window.Engine`
  - `window.__engineTestApi`

目的：

- 便于自动化烟测
- 便于后续回归直接读取运行态

## 3. 本轮验证

### 3.1 静态检查

已执行：

```bash
npx eslint script/engine/CoreEngine.js \
  script/engine/EnemyActionPlanner.js \
  script/engine/DataManagerV2.js \
  script/engine/buff/BuffRegistry.js \
  script/engine/buff/BuffManager.js \
  script/engine/buff/BuffSystem.js
```

结果：

- 通过

### 3.2 浏览器实战对照验证

已执行两组 `level_1_1` 烟测：

#### A. 不挂 `buff_counter`

- 玩家 HP：`100 -> 90`
- 敌人 HP：`50 -> 50`
- 无反击日志

#### B. 挂 `buff_counter`

- 玩家 HP：`100 -> 90`
- 敌人 HP：`50 -> 40`
- 出现日志：
  - `Hero triggered 反击 and dealt 10 HP to 哥布林战士.`

结论：

- 反击 Buff 已真实生效
- 新桥接已进入主流程

## 4. 本轮对应文档

- 验收清单：
  - `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CodexAnylyse/验收清单/2026-03-26-0026-反击Buff与受击后置事件验收清单.md`
- 验收记录：
  - `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CodexAnylyse/验收记录/2026-03-26-0026-level_1_1反击Buff实战验收记录.md`

## 5. 当前未完成项

- Buff 存档/读档恢复链路仍未验证
- `ATTACK` Buff 仍是最小桥接，不是完整通用攻击动作系统
- 多 Buff 同时触发顺序未系统回归
- 敌人技能 alias 仍是运行时过渡方案

## 6. 当前阶段状态

当前阶段应标记为：

- `已完成实现`
- `已完成最小实战验证`
- `待人工验收`

当前阶段仍不能标记为：

- `全部闭环`
- `最终验收通过`

## 7. 下一轮建议

下一轮建议只推进一个切片：

1. Buff 存档/读档链路验证

或：

2. `onTakeDamage` / `onDefendPost` 相关 Buff 的批量回归

不建议下一轮同时做：

- UI 大改
- 内容数据全面重构
- 新一批 Buff 设计扩展
