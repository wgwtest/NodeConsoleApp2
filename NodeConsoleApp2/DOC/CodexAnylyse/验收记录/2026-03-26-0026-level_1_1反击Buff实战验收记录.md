# level_1_1 反击 Buff 实战验收记录

记录时间：2026-03-26 00:26

## 1. 验收目标

验证以下结论是否真实成立：

- `BATTLE_TAKE_DAMAGE` 和 `BATTLE_DEFEND_POST` 已进入主伤害链路
- `buff_counter` 不再只是配置存在，而会真实改变战斗结果
- `ATTACK` Buff 桥接已能在 `level_1_1` 中生效

## 2. 验收环境

- 项目路径：`/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2`
- 服务地址：`http://127.0.0.1:3101/`
- 测试页：`http://127.0.0.1:3101/test/engine_test_v2.html`
- 浏览器：`Firefox 148.0.2 (headless)`
- 驱动：`geckodriver 0.36.0`
- 运行方式：WebDriver 原生协议 + 页面执行脚本
- 静态检查结果：`npx eslint` 通过

## 3. 前置条件

- 本地静态服务已启动
- `engine_test_v2.html` 可正常加载
- 页面已暴露 `window.Engine` 供自动化读取运行态

## 4. 验收步骤

### 4.1 对照组 A：不挂 `buff_counter`

步骤：

1. 打开测试页
2. 登录 `Hero`
3. 选择关卡 `level_1_1`
4. 不给玩家挂任何额外 Buff
5. 提交空规划
6. 执行回合
7. 等待进入第 2 回合

预期：

- 玩家会受到敌人一次攻击
- 敌人 HP 不应额外下降
- 日志不应出现反击文本

实际：

- 进入第 2 回合成功
- 玩家 HP：`100 -> 90`
- 敌人 HP：`50 -> 50`
- 日志无 `triggered 反击`

结论：

- 通过

### 4.2 对照组 B：挂 `buff_counter`

步骤：

1. 打开测试页
2. 登录 `Hero`
3. 选择关卡 `level_1_1`
4. 给玩家挂 `buff_counter`
5. 提交空规划
6. 执行回合
7. 等待进入第 2 回合

预期：

- 玩家仍会受到敌人一次攻击
- `buff_counter` 会在敌人攻击后触发一次反击
- 敌人 HP 应低于对照组
- 日志中应出现反击文本

实际：

- 进入第 2 回合成功
- 玩家 HP：`100 -> 90`
- 敌人 HP：`50 -> 40`
- 日志出现：
  - `Hero triggered 反击 and dealt 10 HP to 哥布林战士.`
  - `哥布林战士: 重锤 dealt 10 HP to Hero.`

结论：

- 通过

## 5. 验收结论

本轮结论：

- `ATTACK` Buff 已不再是占位
- `buff_counter` 已进入真实战斗主流程
- 新增桥接可以影响运行态 HP 结果，不是单纯日志表现
- 本轮可标记为：`已完成实现` + `待人工验收`

## 6. 关键证据摘要

对照组结果：

- 无反击 Buff：敌人 HP `50`
- 有反击 Buff：敌人 HP `40`

差异点：

- 仅挂 `buff_counter` 的场景额外造成 `10 HP` 伤害
- 差异与反击日志一致

## 7. 当前剩余风险

- 反击桥接当前是最小实现，尚未抽象成完整通用攻击动作系统
- 复杂 `ATTACK` Buff 参数化方案尚未正式定义
- Buff 存档/读档链路未回归
- 多 Buff 同时触发的顺序仍需继续验证

## 8. 建议下一步

1. 验证 Buff 存档/读档后的恢复行为
2. 验证 `onTakeDamage`/`onDefendPost` 下的其他 Buff，例如荆棘或受击获甲
3. 评估是否需要把反击桥接升级为正式 reaction attack 子系统
