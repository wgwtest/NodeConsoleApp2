# 2026-03-29 00:52 A4.0 统一内容契约入口与装载基线人工验收清单

## 1. 验收目标

确认 `#53 A4.0 统一内容契约入口与装载基线` 已把内容入口从分散硬编码收口为统一注册表，并且主流程消费入口与回归页都能基于同一装载边界工作。

## 2. 人工检查项

1. 打开 [config.json](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/config.json)
   - 确认存在顶层 `contentRegistry`
   - 确认包含 `player/items/skills/enemies/levels/buffs/slotLayouts`
   - 确认 `skills.byTree.melee_v4_5.path = skills_melee_v4_5.json`
   - 确认 `buffs.schemaVersion = buffs_v2_1_wrapped`
2. 打开 [DataManagerV2.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js)
   - 确认存在 `_buildContentRegistry`
   - 确认 `loadConfigs()` 先加载 `config.json`，再根据 `contentRegistry` 装载内容包
   - 确认运行时保存了 `contentRegistry` 与 `contentPacks`
3. 打开 [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)
   - 确认存在回归项 `config registry 与 DataManager 装载边界统一`
   - 确认页面执行后全部回归通过
   - 通过标准：应为 `22/22 PASS`，不应出现 `FAIL`
4. 打开 [mock_ui_v11.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html)
   - 按 [05-mock_ui_v11主流程测试基线.md](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CodexAnylyse/05-mock_ui_v11主流程测试基线.md) 执行主流程
   - 参考步骤：`开始冒险 -> 关卡选择 -> 选择关卡 -> 点击技能 -> 部署技能槽 -> 提交规划 -> 执行`
   - 确认执行后能自动推进到下一回合
5. 在 [mock_ui_v11.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html) 中点击 `打开技能树`
   - 确认技能树模态窗可以正常打开
   - 确认可以看到技能节点列表与 KP 区域
   - 确认点击 `×` 可以关闭模态窗

## 3. 通过标准

1. 统一内容入口已经以 `contentRegistry` 形式落地，而不是继续散落在测试页和运行时代码中
2. `DataManagerV2` 装载边界已形成共享口径，可作为 skill/buff/level 三条链的共同上游
3. 回归页与 `mock_ui_v11.html` 主流程都能在统一装载入口下正常运行
4. 本轮改动没有把主流程数据加载打断
