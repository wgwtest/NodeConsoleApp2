# 2026-03-29 08:11 A4.1.2 技能装载与技能树消费边界收口人工验收清单

## 1. 验收目标

确认技能装载与运行时消费已统一收口到 `DataManagerV2` 的 skill catalog 边界，且 `mock_ui_v11.html` 的主流程没有因本轮改动回退。

## 2. 代码验收

1. 打开 [DataManagerV2.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js)
   - 确认存在 `getSkillCatalog()`、`getCurrentSkillTreeId()`、`getSkillPackMeta()`
   - 确认 `loadConfigs()` 中生成了 `gameConfig.skillCatalog`
   - 确认 `getSkillConfig()` 读取的是 `skillCatalog.skillsMap`
2. 打开 [UI_SkillTreeModal.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/ui/UI_SkillTreeModal.js)
   - 确认 `_loadDataFromEngine()` 优先读取 `engine.data.getSkillCatalog()`
   - 确认不再把 `gameConfig.skills` 作为唯一依赖
3. 打开 [CoreEngine.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/CoreEngine.js)
   - 确认 `learnSkill()` 改为通过 `this.data.getSkillConfig(skillId)` 获取技能定义
   - 确认 `_buildEffectiveApCostForAllSkills()` 改为通过 `getSkillCatalog().skillsList` 枚举技能
4. 打开 [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)
   - 确认新增 3 条边界回归测试

## 3. 页面验收

1. 启动本地静态服务后打开 [mock_ui_v11.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html)
2. 按下列主流程执行：
   - 点击 `开始冒险`
   - 点击 `关卡选择`
   - 选择 `幽暗森林边缘`
   - 点击 `打开技能树`
   - 选择一个未学习且可学习的技能，例如 `修理护甲`
   - 点击 `学习`
   - 点击 `提交并关闭`
   - 确认技能区新增 `修理护甲`
   - 选择任一技能，例如 `重锤`
   - 部署到任一合法技能槽，例如敌方 `胸部`
   - 点击 `提交规划`
   - 点击 `执行`
3. 确认结果：
   - 页面能进入下一回合
   - 控制台无脚本错误
   - 技能树学习后的技能在主界面可见
   - 规划提交和执行均可完成

## 4. 回归验收

1. 打开 [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)
2. 确认以下条目为 `PASS`
   - `DataManager 暴露技能目录边界 API`
   - `UI_SkillTreeModal 仅消费技能目录边界`
   - `CoreEngine.learnSkill 仅消费 DataManager 技能边界`
3. 确认既有回归没有新增失败

## 5. 验收记录

1. 人工结论：
   - `通过 / 不通过`
2. 备注：
   - `......`
