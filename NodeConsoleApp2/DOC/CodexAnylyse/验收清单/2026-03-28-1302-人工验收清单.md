# 阶段 1 人工验收清单（按反馈修订）

创建时间：2026-03-28 13:02

状态：`待人工验收`

## 1. 使用说明

这份清单只保留人工必测项。

说明：

1. 自动回归页仍是开发侧自测证据，但不要求你逐项核对
2. 切换样本时，不要求战斗内返回
3. 每条样本均可采用：
   - 刷新页面
   - `开始冒险`
   - `关卡选择`
   - 选择目标样本

## 2. 关卡选择页核对

操作：

1. 打开 `http://127.0.0.1:3101/mock_ui_v11.html`
2. 点击 `开始冒险`
3. 点击 `关卡选择`

核对：

- [ ] `人工验收样本` 显示描述
- [ ] `受击获甲验收样本` 显示描述
- [ ] `敌人修甲验收样本` 显示描述
- [ ] `敌人回血验收样本` 显示描述
- [ ] `护甲免伤验收样本` 显示描述

## 3. HP免伤验收

操作：

1. 刷新页面
2. `开始冒险 -> 关卡选择 -> 人工验收样本`
3. 点击 `HP免伤演示`
4. 放入 `global/self`
5. 点击 `提交规划`
6. 点击 `执行`
7. 等待进入 `回合 2`

预期：

- [ ] 玩家 `HP 100 / 100`
- [ ] 玩家头部护甲 `0 / 20`
- [ ] 敌人为 `验收守卫`

## 4. 护甲免伤验收

操作：

1. 刷新页面
2. `开始冒险 -> 关卡选择 -> 护甲免伤验收样本`
3. 点击 `护甲免伤演示`
4. 放入 `global/self`
5. 点击 `提交规划`
6. 点击 `执行`
7. 等待进入 `回合 2`

预期：

- [ ] 玩家 `HP 100 / 100`
- [ ] 玩家头部护甲 `20 / 20`
- [ ] 敌人为 `验收轻击守卫`

## 5. 受击获甲验收

操作：

1. 刷新页面
2. `开始冒险 -> 关卡选择 -> 受击获甲验收样本`
3. 点击 `受击获甲演示`
4. 放入 `global/self`
5. 点击 `提交规划`
6. 点击 `执行`
7. 等待进入 `回合 2`

预期：

- [ ] 玩家 `HP 100 / 100`
- [ ] 玩家头部护甲 `1 / 20`
- [ ] 敌人为 `验收轻击守卫`

## 6. 敌人修甲验收

操作：

1. 刷新页面
2. `开始冒险 -> 关卡选择 -> 敌人修甲验收样本`
3. 点击 `等待`
4. 放入 `global/self`
5. 点击 `提交规划`
6. 点击 `执行`
7. 等待进入 `回合 2`

预期：

- [ ] 技能列表存在 `等待`
- [ ] 敌人名称为 `验收修理守卫`
- [ ] 敌人胸部护甲从开战的 `4 / 24` 提升为 `14 / 24`

## 7. 敌人回血验收

操作：

1. 刷新页面
2. `开始冒险 -> 关卡选择 -> 敌人回血验收样本`
3. 点击 `等待`
4. 放入 `global/self`
5. 点击 `提交规划`
6. 点击 `执行`
7. 等待进入 `回合 2`

预期：

- [ ] 技能列表存在 `等待`
- [ ] 敌人名称为 `验收医护守卫`
- [ ] 敌人 HP 从开战的 `18 / 90` 提升为 `38 / 90`

## 8. 本轮通过判定

满足以下条件可判定本轮通过：

- [ ] 关卡选择页描述正确显示
- [ ] `HP免伤` 可人工复现
- [ ] `护甲免伤` 可人工复现
- [ ] `受击获甲` 可人工复现
- [ ] 敌人修甲可人工复现
- [ ] 敌人回血可人工复现

## 9. 验收记录区

验收人：

- 

验收时间：

- 

验收结论：

- [ ] 通过
- [ ] 不通过

备注：

- 
