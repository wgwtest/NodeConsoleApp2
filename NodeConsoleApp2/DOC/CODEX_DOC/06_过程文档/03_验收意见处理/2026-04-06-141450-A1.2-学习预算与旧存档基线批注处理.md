# 2026-04-06 14:14 A1.2 学习预算与旧存档基线批注处理

状态：`当前有效`

## 1. 来源清单

1. [A1.2-[待验收]-2026-04-06-012747-扩展Buff学习装配执行闭环-人工验收清单.md](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/02_验收清单/A1.2-[待验收]-2026-04-06-012747-扩展Buff学习装配执行闭环-人工验收清单.md)

## 2. 用户意见摘要

1. `5.1` 批注：当前档位里一共只有 `13 KP`，而 `受击备甲` 还带前置、`绝对防御` 单独就要 `8 KP`，按现清单根本不可能学完。
2. 用户要求确认这是不是事实，以及之前分析为什么会得出“可以学完”的结论。

## 3. 原因判断

1. 用户指出的数学问题是成立的。
2. 这不是 `A1.2` 的 Buff 主流程实现失效，而是此前清单把“干净默认档位”和“当前旧存档”混成了同一个前提。
3. 当前登录页的 `开始冒险` 实际逻辑是：
   - 先执行 `loadGame()`
   - 只有没有存档时才 `createNewGame()`
4. 因此即便页面上写的是 `开始冒险`，如果浏览器里已有旧存档，实际进入的仍然可能是旧成长数据；此时 `重置所有技能` 返还出来的总 `KP` 可以只有 `13`，这时按清单当然不可能学完。
5. 我之前的分析错误在于：
   - 自测和回归是基于 `player.json` 的干净默认模板做的
   - 旧回归只校验了“默认模板当前可用 `30 KP` + 默认已学前置路径”这条口径
   - 没有把“点了重置后从空白构筑学习完整链路的预算”写清楚
   - 也没有把“旧存档会被 `开始冒险` 继续读取”写清楚

## 4. 数学核对

### 4.1 本清单要求的完整学习链

从空白构筑学习以下目标技能：

1. `受击备甲 / skill_regroup`
2. `堡垒姿态 / skill_fortify`
3. `绝对防御 / skill_aegis`
4. `锯齿斩 / skill_1771769351059`

对应完整前置链共需 `21 KP`：

1. `skill_block = 1`
2. `skill_savage_charge = 2`
3. `skill_regroup = 2`
4. `skill_fortify = 5`
5. `skill_aegis = 8`
6. `skill_heavy_swing = 1`
7. `skill_1771769351059 = 2`

### 4.2 干净默认档位的预算

`player.json` 默认玩家为：

1. 当前可用 `KP = 30`
2. 默认已学：
   - `skill_heal = 1`
   - `skill_heavy_swing = 1`
   - `skill_savage_charge = 2`

所以干净默认档位点击 `重置所有技能` 后的预算为：

1. `30 + 1 + 1 + 2 = 34 KP`

结论：

1. 干净默认档位下，`34 >= 21`，可以完成清单要求的完整学习链
2. 旧存档档位下，如果 `重置后 KP = 13`，则 `13 < 21`，确实不可能按当前清单学完

## 5. 本轮处理动作

1. 刷新活跃清单 `A1.2`：
   - 明确本节点必须使用干净浏览器档位
   - 明确 `开始冒险` 会优先读取旧存档
   - 明确 `KP < 21` 时应停止当前档位验收
   - 补写完整数学依据
2. 修正 A1.2 自测报告中的错误表述：
   - 从错误的 `30 -> 13`
   - 改为正确的干净默认档位 `34 -> 13`
3. 补强自动回归：
   - 不再只验证“默认模板当前路径存在”
   - 同时验证“默认清档基线下预算可达”

## 6. 影响范围

1. [A1.2-[待验收]-2026-04-06-012747-扩展Buff学习装配执行闭环-人工验收清单.md](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/02_验收清单/A1.2-[待验收]-2026-04-06-012747-扩展Buff学习装配执行闭环-人工验收清单.md)
2. [2026-04-06-012747-A1.2-Buff学习装配执行闭环自测报告.md](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/01_自测报告/2026-04-06-012747-A1.2-Buff学习装配执行闭环自测报告.md)
3. [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)

## 7. 当前回应

1. 你指出的 `13 KP` 数学矛盾是对的。
2. 我之前“可以学完”的结论，来自干净默认模板，不适用于会被 `开始冒险` 续读的旧存档。
3. 本轮已经把这层前提显式写回清单、自测报告和回归逻辑，后续不再把“旧存档预算不足”误判成 `A1.2` 功能实现错误。
