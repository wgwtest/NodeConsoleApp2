# 验收状态同步与 GitHub 对齐记录

时间：2026-04-09 00:27:05 +0800

状态：`已交接`

对应节点：

1. `#19 / A1.2 扩展 Buff 学习装配执行闭环`
2. `#20 / A2.1 扩展敌人策略样本到真实关卡`
3. `#21 / A2.2 敌人行为人工验收路径`
4. `#63 / C1.1 主菜单成长摘要与技能树入口`
5. `#64 / C1.2 学习提交后主菜单成长摘要即时刷新`
6. `#65 / C1.3 结算页直达技能树与构筑入口`
7. `#66 / B3.1 自动存档槽位与入口语义收口`
8. `#68 / B3.2 统一保存协调层与存档回归补强`
9. `#71 / D3.1 动画展示驱动与验证页基线`
10. `#72 / D3.2 SpineAssets 协作规则与导入契约基线`

## 1. 本轮目的

基于主工程目录中的最新人工批注，把本地验收清单命名、本地计划索引、GitHub Issue 执行合同和 GitHub Project 状态重新对齐，消除“本地已通过但远端仍显示待验收”的分叉状态。

## 2. 本轮完成

1. 以主工程目录 `DOC/CODEX_DOC/05_测试文档/02_验收清单/` 中的 `【】` 批注为唯一依据，确认上面 10 个叶子节点已通过人工验收。
2. 将这 10 份本地验收清单统一从 `-[待验收]-` 改名为 `-[已通过]-`。
3. 更新本地待验收总入口与 `README`，使待验收数量、已通过列表和当前阶段状态与实际人工结论一致。
4. 修正本地计划文档、过程文档和测试索引中对这 10 份清单的旧路径引用。
5. 更新 GitHub Issue 正文：
   - `当前状态` 统一改为 `已验收`
   - 验收清单路径统一改为 `-[已通过]-`
   - `#20` 保留“通过但有 HUD / 技能表现后续议题”的备注
   - `#71` 保留“通过但需继续推进动画幅度、形式可配置化”的备注
6. 更新 GitHub Project：
   - 以上 10 个叶子节点状态统一对齐到 `Done`
   - `#15 #16 #67 #14 #37` 对齐到 `Done`
   - `#28 #29 #30` 对齐到 `In Progress`

## 3. 抽检结果

1. `#19`
   - Issue 正文已显示 `当前状态：已验收`
   - Project 状态已显示 `Done`
2. `#71`
   - Issue 正文已显示 `当前状态：已验收`
   - `验收备注` 中保留了动画配置化后续议题
   - Project 状态已显示 `Done`
3. `#15`
   - Project 状态已显示 `Done`
4. `#28 #29 #30`
   - Project 状态已显示 `In Progress`
5. 仓库全文扫描结果：
   - 上述 10 个节点的 `-[待验收]-` 旧文件名引用已全部清空

## 4. 本轮规则确认

1. 人工验收结论只以用户写在文档中的 `【】` 批注为准，不以文档内部预填的“状态 / 结论”字段为准。
2. 当某个 WBS 节点人工验收通过时，必须在同一轮同步三层状态：
   - 本地验收清单命名与本地索引
   - GitHub Issue 执行合同 `当前状态`
   - GitHub Project `Status`
3. `Hum Check` 仍表示“已实现、已自测、待人工验收”；本轮已通过节点不得继续留在待验收总入口中。

## 5. 后续注意事项

1. GitHub GraphQL 在本机代理环境下仍可能出现 `TLS handshake timeout`，后续同步应继续采用“小批次 + 单条重试 + 回读抽检”的方式。
2. 如果用户后续只补充 `【通过，但有后续议题】` 这类结论，应维持节点 `已验收 / Done`，把新增问题转入后续节点跟踪，不回退已通过节点。
