# Graphify 官方可视化输出接入记录

时间：2026-04-11 23:52:00 +0800

## 1. 本轮完成内容

1. 确认 Graphify 包内存在官方 `to_html()` 输出能力
2. 确认当前 CLI 不直接暴露完整构图命令，因此继续使用项目内包装脚本
3. 升级 `tools/graphify_rebuild.sh`
4. 让重建流程同时输出：
   - `graphify-out/graph.json`
   - `graphify-out/GRAPH_REPORT.md`
   - `graphify-out/graph.html`
   - `graphify-out/wiki/index.md`
5. 更新 `AGENTS.md`
6. 更新 Graphify 使用说明
7. 补充本轮自测报告

## 2. 当前用户可见入口

在当前静态服务端口 `3101` 下：

`http://127.0.0.1:3101/graphify-out/graph.html`

## 3. 关键结论

1. 当前工程已具备官方浏览器图谱页面
2. 该页面来自 Graphify 官方 HTML 输出，不是自制 viewer
3. `wiki/index.md` 也已生成，但它更适合编辑器与 AI 走读，不是本地浏览器的主入口
4. 当前 Chrome MCP 与 headless Chrome 在本机环境中都不稳定，因此本轮按策略采用非阻塞证据完成验证
