# Graphify 接入与知识图谱链路记录

时间：2026-04-11 23:30:05 +0800

## 1. 本轮完成内容

1. 通过独立 venv 安装 `graphifyy`
2. 在当前工程执行 `graphify codex install`
3. 修正自动生成的 `AGENTS.md`，改为使用项目内稳定脚本入口
4. 新增 `.graphifyignore`
5. 新增 `tools/graphify_rebuild.sh`
6. 新增 `tools/graphify_query.sh`
7. 新增 Graphify 使用说明文档
8. 更新 `CODEX_DOC README` 与 `00-本地工程策略映射`
9. 更新仓库 `.gitignore`，排除 `NodeConsoleApp2/graphify-out/`
10. 更新 `/home/wgw/.codex/config.toml`，启用 `multi_agent = true`

## 2. 当前正式入口

1. 规则说明
   - `DOC/CODEX_DOC/04_研发文档/11-Graphify接入与知识图谱使用说明.md`
2. 指令入口
   - `AGENTS.md`
3. 执行脚本
   - `tools/graphify_rebuild.sh`
   - `tools/graphify_query.sh`

## 3. 当前结论

1. Graphify 已作为开发辅助层正式接入当前工程
2. 它不依赖 Git hooks
3. 它不混入游戏运行时
4. 它不会阻断 `mock_ui_v11.html` 主流程
5. 当前首轮建图与查询链路均已通过自测

## 4. 后续维护口径

1. 代码发生明显结构变化后，可执行 `./tools/graphify_rebuild.sh`
2. 需要快速回答结构性问题时，可先读 `graphify-out/GRAPH_REPORT.md`
3. 如果未来代码语言面扩展，再按工程实际补齐新的 `tree-sitter-*` 依赖
