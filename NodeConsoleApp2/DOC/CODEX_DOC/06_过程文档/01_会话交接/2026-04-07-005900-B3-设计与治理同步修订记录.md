# B3 设计与治理同步修订记录

时间：2026-04-07 00:59:00 +0800

状态：`当前有效`

对应节点：

1. `#67 / B3 本地存档与读档系统收口`

## 1. 本轮目的

本轮目的不是继续扩写 B3 功能，而是收口上一轮实现后遗留的治理层偏差：

1. 本地设计说明仍在描述改造前行为
2. 总计划和计划 README 仍残留旧阶段 B 编号
3. GitHub Issue / Project README 仍混有旧焦点与旧依赖

## 2. 本轮处理

### 2.1 本地正式文档

已修订：

1. `DOC/CODEX_DOC/02_设计说明/18-本地存档(local_save_design)-设计说明.md`
2. `DOC/CODEX_DOC/02_设计说明/README.md`
3. `DOC/CODEX_DOC/03_研制计划/README.md`
4. `DOC/CODEX_DOC/03_研制计划/01-WBS-L0-NodeConsoleApp2-可交付版本研发总纲.md`
5. `DOC/CODEX_DOC/03_研制计划/03-执行Issue任务契约与验收口径.md`
6. `DOC/CODEX_DOC/03_研制计划/06-WBS-L2-B3-本地存档与读档系统收口.md`

修订重点：

1. 把本地存档设计说明改成当前真实实现
2. 明确 `save_game` = 自动存档槽位
3. 明确自动槽位 = 当前工作线权威快照
4. 明确手动读档会镜像回自动槽位
5. 明确 `requestSave(...)` 是统一保存协调入口
6. 把计划文档中的正式阶段 B 统一到 `#37 -> #26/#27/#67`
7. 把 Project 状态语义统一到 `Todo / In Progress / Hum Check / Done`

### 2.2 GitHub 协同层

已同步：

1. `#67`
   - `Depends On` 从旧 `#38/#39` 改为 `#26/#27`
2. `#26`
   - `Depends On` / `上级节点` 从旧 `#25` 改为 `#37`
3. `#27`
   - `Depends On` / `上级节点` 从旧 `#25` 改为 `#37`
4. Project README
   - 当前焦点切换到 `#37 / #67`
   - 明确 `#25/#38/#39` 为旧 shadow nodes，不再作为当前引用

## 3. 本轮验证

已执行：

1. `codex_regression_runner.html`
   - 结果：`通过 78 / 78，失败 0`
2. `mock_ui_v11.html` 登录页烟测
   - 结果：出现 `新游戏 / 读取存档`
   - 结果：不再出现 `开始冒险`
   - 结果：无存档时 `读取存档` 禁用

对应自测报告：

1. `DOC/CODEX_DOC/05_测试文档/01_自测报告/2026-04-07-005900-B3-设计与治理同步修订自测报告.md`

## 4. 当前状态

当前可以确认：

1. B3 功能实现状态没有回退
2. 本地正式设计与正式计划已和当前实现对齐
3. GitHub Issue / Project README 已和当前正式阶段焦点对齐

## 5. 下一步建议

1. 用户继续按现有 `B3.1 / B3.2` 验收清单做人审
2. 若用户明确 `【通过】`，同轮同步：
   - 验收清单文件状态
   - Issue `当前状态`
   - Project `Status`
3. 若用户继续要求推进后续功能，则保持：
   - 阶段 B 正式主线：`#37 -> #67`
   - 阶段 C 仅按 `Prep` 继续，不误判为正式 `Core`
