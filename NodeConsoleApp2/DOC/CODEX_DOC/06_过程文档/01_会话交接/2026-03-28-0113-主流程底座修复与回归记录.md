# 主流程底座修复与回归记录

记录时间：2026-03-28 01:13

状态：`待用户确认`

## 1. 本轮目标

- 完成今晚压缩计划的实现
- 基于 `mock_ui_v11.html` 做修复后深度回归
- 输出新的自测报告、验收清单和后续研发计划

## 2. 本轮完成

- 修复宿主接入方式，`mock_ui_v11.html` 改为接入默认引擎单例
- 修复系统模态显隐控制，进入战斗后不再残留选关模态
- 修复技能树关闭焦点管理，消除 `aria-hidden` 焦点告警
- 修复技能树学习保存入口，统一走 `engine.saveGame()`
- 修复 `saveBattleState()` 对 `currentScene / battleState` 的同步
- 修复 `UI_SkillPanel` 对 `SCOPE_MULTI_PARTS / candidateParts / selectedParts` 的支持
- 修复 `TurnPlanner.planMany()` 多选技能的目标部位分发
- 新增浏览器回归脚本：
  - `test/codex_regression_runner.html`

## 3. 本轮回归确认

- 自动回归脚本全部通过
- `裂颅一击` 单选高亮与执行通过
- `多攻测试` 4 槽位规划与 4 条 `DONE` 执行通过
- 敌方 `skill_bite` 多回合稳定执行
- 学习技能时写出的战斗存档不再陈旧

## 4. 当前结论

当前可以认定：

- 今晚计划定义的“主流程底座闭环”已达成
- 当前工程已具备继续扩展 Buff / 敌人行为专项的可验证基础

## 5. 非阻塞残余

- 登录输入框仍缺少 `id / name`
- 时间线 `开始 / 单步 / 直接结算` 未纳入本轮专项
- 自动存档策略未纳入本轮专项

## 6. 下一轮建议顺序

1. 先做 Buff 效果矩阵与事件触发专项
2. 再做敌人策略类型扩展与多敌人协同
3. 最后补战斗恢复读档与自动化回归扩面
