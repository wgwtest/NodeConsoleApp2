# WBS-3.4.15 浏览器结算奖励与成长摘要 Smoke 记录

## 背景

WBS-3.4.13 已覆盖主流程进入 `level_1_1` 并执行首回合，WBS-3.4.14 已覆盖三章地图切换和 Boss 关选中。本轮继续补强浏览器层实际游戏闭环：在真实页面中进入关卡后直接调用引擎胜利结算入口，验证首通奖励、进度解锁、结算 UI 和返回主菜单后的成长摘要。

## 改动

更新 `tools/acceptance_click_smoke.mjs`：

- 新增 `runSettlementRewardSmoke(cdpEndpoint, mainUrl)`。
- 在主报告中新增 `settlementReward` 字段。
- 通过真实页面 `mock_ui_v11.html` 执行：
  - 点击 `新游戏`。
  - 进入 `关卡选择`。
  - 选中 `level_1_1`。
  - 点击 `进入关卡`。
  - 等待进入 `BATTLE_LOOP`。
  - 调用 `window.GameEngine.endBattle(true)`。
  - 验证 `BATTLE_SETTLEMENT` 和标题 `战斗胜利`。
  - 点击 `返回主菜单`。
  - 验证回到 `MAIN_MENU`。

更新 `test/acceptance_click_smoke_contract.test.mjs`：

- 契约固定 `runSettlementRewardSmoke`、`GameEngine.endBattle(true)`、`战斗胜利`、`本局奖励`、`首次通关`、`返回主菜单` 等关键路径文本，避免后续误删浏览器结算 smoke。

## 实际浏览器验收

复用当前 enemy worktree 服务：

- `PORT=3111 node app.js`
- Chrome CDP：`http://127.0.0.1:9223`
- 服务工作目录：`/home/wgw/CodexProject/NodeConsoleApp2/.worktree/enemy-design-20260522/NodeConsoleApp2`

验收命令：

```bash
node tools/acceptance_click_smoke.mjs
```

关键结果：

- `settlementReward.levelId = level_1_1`
- `settlementReward.victory = true`
- `settlementReward.rewardKp = 1`
- `settlementReward.firstClear = true`
- `settlementReward.nextLevelId = level_1_2`
- `beforeSkillPoints = 30`
- `afterSkillPoints = 31`
- `afterCompleted = ["level_1_1"]`
- `afterUnlocked = ["level_1_1", "level_1_2"]`
- `currentState = MAIN_MENU`

结算 UI 关键文本：

- `战斗胜利`
- `本局奖励`
- `首次通关`
- `前往下一关：失联路标`
- `返回主菜单`

主菜单成长摘要关键文本：

- `成长摘要`
- `知识点 KP31`
- `最近成长来源`
- `森林外缘：+1 KP / +133 经验 / +69 金币，首次通关`

## 验证命令

```bash
node --test test/acceptance_click_smoke_contract.test.mjs
node --test test/acceptance_click_smoke_contract.test.mjs test/campaign_balance_content.test.mjs test/campaign_balance_simulator.test.mjs
node --test test/skill_buff_battle_robot.test.mjs test/skill_buff_decoupled_runtime.test.mjs test/skill_formal_skill_matrix.test.mjs
node tools/validate_skill_authoring_guard.mjs assets/data/skills_enemy_v1.json assets/data/buffs_v2_7.json
node tools/validate_skill_authoring_guard.mjs assets/data/skills_melee_v4_5.json assets/data/buffs_v2_7.json
git diff --check
```

验证结果：

- 自动点击契约：`1/1 pass`
- 关卡内容、平衡模拟、30 关首回合运行时 smoke：`20/20 pass`
- 技能 / Buff / 战斗运行时：`27/27 pass`
- 敌人技能 authoring guard：通过
- 玩家近战技能 authoring guard：通过
- `git diff --check`：通过

## 当前判断

浏览器自动点击现在覆盖三类路径：

1. 主流程进入 `level_1_1` 并执行一回合。
2. 关卡选择页跨三章切图并选中三章 Boss 关。
3. `level_1_1` 胜利结算、首通奖励、下一关解锁、返回主菜单成长摘要。

这可以证明基础游戏闭环没有断链。仍未覆盖完整人工体验级验收，例如胜利后进入技能树实际学习、从下一关按钮进入后续关卡、以及 1920x1080 战斗 UI 状态反馈截图审计。
