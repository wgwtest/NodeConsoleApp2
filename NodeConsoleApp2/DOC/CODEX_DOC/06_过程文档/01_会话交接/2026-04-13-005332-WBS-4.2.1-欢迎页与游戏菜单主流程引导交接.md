# 2026-04-13 WBS-4.2.1 欢迎页与游戏菜单主流程引导交接

## 1. 本轮落地点

本轮只处理 `WBS-4.2.1` 的最小切片，不碰战斗逻辑：

1. `renderLogin()` 增加 `main-flow` 摘要区
2. `renderMainMenu()` 增加 `main-flow` 摘要区
3. 说明内容全部回链到 [05-mock_ui_v11主流程测试基线](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/01_需求分析/05-mock_ui_v11主流程测试基线.md)

## 2. 当前稳定口径

欢迎页：

1. `新游戏`：创建新的自动存档并进入游戏菜单
2. `读取存档`：直接恢复到存档对应页面
3. 正式主流程顺序：`新游戏 -> 游戏菜单 -> 关卡选择 -> 选择技能并部署到技能槽 -> 提交规划 -> 执行回合`

游戏菜单：

1. `关卡选择`：当前正式主流程入口
2. `技能树 / 构筑`：主流程关联子流程，不直接开战
3. 进入关卡后继续走 `选择技能并部署到技能槽 -> 提交规划 -> 执行回合`

## 3. 已完成验证

执行：

```bash
node --test test/d1_ui_modal_semantics.test.mjs test/d1_information_priority.test.mjs test/c_growth_ui_regression.test.mjs test/level_authoring_summary.test.mjs
```

结果：

1. `18 / 18 PASS`

## 4. 后续建议

下一步可继续承接：

1. 把同样的“为什么点这个按钮”语义补到更多主流程关键页
2. 进入 `WBS-4.2.2` 时，将当前散落提示收口成统一术语层
3. 进入 `WBS-4.2.3` 时，把阻塞 / 空态 / 错误反馈统一为可解释的恢复指引
