# WBS-3.4.2 敌人模板扩充自测记录

日期：2026-05-25

分支：`codex/enemy-design-20260522`

## 1. 本轮目标

推进 `WBS-3.4.2 敌人模板扩充`，让三章 30 个正式关卡具备足够敌人差异和压力角色覆盖。

本轮不解决：

1. 新增或重做敌人技能。
2. 自动试玩器和平衡报告。
3. 根据试玩报告调参。
4. 玩家技能树补洞。

## 2. 改动范围

敌人事实源：

1. `assets/data/enemies.json`
2. `assets/enemy_packs/authoring/enemies_20260525_034200.json`

关卡事实源：

1. `assets/map_packs/current/story_pack_v1/levels.json`
2. `assets/map_packs/authoring/story_pack_v1/levels.json`
3. `assets/data/levels.json`

测试：

1. `test/campaign_balance_content.test.mjs`

## 3. 内容结果

当前结果：

| 项目 | 当前值 |
| --- | ---: |
| 敌人模板总数 | 21 |
| 30 个正式关卡实际使用敌人 | 20 |
| 第一章实际使用敌人 | 8 |
| 第二章实际使用敌人 | 8 |
| 第三章实际使用敌人 | 8 |

每章均覆盖以下角色标签：

1. `role_fast`
2. `role_armor`
3. `role_repair`
4. `role_status`
5. `role_elite`
6. `role_boss`

字段补齐：

1. 所有敌人都有 `race / class / tags / description`。
2. 所有敌人都有 `stats.hp / stats.maxHp / stats.ap / stats.speed`。
3. 所有敌人都有 `head / chest / abdomen / arm / leg` 标准部位。
4. 所有敌人都有非空 `skills`。
5. 所有敌人都有 `presentation.battleSpriteRef`。

技能引用策略：

1. 本轮复用 `assets/data/skills_enemy_v1.json`。
2. 未新增敌人技能，避免越过 `WBS-3.4.3` 的 AP 成本与技能组专项验收。
3. 当前所有敌人技能引用均能在敌人技能包中解析。

## 4. TDD 记录

RED：

```bash
node --test test/campaign_balance_content.test.mjs
```

预期失败点：

1. 敌人模板总数只有 9 个，不满足不少于 18 个。
2. 旧敌人字段不完整。
3. 三章角色覆盖不足。

GREEN：

```bash
node --test test/campaign_balance_content.test.mjs
```

结果：`4/4 pass`。

## 5. 自测命令

已执行：

```bash
node --test test/campaign_balance_content.test.mjs
node --test test/enemy_runtime_catalog.test.mjs test/enemy_editor_workspace.test.mjs test/enemy_editor_page.test.mjs
node --test test/level_select_map_runtime.test.mjs test/level_detail_workspace.test.mjs test/level_detail_editor_page.test.mjs
node --test test/skill_buff_battle_robot.test.mjs
node --test test/level_map_workspace.test.mjs test/level_map_preview_page.test.mjs test/level_map_editor_page.test.mjs
git diff --check
```

结果：

1. 内容约束测试通过：`4/4 pass`。
2. 敌人运行时与敌人编辑器测试通过：`18/18 pass`。
3. 关卡选择与关卡详情编辑器测试通过：`20/20 pass`。
4. 技能战斗机器人测试通过：`2/2 pass`。
5. 地图编辑器、预览页与工作区测试通过：`40/40 pass`。
6. JSON 解析检查通过。
7. `git diff --check` 无输出。

说明：测试日志中仍可能出现 fixture 内可选 `enemySkills` 404 警告，该警告不影响断言。

## 6. 后续任务

下一步进入 `WBS-3.4.3`：

1. 检查所有实际使用敌人的技能数量与 AP 成本。
2. 为普通、进阶、精英、Boss 建立敌人技能组标准。
3. 必要时新增敌人技能，并运行技能护栏。
4. 确保 `EnemyActionPlanner` 能为所有实际使用敌人生成合理首回合行动。

随后进入 `WBS-3.4.4 / WBS-3.4.5`：

1. 建立自动试玩模拟器与报告。
2. 基于报告调敌人数值、敌人技能和敌人池。
3. 只有当报告证明玩家技能树缺反制手段时，才补玩家技能树。
