# WBS-3.4.21 浏览器第二章完整连续推进 Smoke 记录

## 背景

WBS-3.4.20 已把浏览器自动点击扩展到第一章完整 10 关。本轮继续向长期目标推进，把同类强运行时证据扩展到第二章完整 10 关，重点验证跨章节地图切换、第二章关卡进入、战斗规划、执行、结算和第三章解锁链路。

## TDD 记录

先更新 `test/acceptance_click_smoke_contract.test.mjs`，要求自动点击脚本固定以下内容：

- `runChapterTwoProgressionSmoke`
- `chapterTwoProgression`
- `level_2_1` 至 `level_2_10`

红灯命令：

```bash
node --test test/acceptance_click_smoke_contract.test.mjs
```

红灯结果：

- 失败原因：`tools/acceptance_click_smoke.mjs` 尚未声明第二章目标关卡数组，也没有 `runChapterTwoProgressionSmoke`。

实现后同一命令通过：`1/1 pass`。

## 改动

更新 `tools/acceptance_click_smoke.mjs`：

- 将原 `runChapterOneProgressionSmoke` 的主体抽为 `runChapterProgressionSmoke(cdpEndpoint, mainUrl, options)`。
- 第一章保留为包装函数，继续覆盖 `level_1_1` 至 `level_1_10`，并保留 `level_1_1` 结算后学习 `skill_block / 修理护甲` 的验证。
- 新增 `runChapterTwoProgressionSmoke`，覆盖 `level_2_1` 至 `level_2_10`。
- 主报告新增 `chapterTwoProgression` 字段。
- 通用章节推进支持 `mapId`，进入章节首关前会先切换到目标地图：
  - 第一章：`chapter_1_authoring_map`
  - 第二章：`chapter_2_authoring_map`

## 调试记录

第一次浏览器运行失败：

```text
等待超时：level_2_1 节点
```

根因：

- 默认关卡选择页停留在第一章地图。
- 第二章 smoke 在进入 `level_2_1` 前没有切换到 `chapter_2_authoring_map`。

修正：

- 给通用章节推进 helper 增加 `mapId` 参数。
- `enterLevelFromMenu` 打开关卡选择后，先点击 `.level-map-switcher__button[data-map-id="..."]`，并等待 `.level-select-runtime-map.dataset.mapId` 切换完成，再查询目标关卡节点。

## 实际浏览器验收

验收环境：

- `PORT=3111 node app.js`
- Chrome CDP：`http://127.0.0.1:9223`
- 服务进程 cwd：`/home/wgw/CodexProject/NodeConsoleApp2/.worktree/enemy-design-20260522/NodeConsoleApp2`

验收命令：

```bash
APP_BASE_URL=http://127.0.0.1:3111 CDP_ENDPOINT=http://127.0.0.1:9223 node tools/acceptance_click_smoke.mjs
```

关键结果：

- `chapterTwoProgression.mapId = chapter_2_authoring_map`
- `chapterTwoProgression.targetLevelIds` 覆盖 `level_2_1` 至 `level_2_10`。
- `levelEntries.length = 10`
- `roundSnapshots.length = 10`
- `settlementSnapshots.length = 10`
- 10 关均执行了至少一轮真实玩家规划：
  - 自动选择可部署技能。
  - 自动落槽。
  - 点击 `提交规划`。
  - 点击 `执行回合`。
  - `roundExecuted = true`。
- 10 关均产生胜利结算，且每关写入 `completedLevels`。
- 第二章结算链路逐关产出正确 `nextLevelId`：
  - `level_2_1 -> level_2_2`
  - ...
  - `level_2_10 -> level_3_1`
- 最终 `unlockedLevels` 包含 `level_3_1`。
- 最终停留在 `BATTLE_SETTLEMENT / 战斗胜利`。

## 当前判断

浏览器自动点击现在已经证明第一章和第二章各自完整 10 关的入口、战斗规划、执行、结算和下一章解锁链路可用。长期目标仍未完成：强浏览器证据还缺第三章完整 10 关；并且当前章节 smoke 仍通过 `GameEngine.endBattle(true)` 强制结算，不能替代完整自然战斗难度验收。
