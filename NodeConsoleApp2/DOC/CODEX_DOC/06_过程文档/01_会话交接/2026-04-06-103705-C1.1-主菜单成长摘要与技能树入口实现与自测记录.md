# C1.1 主菜单成长摘要与技能树入口实现与自测记录

时间：2026-04-06 10:37:05 +0800

状态：`当前有效`

对应 WBS：`#63 / C1.1`

## 1. 本轮输入

1. 用户要求继续推进当前工程。
2. 远端核对后确认：
   - 阶段 A 仍有 `#18 ~ #21` 待人工验收尾项
   - `#29 / C1 技能树与构筑` 仍是开放工作包，但没有任何 L3 叶子任务
3. 按当前工程策略，不能直接在抽象工作包 `#29` 上裸写实现。

## 2. 本轮关键判断

### 2.1 先补叶子任务，再动代码

因为当前策略要求：

1. 人工验收清单必须严格对应 WBS
2. 工作来源必须来自明确 issue 节点
3. GitHub Issues + sub-issues 是执行树真源

所以本轮先在 `#29` 下补出 `#63 / C1.1`，再进入实现。

### 2.2 本轮只做 C1 的最小 Prep 切片

`C1` 的正式目标是“成长资源来源、技能学习结果、构筑差异形成真实反馈”。

但在阶段 A 尚未完全通过人工验收时，本轮只做低耦合 Prep：

1. 让主菜单能看见当前成长状态
2. 让主菜单能直接进入技能树
3. 不改构筑持久化协议
4. 不扩展成长资源来源

## 3. 本轮实现

### 3.1 GitHub 执行树

落地内容：

1. 新建 `#63 / C1.1 主菜单成长摘要与技能树入口`
2. 将 `#63` 作为 `#29` 的正式 sub-issue

### 3.2 主菜单成长摘要与入口

实现文件：

1. `script/ui/UI_SystemModal.js`

落地内容：

1. `renderMainMenu()` 增加 `成长摘要` 区块
2. 摘要展示：
   - `知识点 KP`
   - `已学技能`
3. 主菜单增加 `技能树 / 构筑` 按钮
4. 点击时发送：

```js
this.engine.eventBus.emit('UI:OPEN_SKILL_TREE', { source: 'MAIN_MENU' });
```

### 3.3 红灯测试

实现文件：

1. `test/codex_regression_runner.html`

新增失败断言：

1. 主菜单存在 `成长摘要`
2. 主菜单存在 `技能树 / 构筑`
3. 点击后发出 `UI:OPEN_SKILL_TREE`
4. 原有 `关卡选择` 不被破坏

首轮回归结果：

1. `通过 67 / 68，失败 1`
2. 失败点：
   - `UI_SystemModal 会渲染成长摘要并提供技能树入口`

## 4. 本轮验证

### 4.1 自动回归

命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=25000 --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html
```

结果：

1. 修复后 `通过 68 / 68，失败 0`

### 4.2 真实页面烟测

主入口：

1. `mock_ui_v11.html`

驱动方式：

1. `开始冒险`
2. 进入 `游戏菜单`
3. 检查 `成长摘要`
4. 点击 `技能树 / 构筑`
5. 关闭技能树
6. 确认主菜单仍可继续导航

本轮烟测页：

1. 本地临时文件 `test-results/c1_1_main_menu_smoke.html`
2. 说明：该页仅用于本地自测，未纳入版本管理

命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=120000 --dump-dom http://127.0.0.1:3101/test-results/c1_1_main_menu_smoke.html
```

结果：

1. `PASS C1.1 主菜单成长摘要与技能树入口烟测通过`
2. 日志包含：
   - 已点击 `开始冒险`
   - 已进入 `游戏菜单`
   - 已确认成长摘要渲染
   - 已点击 `技能树 / 构筑`
   - 已确认技能树弹层打开，且主菜单未被破坏
   - 已关闭技能树弹层

## 5. 新增证据

1. 自测报告：
   - [2026-04-06-103705-C1.1-主菜单成长摘要与技能树入口自测报告.md](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/01_自测报告/2026-04-06-103705-C1.1-主菜单成长摘要与技能树入口自测报告.md)
2. 人工验收清单：
   - [C1.1-[已通过]-2026-04-06-103705-主菜单成长摘要与技能树入口-人工验收清单.md](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/02_验收清单/C1.1-[已通过]-2026-04-06-103705-主菜单成长摘要与技能树入口-人工验收清单.md)

## 6. 当前状态

1. `#63 / C1.1`
   - 已实现
   - 已自测
   - 已形成人工验收清单
2. `#29 / C1`
   - 已开始进入子任务化与实际落地
   - 当前仍处于 `开发中`
3. 阶段 A 的待验收尾项：
   - `#18`
   - `#19`
   - `#20`
   - `#21`
   不受本轮 Prep 切片阻断

## 7. 下一步建议

`#29 / C1` 的下一条建议切片应继续保持低耦合：

1. 继续补第二个 L3 节点
2. 目标聚焦“学习结果如何回流到主菜单 / 下一局的可见反馈”
3. 不直接把长期成长、资源来源和内容扩充揉成一个大节点
