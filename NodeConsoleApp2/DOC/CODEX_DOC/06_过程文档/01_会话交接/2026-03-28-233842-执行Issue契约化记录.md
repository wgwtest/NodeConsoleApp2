# 2026-03-28 23:38 执行 Issue 契约化记录

## 1. 本轮目标

把阶段 A 的执行 issue `#53 ~ #62` 从“只有树和时间字段”收口成“可直接执行、可直接验收”的最小工作契约，并同步本地说明文档。

## 2. 本轮完成项

1. 远端 GitHub issue `#53 ~ #62` 已补齐以下结构：
   - `目标`
   - `Owner`
   - `Write Scope`
   - `Depends On`
   - `验收入口`
   - `验收标准`
   - `输出物`
   - `证据链接`
   - `当前状态`
   - `上级节点`
2. 对技能链执行 issue 补入真实验证入口：
   - `test/skill_editor_test_v3.html`
   - `test/skill_editor_test.html`
   - `test/skill_editor_io_test.html`
   - `mock_ui_v11.html`
3. 对 Buff 链执行 issue 补入真实验证入口：
   - `test/buff_editor_v3.html`
   - `test/buff_editor_v4.html`
   - `test/test_doc/buff_editor_test_doc_v2.md`
   - `mock_ui_v11.html`
4. 对关卡链执行 issue 明确当前现状：
   - 当前没有完整独立关卡编辑器页面
   - 运行时验证入口以 `mock_ui_v11.html` 和 `test/codex_regression_runner.html` 为主
   - 配置验证入口以 `assets/data/levels.json` 和 `assets/data/enemies.json` 为主
5. 新增本地口径文档：
   - [03-执行Issue任务契约与验收口径](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/03_研制计划/03-执行Issue任务契约与验收口径.md)
6. 更新 [开发计划 README](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/03_研制计划/README.md)
   - 让阅读入口能直接指向新的执行契约文档

## 3. 当前结论

当前这一层的任务治理方式已经稳定为：

1. GitHub Project 负责看板、排程、时间、状态
2. GitHub Issue 负责最小执行契约和验收入口
3. `DOC/CODEX_DOC` 负责长说明、验收清单、自测报告、会话交接

这意味着：

1. 用户在 Project 中看结构和推进节奏
2. 用户点进 issue 就能知道具体怎么做、怎么验收
3. 本地文档不再和 Project 抢“谁是计划真源”，而是补充长信息

## 4. 后续建议

下一轮如果继续细化执行 issue，建议默认沿用本轮固定模板。

优先顺序建议：

1. 对后续新建执行 issue 直接套用同样模板，避免再次补录
2. 当某个执行 issue 进入 `已自测` 或 `待人工验收` 时，把对应的自测报告、验收清单路径反链回 issue
3. 若后续引入新的独立编辑器页面，应及时回写对应 issue 的 `验收入口`
