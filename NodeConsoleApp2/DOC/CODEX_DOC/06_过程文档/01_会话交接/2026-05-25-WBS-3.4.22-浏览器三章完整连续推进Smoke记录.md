# WBS-3.4.22 浏览器三章完整连续推进 Smoke 记录

## 背景

WBS-3.4.20 覆盖第一章完整 10 关，WBS-3.4.21 覆盖第二章完整 10 关。本轮继续补齐第三章，并让主浏览器 smoke 同时输出三个章节的章节级推进报告，使三章 30 关都具备浏览器级入口、规划、执行、结算证据。

## TDD 记录

先更新 `test/acceptance_click_smoke_contract.test.mjs`，要求自动点击脚本固定以下内容：

- `runChapterThreeProgressionSmoke`
- `chapterThreeProgression`
- `level_3_1` 至 `level_3_10`

红灯命令：

```bash
node --test test/acceptance_click_smoke_contract.test.mjs
```

红灯结果：

- 失败原因：`tools/acceptance_click_smoke.mjs` 尚未声明第三章目标关卡数组，也没有 `runChapterThreeProgressionSmoke`。

实现后同一命令通过：`1/1 pass`。

## 改动

更新 `tools/acceptance_click_smoke.mjs`：

- 新增 `runChapterThreeProgressionSmoke`。
- 第三章目标关卡为 `level_3_1` 至 `level_3_10`。
- 第三章地图为 `chapter_3_authoring_map`。
- 主报告新增 `chapterThreeProgression` 字段。
- 复用 `runChapterProgressionSmoke`，每关仍执行：
  - 切换到目标章节地图。
  - 进入目标关卡。
  - 自动选择可部署技能。
  - 自动落槽。
  - 点击 `提交规划`。
  - 点击 `执行回合`。
  - 使用 `GameEngine.endBattle(true)` 进入胜利结算。
  - 验证 `completedLevels`、`nextLevelId`、结算状态。

## 实际浏览器验收

验收环境：

- `PORT=3111 node app.js`
- Chrome CDP：`http://127.0.0.1:9223`
- 服务进程 cwd：`/home/wgw/CodexProject/NodeConsoleApp2/.worktree/enemy-design-20260522/NodeConsoleApp2`

验收命令：

```bash
APP_BASE_URL=http://127.0.0.1:3111 CDP_ENDPOINT=http://127.0.0.1:9223 node tools/acceptance_click_smoke.mjs
```

关键结果：

- `chapterOneProgression.targetLevelIds` 覆盖 `level_1_1` 至 `level_1_10`。
- `chapterTwoProgression.targetLevelIds` 覆盖 `level_2_1` 至 `level_2_10`。
- `chapterThreeProgression.targetLevelIds` 覆盖 `level_3_1` 至 `level_3_10`。
- 三个章节各自：
  - `levelEntries.length = 10`
  - `roundSnapshots.length = 10`
  - `settlementSnapshots.length = 10`
  - 10 关均 `roundExecuted = true`
  - 10 关均产生胜利结算并写入 `completedLevels`
- 跨章解锁：
  - 第一章末关 `level_1_10 -> level_2_1`
  - 第二章末关 `level_2_10 -> level_3_1`
- 第三章末关 `level_3_10` 的 `nextLevelId = ""`，结算文案为“当前章节推进已更新”，符合最终章节语义。
- 最终停留在 `BATTLE_SETTLEMENT / 战斗胜利`。

## 当前判断

浏览器自动点击现在已经覆盖三章 30 关的章节级可进入、可规划、可执行、可结算链路。长期目标仍未完成：当前 smoke 用强制胜利证明流程链路，不等同于自然战斗难度已完全合适；后续应补自然战斗或更接近自然策略的长流程验证，用失败证据继续判断敌人强度、敌技绑定和玩家技能树缺口。
