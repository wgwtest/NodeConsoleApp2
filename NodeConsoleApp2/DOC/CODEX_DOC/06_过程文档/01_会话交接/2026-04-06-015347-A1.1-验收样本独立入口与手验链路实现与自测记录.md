# A1.1 验收样本独立入口与手验链路实现与自测记录

时间：2026-04-06 01:53:47 +0800

状态：`当前有效`

对应 WBS：`#18 / A1.1`

## 1. 本轮输入

1. 用户要求继续按既定顺序推进，并注意拉取远端评论。
2. 本轮核对后发现：
   - `#18` 没有新的远端评论
   - `#19` 的“Buff 样本不足”评论已在上一轮被 A1.2 吸收
3. 继续执行 `#18 / A1.1` 时，发现真实页面与旧计划之间存在冲突：
   - 旧 A1.1 口径要求验收样本出现在 `关卡选择`
   - 但 `B2` 已通过约束要求 `关卡选择` 只展示 `story`

## 2. 本轮关键判断

不能直接把旧行为恢复回 `关卡选择`。

原因：

1. 那会破坏 `B2` 已通过的故事关卡流转约束。
2. 当前更合理的收口方式是：
   - 保留故事关卡选择页只显示 `story`
   - 单独补一个 `验收样本` 入口

## 3. 本轮实现

### 3.1 DataManager

1. 抽出 `_getLevelsListByKind(kind)`
2. 保留 `_getStoryLevelsList()`
3. 新增 `_getAcceptanceLevelsList()`
4. 新增 `getAcceptanceLevelSelectEntries()`

### 3.2 UI_SystemModal

1. 主菜单新增 `验收样本`
2. 保留 `关卡选择` 只服务故事关卡
3. 新增 `renderAcceptanceLevelSelect()`
4. 新增说明文案：
   - `本页用于人工验收样本，不影响故事推进。`

### 3.3 回归页

新增并转绿：

1. `DataManager 构建独立验收样本选择入口`
2. `UI_SystemModal 提供独立验收样本入口`

## 4. 本轮验证

### 4.1 TDD 红绿过程

先补失败断言，再改代码：

1. 初次回归结果：
   - `通过 62 / 64，失败 2`
2. 两个失败项：
   - `DataManager 构建独立验收样本选择入口`
   - `UI_SystemModal 提供独立验收样本入口`
3. 实现后再次回归：
   - `通过 64 / 64，失败 0`

### 4.2 真实页面烟测

结论：

1. `关卡选择` 仍只显示 `3` 张故事关卡卡片
2. `验收样本` 入口已出现
3. `HP免伤 / 护甲免伤 / 受击获甲` 三条 Buff 手验链路已恢复
4. `敌人修甲 / 敌人回血` 两条共享样本未被本轮入口改造破坏

## 5. 新增证据

1. 自测报告：
   - [2026-04-06-015347-A1.1-验收样本独立入口与手验链路自测报告.md](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/01_自测报告/2026-04-06-015347-A1.1-验收样本独立入口与手验链路自测报告.md)
2. 人工验收清单：
   - [A1.1-[待验收]-2026-04-06-015347-验收样本独立入口与手验链路-人工验收清单.md](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/02_验收清单/A1.1-[待验收]-2026-04-06-015347-验收样本独立入口与手验链路-人工验收清单.md)

## 6. 当前状态

1. `#18 / A1.1`
   - 已实现
   - 已自测
   - 已形成人工验收清单
   - 建议同步为 `待人工验收 / Hum Check`

## 7. 下一步建议

1. 用户按 A1.1 清单做人工验收
2. 主线继续按既定顺序进入：
   - `#20 / A2.1`
   - 然后 `#21 / A2.2`
