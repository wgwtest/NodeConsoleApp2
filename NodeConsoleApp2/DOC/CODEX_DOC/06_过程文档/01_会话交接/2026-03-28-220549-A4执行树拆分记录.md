# 2026-03-28 22:05 A4 执行树拆分记录

## 1. 本轮目标

把阶段 A 的内容工具链并行启动线进一步拆成可执行的 issue tree，避免 `#50/#51/#52` 停留在过粗粒度。

## 2. 本轮完成项

1. 在 GitHub issue tree 中新增：
   - `#53 WBS L3: A4.0 统一内容契约入口与装载基线`
   - `#54 ~ #56` 技能链执行任务
   - `#57 ~ #59` Buff 链执行任务
   - `#60 ~ #62` 关卡链执行任务
2. 固化新的树关系：
   - `#49 -> #53`
   - `#50 -> #54/#55/#56`
   - `#51 -> #57/#58/#59`
   - `#52 -> #60/#61/#62`
3. 把 `#53 ~ #62` 加入 GitHub Project，并补齐：
   - `Status=Todo`
   - `Phase=A`
   - `Contributes To=M1`
   - `Work Type=Task`
   - 对应的 `Start Date / Target Date`
4. 更新本地计划文档，使阶段 A 文档能直接映射到 `#53 ~ #62`

## 3. 当前结论

当前 A4 的执行结构已经固定为：

1. `#53` 共享基线
2. `#54/#55/#56` 技能链
3. `#57/#58/#59` Buff 链
4. `#60/#61/#62` 关卡链

这意味着：

1. 技能、Buff、关卡三条线可以并行推进
2. 三条线只通过共享入口 `#53` 汇合
3. 每条线内部都按：
   - 契约收口
   - 装载边界
   - 局部验证
   的顺序推进

## 4. 后续建议

下一轮建议直接开工下列任务之一：

1. `#53` 统一内容契约入口与装载基线
2. `#54` 技能 schema 与编辑器字段映射收口
3. `#57` Buff schema 与 workspace 契约收口
4. `#60` 关卡 schema 与敌人池配置映射收口

如果要最大化并行收益，建议：

1. 一人做 `#53`
2. 一人做 `#54`
3. 一人做 `#57`
4. 一人做 `#60`
