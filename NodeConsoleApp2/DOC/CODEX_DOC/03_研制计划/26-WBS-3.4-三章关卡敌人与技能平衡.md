# WBS-3.4 三章关卡敌人与技能平衡

创建时间：2026-05-25

状态：`内容与模拟器闭环已完成 / WBS-3.4.7 第三章 P0 敌人生态已落数据`

归属阶段：`WBS-3 成长与内容闭环`

关联设计：

1. `02_设计说明/S6_敌人系统与编辑器/11-敌人系统(enemy_design)-设计说明.md`
2. `02_设计说明/S4_技能系统与编辑器/06-技能平衡(skill_balance_design)-设计说明.md`
3. `02_设计说明/S4_技能系统与编辑器/29-敌人技能包与技能编辑器模式切换(enemy_skill_pack_editor_mode)-设计说明.md`

## 1. 工作目标

把当前“三章地图 + 少量关卡模板 + 少量敌人样本”的内容状态推进为可试玩、可调参、可回归的三章 30 关内容闭环。

目标包括：

1. 建立 30 个编号关卡的独立事实源。
2. 扩充敌人模板和敌人技能绑定。
3. 建立敌人难度曲线。
4. 建立自动试玩报告。
5. 根据试玩结果判断是否需要补玩家技能树。

## 2. 当前基线

当前主分支 `1c95e24` 数据基线：

| 项目 | 当前值 |
| --- | ---: |
| 地图章节 | 3 |
| 地图节点 | 36 |
| 编号关卡 | 30 |
| 独立 story `levelId` | 30 |
| 路线 / 补给 / 事件变体 | 6 |
| 独立 story 敌人池 | 30 |
| 敌人模板 | 27 |
| 实际被关卡使用的敌人 | 24 |
| 敌人技能 | 36 |
| 玩家技能 | 45 |

当前验证结论：

1. `WBS-3.4.1` 到 `WBS-3.4.5` 的首轮内容、敌人、敌技、模拟器和调参目标已有测试或过程记录证据。
2. `test/campaign_balance_content.test.mjs` 验证 30 关事实源、路线变体、敌人数量、角色覆盖、敌技引用和实例化。
3. `test/campaign_balance_simulator.test.mjs` 验证 30 关固定构筑、低 KP 进度式、Boss 压力、第二章护甲压力和运行时首回合 smoke。
4. `test/acceptance_click_smoke_contract.test.mjs` 固定浏览器 smoke 合同。
5. 当前低 KP 进度式模拟从 `0 KP` 开始可输出完整报告，但该结果只能作为诊断信号，不能作为“必须自然全通”的调参目标。
6. 当前浏览器低 KP 自然连续流程已推进到两章稳定目标，并扩展到第三章诊断入口；第三章失败或互杀应优先进入流派、敌人职责、敌技压力和玩家反制矩阵分析。
7. 当前 30 关实际引用 24 个敌人模板，第三章 `level_3_5` 到 `level_3_8` 已完成章节专属敌人替换；第一、二章仍有旧样本补位，下一阶段重点仍不是继续微调单个伤害数值，而是继续补齐敌人生态和反制关系。

## 3. 工作拆分

### WBS-3.4.1 30 关内容事实源补齐

当前状态：`已完成`

工作内容：

1. 从三章地图节点中抽取 30 个编号关卡。
2. 为每个编号关卡生成独立 `levelId`。
3. 为每个编号关卡生成独立 `enemyPoolId`。
4. 明确 6 个路线 / 补给 / 事件节点的作用。
5. 同步更新：
   - `assets/map_packs/current/story_pack_v1/maps.json`
   - `assets/map_packs/current/story_pack_v1/levels.json`
   - `assets/map_packs/authoring/story_pack_v1/maps.json`
   - `assets/map_packs/authoring/story_pack_v1/levels.json`
   - 必要时同步 `assets/data/levels.json`

验收方法：

1. 统计 30 个编号关卡拥有 30 个独立 `levelId`。
2. 每个编号关卡能通过 `DataManagerV2.instantiateLevel()` 实例化。
3. 每个编号关卡至少绑定 1 个敌人池。
4. 地图节点不再大面积复用 `level_1_1 / level_1_2_story / level_1_3_story`。

### WBS-3.4.2 敌人模板扩充

当前状态：`已完成首轮`

工作内容：

1. 把敌人模板从 9 个扩充到首轮 18-24 个；进入 WBS-3.4.7 后允许在矩阵约束下扩展到 28 个以内。
2. 每章至少设计 6 个实际使用敌人。
3. 每章至少包含：
   - 快攻敌人
   - 护甲敌人
   - 修复 / 防御敌人
   - 状态敌人
   - 精英敌人
   - Boss
4. 每个敌人补齐：
   - `race`
   - `class`
   - `tags`
   - `description`
   - `stats`
   - `bodyParts`
   - `skills`
   - `presentation`

验收方法：

1. `assets/data/enemies.json` 中敌人模板数量达到目标。
2. 实际被 30 个编号关卡使用的敌人不少于 15 个。
3. 敌人模板无缺失关键字段。
4. 敌人引用不会破坏敌人编辑器和地图编辑器测试。

### WBS-3.4.3 敌人技能补全

当前状态：`已完成首轮`

工作内容：

1. 检查所有实际使用敌人的 `skills[]`。
2. 复用已有 `skills_enemy_v1.json` 中可用技能。
3. 必要时新增敌人技能。
4. 保证普通敌人 2 个技能、进阶敌人 2-3 个技能、精英 3 个技能、Boss 4 个技能。
5. 确保敌人技能 AP 成本与敌人 `stats.ap` 匹配。

验收方法：

1. 所有被使用敌人的 `skills[]` 引用都存在。
2. 没有敌人只拥有无法支付 AP 的技能。
3. `EnemyActionPlanner` 能为每个被使用敌人生成首回合行动。
4. 敌人技能包测试通过。

### WBS-3.4.4 自动试玩与平衡报告

当前状态：`已完成模拟器层闭环`

工作内容：

1. 新增自动试玩工具：
   - `tools/campaign_balance_simulator.mjs`
2. 新增内容矩阵测试：
   - `test/campaign_balance_content.test.mjs`
3. 新增模拟器测试：
   - `test/campaign_balance_simulator.test.mjs`
4. 输出报告：
   - `test-results/campaign-balance-report.json`
   - `test-results/campaign-balance-summary.md`

最小试玩维度：

1. 关卡 ID。
2. 玩家构筑。
3. 胜负。
4. 回合数。
5. 玩家剩余 HP。
6. 敌人剩余 HP。
7. 敌人技能使用分布。
8. 失败原因。

验收方法：

1. 自动试玩覆盖 30 个编号关卡。
2. 至少覆盖基础构筑、推荐构筑、偏科构筑。
3. 报告能区分“敌人数值过高”、“敌人技能过强”、“玩家技能树缺反制”、“玩家构筑错误”。

### WBS-3.4.5 调参与玩家技能树补洞

当前状态：`已完成首轮调参；玩家技能树补洞未触发`

工作内容：

1. 根据自动试玩报告调敌人属性。
2. 根据自动试玩报告调敌人技能。
3. 根据自动试玩报告调关卡敌人池。
4. 只有在敌人压力合理但玩家没有反制手段时，才补玩家技能树。
5. 新增或修改玩家技能必须运行技能护栏。

验收方法：

1. 推荐构筑下普通关不应大面积失败。
2. 推荐构筑下 Boss 不应稳定无损通过。
3. 偏科构筑应在对应压力关明显吃亏。
4. 所有技能修改通过技能契约和运行时回归。

### WBS-3.4.6 浏览器低 KP 自然连续诊断入口

当前状态：`实施中`

工作内容：

1. 把 `tools/acceptance_click_smoke.mjs` 中的 `runNaturalProgressiveCampaignSmoke()` 从当前两章目标扩展为三章诊断目标。
2. 更新 `test/acceptance_click_smoke_contract.test.mjs`，固定 `targetChapterCount = 3`、`1920 x 1080` 验收视口、CDP 超时保护和第三章诊断入口。
3. 保留真实 UI 路径：
   - 新游戏
   - 关卡选择
   - 真实技能部署
   - 提交规划
   - 执行回合
   - 自然胜负结算
   - 结算后技能树学习
4. 禁止在该低 KP 自然连续路径里调用 `GameEngine.endBattle(true)`。
5. 报告必须记录：
   - 初始技能树
   - 每关战斗前技能树
   - 每关结算后技能树
   - 每次学习前后快照
   - 每关规划技能
   - 胜负结果和失败摘要
   - completed / unlocked levels
6. 第三章失败不再直接视为验收失败；先归因为自动策略、UI 选点、技能树持久化、敌人数值、敌人技能、关卡敌人池或玩家技能树反制缺口。
7. 禁止为了让自动低 KP 玩家自然全通而写死 3-9 / 3-10 专用伤害阈值或复杂选技策略。

验收方法：

1. `node --test test/acceptance_click_smoke_contract.test.mjs` 通过。
2. 浏览器 smoke 在 1920x1080、`APP_BASE_URL`、`CDP_ENDPOINT` 环境下执行。
3. 低 KP 自然连续路径目标关卡为 30 个正式 story 关卡，但强制门槛只要求前两章稳定通过并进入第三章诊断。
4. 如果第三章自然通过，记录证据，不改平衡数据。
5. 如果第三章失败，输出失败关卡、技能树、规划、生命值、敌人剩余 HP 和失败归因，并转入 WBS-3.4.7 的系统设计矩阵。

### WBS-3.4.7 流派-敌人职责-反制矩阵重整

当前状态：`设计矩阵已落文档 / 第三章 P0 内容实现已完成`

工作内容：

1. 建立玩家流派矩阵：基础输出、破甲、防御补甲、治疗续航、DOT、控制、爆发、吸血 / 斩杀。
2. 建立敌人职责矩阵：快攻、护甲、防御修复、状态压迫、控制、精英组合、Boss 机制。
3. 建立敌技压力矩阵：每个敌技必须说明制造的压力、可反制方式和失败诊断信号。
4. 建立 30 关节奏矩阵：每章 1-2 引入、3-6 变化、7-9 高压检查、10 Boss 收束。
5. 标记旧样本敌人：保留可复用样本，但后续关卡应逐步替换为章节语义清晰的敌人。
6. 只有矩阵证明玩家缺少反制手段时，才补玩家技能树；否则优先调敌人、敌技或关卡敌人池。

验收方法：

1. 设计文档中存在玩家流派、敌人职责、敌技压力和关卡节奏四张矩阵。
2. 每个正式关卡至少能映射到一个敌人职责和一个玩家反制方向。
3. 每个被使用敌人的核心技能至少能映射到一个压力点和一个反制方向。
4. 自动试玩和浏览器 smoke 的失败诊断能回填到矩阵，而不是直接触发数值微调。

首轮内容实现建议：

1. 已补第三章 `level_3_5 / level_3_6 / level_3_7 / level_3_8` 的章节专属敌人。
2. 已新增敌人：
   - `enemy_c3_blood_rite_acolyte`
   - `enemy_c3_mire_chain_jailer`
   - `enemy_c3_twilight_warden`
   - `enemy_c3_plague_standard_bearer`
3. 已新增敌技：
   - `enemy_skill_blood_rite_cut`
   - `enemy_skill_mire_chain_drag`
   - `enemy_skill_twilight_warden_bulwark`
   - `enemy_skill_plague_standard_wave`
4. 每个新增敌人必须先绑定职责、压力点和玩家反制方向，再进入敌人数值和技能设计。

## 4. 交付顺序

历史推荐顺序：

1. `WBS-3.4.1` 先补 30 关事实源。
2. `WBS-3.4.2` 扩敌人。
3. `WBS-3.4.3` 补敌人技能。
4. `WBS-3.4.4` 建自动试玩报告。
5. `WBS-3.4.5` 基于报告调参和补玩家技能树。

当前顺序：

1. 保留 `WBS-3.4.6` 作为真实 UI 诊断入口，不再把 30/30 自然全通作为当前主目标。
2. 优先实施 `WBS-3.4.7`，补齐玩家流派、敌人职责、敌技压力和关卡节奏矩阵。
3. 第三章 P0 敌人生态已完成首轮内容实现；下一步优先继续替换第一、二章旧样本补位。
4. 最后根据自动试玩和浏览器诊断证据调敌人数值、敌人技能或玩家技能树。

## 5. 验证命令

基础验证：

```bash
cd /home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2
node --test test/enemy_runtime_catalog.test.mjs test/enemy_editor_workspace.test.mjs test/enemy_editor_page.test.mjs
node --test test/level_select_map_runtime.test.mjs test/level_detail_workspace.test.mjs test/level_detail_editor_page.test.mjs
node --test test/skill_buff_battle_robot.test.mjs test/skill_editor_file_persistence.test.mjs
```

本轮新增后应补充：

```bash
node --test test/campaign_balance_content.test.mjs test/campaign_balance_simulator.test.mjs
node tools/campaign_balance_simulator.mjs --report test-results/campaign-balance-report.json
```

下一阶段新增验证：

```bash
node --test test/acceptance_click_smoke_contract.test.mjs
APP_BASE_URL=http://127.0.0.1:3111 CDP_ENDPOINT=http://127.0.0.1:9223 node tools/acceptance_click_smoke.mjs
```

## 6. 完成定义

WBS-3.4 完成必须满足：

1. 30 个编号关卡都有独立事实源。当前已满足。
2. 实际被关卡使用的敌人不少于 15 个。当前已满足。
3. 所有被使用敌人技能引用合法。当前已满足。
4. 自动试玩报告覆盖 30 个编号关卡。当前模拟器层已满足。
5. 至少完成一轮基于报告的调参。当前已满足首轮。
6. 如补玩家技能树，必须有报告证明补洞必要性。当前未触发补洞。
7. 文档、数据、测试和报告一致。当前已修正敌人模板、使用敌人和敌技数量基线。
8. 浏览器低 KP 自然连续流程至少稳定覆盖前两章并进入第三章诊断；第三章失败必须能回填到流派、敌人职责、敌技压力或玩家反制矩阵。
9. 下一阶段开始调参前，必须先完成对应敌人生态和反制矩阵检查，避免继续围绕单个关卡过度调数值。
