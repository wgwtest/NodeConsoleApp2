# WBS L1 计划：阶段A 战斗竖切闭环

创建时间：2026-03-28 09:16

最后整理时间：2026-04-08

状态：`已审定，已验收关闭；对应 #14`

## 1. 计划目标

这份计划不是泛泛的“后续方向”，而是 `#14` 的稳定阶段计划文档，目标有三个：

1. 把当前仅能由自动回归证明的 Buff / 敌人策略行为，逐步转成可人工点击验收
2. 在不破坏 `mock_ui_v11.html` 主流程基线的前提下，扩展真实战斗样本
3. 补上战斗中保存 -> 刷新 -> 继续战斗这一条高风险主链路
4. 并行启动技能 / Buff / 关卡三类内容契约与作者工具链

与总计划的关系：

1. 上位文档是 [01-WBS-L0-NodeConsoleApp2-可交付版本研发总纲](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/03_研制计划/01-WBS-L0-NodeConsoleApp2-可交付版本研发总纲.md)
2. 本文聚焦总计划中的阶段 A：战斗竖切闭环
3. 阶段 A 关闭后，本文应保留为 `#14` 的稳定记录，不应被下一阶段内容覆盖
4. 当前激活节点与阅读入口统一记录在 `README.md`
5. 若阶段 A 已达到 `待人工验收`，允许启动阶段 B 的 `Prep`，但不允许把阶段 B `Core` 标记为正式开始
6. 阶段 A 现在不仅承载战斗闭环，也承载内容工具链的并行启动职责

## 1.1 GitHub 执行树映射

本文对应的真实执行树如下：

| WBS 层级 | Issue | 含义 | 本文作用 |
| --- | --- | --- | --- |
| L1 | `#14` | 阶段 A 战斗竖切闭环 | 本文所属阶段节点 |
| L2 | `#15` | A1 Buff 主流程完成 | 本文中 Buff 相关执行批次的上位工作包 |
| L2 | `#16` | A2 敌人行为完成 | 本文中敌人行为相关执行批次的上位工作包 |
| L2 | `#17` | A3 战斗恢复完成 | 本文中战斗恢复相关执行批次的上位工作包 |
| L2 | `#49` | A4 内容契约与作者工具链启动 | 本文中并行内容工具链的共享上游包 |
| L3 | `#18` | A1.1 补齐 Buff 人工验收入口与真实主流程样本 | 对应本文“执行批次 1”的 Buff 入口部分 |
| L3 | `#19` | A1.2 扩展 Buff 学习-装配-执行闭环 | 对应本文“执行批次 2” |
| L3 | `#20` | A2.1 扩展敌人策略样本到真实关卡 | 对应本文“执行批次 3”的敌人样本部分 |
| L3 | `#21` | A2.2 固化敌人行为的人工验收路径 | 对应本文“执行批次 1/3”的敌人验收部分 |
| L3 | `#22` | A3.1 梳理战斗恢复所需的最小 runtime snapshot | 对应本文“执行批次 4”的恢复建模部分 |
| L3 | `#23` | A3.2 恢复后重建 BuffManager 与 planning state | 对应本文“执行批次 4”的恢复重建部分 |
| L3 | `#24` | A3.3 增加恢复后继续执行回归 | 对应本文“执行批次 4”的回归与闭环部分 |
| L3 | `#50` | A4.1 技能编辑契约与编辑链启动 | 对应本文“并行启动线”的技能子线 |
| L3 | `#51` | A4.2 Buff 编辑契约与编辑链启动 | 对应本文“并行启动线”的 Buff 子线 |
| L3 | `#52` | A4.3 关卡编辑契约与编辑链启动 | 对应本文“并行启动线”的关卡子线 |
| L3 | `#53` | A4.0 统一内容契约入口与装载基线 | 对应本文“并行启动线”的共享基线任务 |
| L4 | `#54` | A4.1.1 技能 schema 与编辑器字段映射收口 | 对应技能链的契约收口任务 |
| L4 | `#55` | A4.1.2 技能装载与技能树消费边界收口 | 对应技能链的装载边界任务 |
| L4 | `#56` | A4.1.3 技能编辑链局部验证入口 | 对应技能链的局部验证任务 |
| L4 | `#57` | A4.2.1 Buff schema 与 workspace 契约收口 | 对应 Buff 链的契约收口任务 |
| L4 | `#58` | A4.2.2 Buff 装载与运行时消费边界收口 | 对应 Buff 链的装载边界任务 |
| L4 | `#59` | A4.2.3 Buff 编辑链局部验证入口 | 对应 Buff 链的局部验证任务 |
| L4 | `#60` | A4.3.1 关卡 schema 与敌人池配置映射收口 | 对应关卡链的契约收口任务 |
| L4 | `#61` | A4.3.2 关卡装载与实例化边界收口 | 对应关卡链的装载边界任务 |
| L4 | `#62` | A4.3.3 关卡编辑链局部验证入口 | 对应关卡链的局部验证任务 |

解释：

1. 本文是当前阶段 A 的执行计划，不再自行创造另一套并列树
2. 本文后面出现的“执行批次 1~4”只是当前落地顺序，不等于新的 WBS 阶段
3. 若本文与 GitHub issue tree 冲突，以 `#14 ~ #24` 与 `#49 ~ #62` 的真实树和总计划联合判断为准

## 1.2 时间估计与评估节点

| 批次 | 对应 WBS | Start Date | Target Date | 评估重点 |
| --- | --- | --- | --- | --- |
| 批次 1 | `#18` + `#21` 部分 | `2026-03-25` | `2026-03-29` | 手工可验入口是否成立 |
| 批次 2 | `#19` | `2026-03-29` | `2026-03-30` | Buff 学习-装配-执行链是否可观察 |
| 批次 3 | `#20` + `#21` 部分 | `2026-03-30` | `2026-04-01` | 敌人样本是否形成真实策略差异 |
| 批次 4 | `#22 ~ #24` | `2026-03-29` | `2026-04-03` | 保存-刷新-恢复-继续执行是否闭环 |
| 并行线 A4 | `#49 ~ #62` | `2026-03-28` | `2026-04-03` | 内容契约、作者工具边界与运行时解耦是否落稳 |

阶段 A 最终评估时间：

1. `2026-04-03`
2. 评估结论应同步到总计划、README 和 GitHub Project Roadmap

## 1.3 与后续阶段的并行关系

阶段 A 是正式主线上的当前阶段。

当阶段 A 尚未通过人工验收时：

1. 阶段 A 仍然不能视为 `Closed`
2. 但只要阶段 A 已达到：
   - 实现完成
   - 自测完成
   - 验收清单齐备
   - 会话交接齐备
3. 就允许启动阶段 B 的 `Prep`

阶段 B 可提前启动的内容包括：

1. 结果页契约草案
2. 奖励入账数据样本
3. 关卡流转测试脚手架
4. 不绑定正式战斗结算契约的 UI 壳子

阶段 B 不可提前启动的内容包括：

1. 真实结算入账逻辑
2. 真实关卡完成判定主链路
3. 依赖阶段 A 稳定存档与恢复语义的正式实现

## 1.4 阶段 A 内部的并行启动线

除了战斗主流程闭环外，阶段 A 还应并行启动一条“内容契约与作者工具链”启动线。

这条线的原则是：

1. 技能、Buff、关卡三块按并行逻辑推进
2. 三块都通过完整配置文件互通
3. 三块都不应直接耦合到 `mock_ui_v11.html` 的具体实现
4. 三块的共同上游是统一内容契约层，而不是彼此互相依赖

阶段 A 内并行启动的三条线如下：

1. `#50` 技能编辑链
   - 目标：技能配置契约、技能编辑器、技能树消费逻辑之间的边界明确
2. `#51` Buff 编辑链
   - 目标：Buff 配置契约、Buff 编辑器、运行时消费逻辑之间的边界明确
3. `#52` 关卡编辑链
   - 目标：关卡配置契约、关卡编辑方案、运行时实例化逻辑之间的边界明确

共享上游包为：

1. `#49 A4 内容契约与作者工具链启动`

这三条线在阶段 A 的目标不是“全部做完”，而是：

1. 进入正式推进序列
2. 明确契约边界
3. 确保运行时只消费配置文件
4. 为后续阶段的产品化与内容生产闭环打底

## 2. 当前基线

截至已推送提交 `e3b6c25`，当前已确认的基线如下：

1. `mock_ui_v11.html` 主流程可稳定进入战斗、规划、执行、进入下一回合
2. 多选技能部署与执行已有真实页面证据
3. 下列行为已有自动回归证据：
   - `PREVENT_DAMAGE_HP`
   - `PREVENT_DAMAGE_ARMOR`
   - `HEAL_ARMOR`
   - 敌人受损修甲
   - 敌人低血量回血
4. 当前仍缺：
   - 上述行为的人工可点击验收入口
   - 防御/恢复型敌人的真实关卡接入
   - 战斗中存档恢复验收

## 3. 核心原则

### 3.1 不破坏当前默认主流程

当前用户已接受的主流程基线是：

1. `开始冒险`
2. `关卡选择`
3. 进入唯一关卡
4. 战斗规划
5. `提交规划`
6. `执行`
7. 自动进入下一回合

因此后续扩展遵循：

1. 不轻易把默认入口改成复杂多分支
2. 如需增加人工验收能力，优先增加“验收样本入口”或“验收专用技能”，而不是破坏当前唯一基线

### 3.2 先让行为可验，再继续扩行为

当前真正的阻塞，不是“代码写不出来”，而是“人工无法逐项确认”。

所以后续顺序必须是：

1. 先补人工验收入口
2. 再扩更多 Buff / 敌人行为
3. 最后补战斗恢复

### 3.3 继续沿用工程策略

每一轮切片都必须产出：

1. 自测报告
2. 验收清单
3. 会话交接记录

文档目录仍使用：

- `DOC/CODEX_DOC/`

### 3.4 内容工具链与主流程解耦

阶段 A 默认遵守以下解耦原则：

1. 游戏主流程只消费：
   - Skill 配置文件
   - Buff 配置文件
   - Level 配置文件
2. 技能编辑器、Buff 编辑器、关卡编辑器都属于内容生产工具，不应成为主流程运行时的硬依赖
3. 如果某项主流程实现必须直接依赖编辑器页面本身，说明边界设计有问题，应优先改为依赖配置契约

## 4. 执行批次总览

建议按四个执行批次推进。

### 批次 1：补人工验收入口

对应 WBS：

1. `#18`
2. `#21` 的敌人验收入口部分

目标：

- 让 `HP免伤 / 护甲免伤 / 受击获甲 / 敌人修甲 / 敌人回血` 至少有一条可人工点击复现的路径

输出：

- `mock_ui_v11.html` 中可用于人工验收的技能/关卡入口
- 一份新的人工验收清单

### 批次 2：扩展 Buff 主流程样本

对应 WBS：

1. `#19`

目标：

- 让 Buff 不只是“运行时通过”，而是“在主流程中能学、能放、能执行、能观察结果”

输出：

- 至少 2 条可真实触发的 Buff 主流程样本

### 批次 3：扩展敌人真实行为样本

对应 WBS：

1. `#20`
2. `#21` 的敌人行为固化部分

目标：

- 让真实关卡中的敌人不再只有 `skill_bite`
- 至少出现 3 类真实可观察的敌人选择逻辑

输出：

- 防御型 / 恢复型 / 追击型敌人样本

### 批次 4：补战斗恢复链路

对应 WBS：

1. `#22`
2. `#23`
3. `#24`

目标：

- 支持战斗中保存 -> 刷新 -> 恢复后继续战斗

输出：

- 读档恢复自动回归
- 一条真实页面恢复链路

### 并行启动线：内容契约与作者工具链

这不是替代批次 1~4 的第五个顺序批次，而是阶段 A 内的并行启动线。

包含三条子线：

1. `#50` 技能编辑链
2. `#51` Buff 编辑链
3. `#52` 关卡编辑链

共享基线任务：

1. `#53` 统一内容契约入口与装载基线

阶段 A 内这条并行线的目标：

1. 技能编辑链
   - 明确 `skills_*.json` 的运行时契约、编辑器契约、技能树消费边界
2. Buff 编辑链
   - 明确 `buffs_*.json` 的运行时契约、编辑器契约、模拟器边界
3. 关卡编辑链
   - 明确 `levels.json / enemies.json` 的运行时契约、关卡编辑目标、实例化边界
4. 三条线共同目标
   - DataManager 只从配置源加载
   - 主流程不反向绑定编辑器实现
   - 后续扩展不以“当前技能体系已终版”为前提

### 并行启动线的执行拆分

为保证三条内容链既能并行推进，又不因为共享入口而相互缠绕，阶段 A 现按“共享基线 + 三条独立执行链”拆分：

1. 共享基线
   - `#53`
   - 作用：统一 `assets/data/config.json`、`DataManagerV2`、schema version 和 source registry 的公共边界
2. 技能链
   - `#54` 契约收口
   - `#55` 装载与消费边界
   - `#56` 局部验证入口
3. Buff 链
   - `#57` 契约收口
   - `#58` 装载与消费边界
   - `#59` 局部验证入口
4. 关卡链
   - `#60` 契约收口
   - `#61` 装载与实例化边界
   - `#62` 局部验证入口

这组拆分的执行规则是：

1. `#53` 是共同上游，但不把技能、Buff、关卡三条链绑成串行开发
2. 每条链内部都遵循：
   - 先契约
   - 再装载边界
   - 后局部验证
3. 三条链都允许并行推进各自的 `契约收口` 任务
4. 三条链的 `装载边界` 任务只依赖 `#53` 和各自的契约收口任务
5. 三条链的 `局部验证` 任务只依赖本链的装载边界任务，不互相阻塞

## 5. 分批次详细计划

## 执行批次 1：补人工验收入口

### 5.1 目标

解决你刚刚指出的核心问题：

- 验收清单里有些项只能靠自动回归证明，不能手工点出来

这一阶段结束后，至少以下能力要有人工入口：

1. `HP免伤`
2. `护甲免伤`
3. `受击获甲`
4. 敌人受损修甲
5. 敌人低血量回血

### 5.2 计划改动文件

- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/skills_melee_v4_5.json`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/enemies.json`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/levels.json`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/ui/UI_SystemModal.js`

### 5.3 实施方案

建议不要直接污染当前默认唯一关卡，而是新增一个“验收样本关卡”。

建议新增：

1. `level_1_2_acceptance_buff_enemy`
   - 用于人工验收 Buff 和敌人策略
2. 样本敌人：
   - `enemy_guard_demo`
   - 技能包含修甲/回血/普通攻击
3. 样本技能：
   - `skill_demo_hp_guard`
   - `skill_demo_armor_guard`
   - `skill_demo_on_hit_armor`

这样可以做到：

1. `level_1_1` 仍保留现有稳定基线
2. 新能力在新关卡里人工验收

### 5.4 验收标准

人工可点击复现以下现象：

1. 玩家挂 `HP免伤` 后，被敌人命中时 HP 不下降
2. 玩家挂 `护甲免伤` 后，被敌人命中时护甲不下降，且伤害不穿透 HP
3. 玩家挂 `受击获甲` 后，被敌人命中时先补甲，再由护甲吸收本次伤害
4. 样本敌人护甲受损后，优先用修甲技能
5. 样本敌人低血量时，优先用回血技能

### 5.5 风险

1. `mock_ui_v11.html` 当前技能显示层与真实技能池并不完全一致
2. 新增验收关卡后，关卡选择 UI 可能暴露新的列表/排序问题

### 5.6 完成判定

完成这一阶段后，你应该不需要再问“这个行为我怎么手工确认”。

## 执行批次 2：扩展 Buff 主流程样本

### 6.1 目标

让 Buff 在“学习 -> 装配 -> 执行 -> 观察结果”这一条主流程里完整成立。

### 6.2 计划改动文件

- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/skills_melee_v4_5.json`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/buffs_v2_7.json`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/CoreEngine.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/buff/BuffSystem.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/ui/UI_SkillPanel.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html`

### 6.3 重点任务

1. 建立 Buff 演示技能与 Buff 效果的明确映射
2. 补 `BATTLE_ACTION_PRE / BATTLE_ATTACK_PRE / BATTLE_TAKE_DAMAGE_PRE / BATTLE_ATTACK_POST` 的真实页面样本
3. 让至少两条主动技能在主流程中形成清晰 Buff 联动：
   - 防御链
   - 受击触发链

### 6.4 验收标准

人工验收中至少应能看到：

1. 技能施放后 Buff 图标或状态变化
2. 下一次攻击/受击时 Buff 产生可观察影响
3. Buff 生命周期至少可观察一回合以上

### 6.5 完成判定

完成这一阶段后，Buff 不再只是底层“生效”，而是“主流程中稳定可用”。

## 执行批次 3：扩展敌人真实行为样本

### 7.1 目标

把当前自动回归中的敌人行为逻辑，转成真实关卡中可观察的战斗行为。

### 7.2 计划改动文件

- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/enemies.json`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/levels.json`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/skills_melee_v4_5.json`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/EnemyActionPlanner.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/CoreEngine.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html`

### 7.3 重点任务

1. 增加真实敌人样本：
   - 防御型
   - 恢复型
   - 追击型
2. 扩展策略评分：
   - 对薄弱部位优先攻击
   - 对破甲部位追击
   - 在不同血线下切换技能
3. 做多敌人样本，但先不要求复杂协同

### 7.4 验收标准

人工和自动都能看到至少 3 类行为：

1. 受损修甲
2. 低血量回血
3. 优先追击玩家薄弱/破甲部位

### 7.5 完成判定

完成这一阶段后，敌人行为才算真正从“脚本型”走向“策略型”。

## 执行批次 4：补战斗恢复链路

### 8.1 目标

闭环战斗中存档恢复。

### 8.2 计划改动文件

- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/CoreEngine.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/TurnPlanner.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/ui/UI_SkillPanel.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html`

### 8.3 重点任务

1. 明确战斗中 runtime snapshot 的最小必要字段
2. 确保恢复后：
   - `BuffManager` 重建
   - `planning budget` 重建
   - `timeline / turn / queue` 状态一致
3. 增加恢复后的继续执行回归

### 8.4 验收标准

人工恢复链路：

1. 进入战斗
2. 完成一轮规划
3. 中途保存
4. 刷新页面
5. 恢复战斗
6. 继续规划与执行

预期：

1. 玩家/敌人状态不丢
2. Buff 状态不丢
3. 可继续正常进入下一回合

## 6. 推荐执行顺序

建议采用下面的顺序，避免反复返工：

1. 先做批次 1
   - 原因：先解决“怎么验”
2. 再做批次 2
   - 原因：Buff 的主流程可观察性最差，先补
3. 再做批次 3
   - 原因：敌人策略扩展依赖真实敌人样本和验收入口
4. 最后做批次 4
   - 原因：恢复链路最容易被前面新增状态影响，宜最后闭环

## 7. 每批次输出要求

每完成一个阶段，必须新增：

1. 自测报告
2. 人工验收清单
3. 会话交接记录

建议命名继续沿用：

- `DOC/CODEX_DOC/05_测试文档/01_自测报告/`
- `DOC/CODEX_DOC/05_测试文档/02_验收清单/`
- `DOC/CODEX_DOC/06_过程文档/01_会话交接/`

## 8. 我对这份计划的判断

这份计划是可执行的，原因是：

1. 它先解决当前最大的验收缺口
2. 它不要求一次性重构战斗系统
3. 它把风险最高的“战斗恢复”放到了行为样本稳定之后
4. 它保持了当前 `mock_ui_v11.html` 已验收主流程不被随意破坏

## 9. 建议你优先审定的点

你只需要先拍板以下三件事：

1. 是否接受“新增验收样本关卡”，而不是改坏当前唯一基线关卡
2. 是否接受“先把自动回归能力转成手工可验入口”作为下一阶段最高优先级
3. 是否接受“战斗恢复”放在 Buff / 敌人真实样本之后

如果这三点你认可，我就可以按这份详细计划继续推进。
