# 2026-04-07 00:39 B3.1 自动存档槽位与入口语义收口人工验收清单

## 1. 验收目标

确认 `#66 / B3.1 自动存档槽位与入口语义收口` 已把本地存档入口从“隐式默认读档”收口为“显式新游戏 / 显式读档”，并把 `save_game` 变成用户可理解的正式自动存档槽位。

## 2. 验收入口

1. 主页面：
   [mock_ui_v11.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html)
2. 回归页：
   [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)

## 3. 人工检查项

### 3.1 登录页语义

验证目的：

确认登录页已经不再把“开始冒险”当成默认读档入口。

操作：

1. 打开 `http://127.0.0.1:3101/mock_ui_v11.html`
2. 观察首屏登录模态窗

通过标准：

1. 页面出现 `新游戏`
2. 页面出现 `读取存档`
3. 页面不再出现 `开始冒险`

为什么这能证明本项通过：

1. 这说明用户进入游戏前必须先明确选择“开新局”还是“读已有档”，不再存在默认隐式读档语义。

### 3.2 无存档时的读档入口状态

验证目的：

确认系统不会在没有任何本地存档时，给出一个看似可用但实际无意义的“读取存档”入口。

操作：

1. 在干净 localStorage 环境下打开主页面
2. 观察登录页的 `读取存档` 按钮

通过标准：

1. `读取存档` 按钮处于禁用态
2. 页面出现“当前没有可读取的本地存档，请先创建新游戏。”

为什么这能证明本项通过：

1. 这说明入口状态和真实存档状态已经一致，不再让用户点击一个本来就没有后续的伪入口。

### 3.3 自动存档正式槽位

验证目的：

确认 `save_game` 已经在 UI 上显式呈现为“自动存档”，而不是隐形默认工作副本。

操作：

1. 点击 `新游戏`
2. 进入 `游戏菜单`
3. 点击 `存档 / 读档`

通过标准：

1. 页内存在 `自动存档`
2. `自动存档` 卡片存在 `读取`
3. `自动存档` 卡片不存在 `保存`
4. 仍保留 `手动槽位 1 ~ 3`

为什么这能证明本项通过：

1. `自动存档` 能被看见，说明“默认工作副本”已经变成了正式槽位语义。
2. `自动存档` 不能手动覆盖，说明它和手动槽位职责已被区分。

### 3.4 战斗内菜单语义

验证目的：

确认战斗中的快捷入口已经明确表达“回到当前战斗”，不再和“读取持久化存档”混淆。

操作：

1. 从主菜单进入任意故事关卡
2. 进入战斗后点击齿轮菜单按钮
3. 观察菜单首个快捷入口文案

通过标准：

1. 菜单出现 `返回战斗`
2. 菜单不再出现 `继续游戏`

为什么这能证明本项通过：

1. `返回战斗` 明确表示这是当前内存态快捷返回，不是持久化读档动作，语义边界已经清晰。

### 3.5 回归页核对

验证目的：

确认入口语义和自动存档槽位这组契约已经被自动化固定。

操作：

1. 打开 `http://127.0.0.1:3101/test/codex_regression_runner.html`
2. 等待页面自动执行完成

通过标准：

1. 页面总结果为 `通过 78 / 78，失败 0`
2. 以下条目为 `PASS`：
   - `CoreEngine.login 新游戏入口不应隐式读档`
   - `DataManager.getSaveList 包含自动存档正式槽位`
   - `UI_SystemModal 登录页提供显式新游戏与读档入口`
   - `UI_SystemModal 战斗内菜单使用返回战斗语义`
   - `UI_SystemModal 存档页会显示反馈与槽位元数据`

为什么这能证明本项通过：

1. 这说明不是只靠手工观察“看起来对了”，而是相关技术契约也已经被自动化守住。

【待批注】
