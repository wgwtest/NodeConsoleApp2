# 2026-03-29 07:58 A4.1.1 技能 schema 与编辑器字段映射收口人工验收清单

## 1. 验收目标

确认 `#54 A4.1.1 技能 schema 与编辑器字段映射收口` 已把技能 pack、技能编辑器页面、I/O 页与设计文档统一到 `skills_melee_v4_5.json` 的当前契约，不再存在明显的字段映射冲突。

## 2. 人工检查项

1. 打开 [skills_melee_v4_5.json](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/skills_melee_v4_5.json)
   - 确认 `meta.enums.targetScopes` 包含 `SCOPE_MULTI_PARTS`
   - 确认 `meta.enums.selectionModes` 包含 `random_single`
   - 确认 `meta.enums.tags` 能覆盖 `SCOPE_MULTI_PARTS / DEFENSE / DEMO / UTILITY / WAIT`
2. 在 [skills_melee_v4_5.json](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/skills_melee_v4_5.json) 中抽查任意普通技能与演示技能
   - 确认都存在 `unlock.cost.kp`
   - 确认都存在 `editorMeta.group / locked / editState`
3. 打开 [04-技能编辑器(skill_editor_design)-设计说明.md](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/02_设计说明/04-技能编辑器(skill_editor_design)-设计说明.md)
   - 确认文档中的标准样本文件名已改为 `skills_melee_v4_5.json`
   - 确认文档仍明确区分原子字段、显式保存字段和只读字段
4. 打开 [skill_editor_test_v3.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/skill_editor_test_v3.html)
   - 确认导入按钮文案为 `Import skills (v4_5 pack preferred)`
   - 确认 `target.scope` 下拉中可见 `SCOPE_MULTI_PARTS`
   - 确认 `selection.mode` 下拉中可见 `random_single`
   - 确认 `costs.partSlot.part` 下拉只保留 `head/chest/abdomen/arm/leg`
5. 打开 [skill_editor_io_test.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/skill_editor_io_test.html)
   - 点击 `Load from ../assets/data/skills_melee_v4_5.json`
   - 确认已加载技能数量大于 `0`
   - 点击 `Generate JSON`
   - 确认输出中存在 `$schemaVersion`、`meta`、`skills`
6. 打开 [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)
   - 确认存在回归项 `技能 pack schema 与编辑器字段映射对齐`
   - 确认页面执行后全部回归通过

## 3. 通过标准

1. 技能 pack 的枚举、样本字段和编辑器页面默认口径已经一致
2. 技能编辑器不再默认暴露过期的 6 部位与旧目标范围枚举
3. I/O 测试页默认导入导出的是 pack 结构，而不是把裸数组或 map 继续当成主格式
4. 回归页已经能自动守住这组契约，不需要靠口头解释维持
【通过】