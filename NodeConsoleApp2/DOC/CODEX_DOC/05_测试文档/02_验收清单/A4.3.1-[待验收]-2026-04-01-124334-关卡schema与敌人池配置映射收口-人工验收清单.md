# 2026-04-01 12:43 A4.3.1 关卡 schema 与敌人池配置映射收口人工验收清单

## 1. 验收目标

确认当前项目已经把关卡数据统一切到 wrapped pack 结构，且 `enemyPoolId -> enemyPools.members` 的映射可被稳定识别。

## 2. 验收入口

1. [levels.json](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/levels.json)
2. [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)

## 3. 前置条件

1. 使用本地静态服务打开项目
2. 验收时无需修改任何 JSON 内容

## 4. 人工验收步骤

### 4.1 关卡 pack 结构检查

1. 打开 [levels.json](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/levels.json)
2. 确认顶层存在以下 4 个关键节点：
   - `$schemaVersion`
   - `meta`
   - `enemyPools`
   - `levels`
3. 观察点：
   - `$schemaVersion = levels_v1_wrapped`
   - `enemyPools.pool_story_goblin_edge.members[0].templateId = goblin_01`
   - `levels.level_1_1.waves[0].enemyPoolId = pool_story_goblin_edge`

### 4.2 回归补充佐证

1. 打开 [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)
2. 确认以下条目为 `PASS`
   - `关卡 pack schema 与敌人池配置映射对齐`
   - `验收关卡与样本敌人/技能已配置`
3. 确认既有回归没有新增失败

## 5. 通过标准

满足以下全部条件才算通过：

1. `levels.json` 顶层结构为 wrapped pack，而不是旧的平铺 level map
2. 关卡波次通过 `enemyPoolId` 引用敌人池
3. 回归页中关卡 pack 映射相关条目保持 `PASS`
