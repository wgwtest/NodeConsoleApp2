# 2026-03-29 08:20 A4.1.3 技能编辑链局部验证入口人工验收清单

## 1. 验收目标

确认当前项目已经具备一条明确、可复现的“技能配置产物 -> 运行时消费”验证路径，且验证入口在页面中真实存在。

本清单只采用页面入口、可观察结果和可恢复操作，不要求验收者阅读源码确认。

## 2. 验收入口

1. 打开 [skill_editor_io_test.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/skill_editor_io_test.html)
2. 打开 [mock_ui_v11.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html)
3. 打开 [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)

## 3. 前置条件

1. 所有页面均通过本地静态服务打开
2. 验收开始前，确认不存在旧的 runtime override 残留
3. 若存在残留，先在 I/O 页点击 `Clear Runtime Override`

## 4. 人工验收步骤

### 4.1 产物注入验证

1. 在 [skill_editor_io_test.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/skill_editor_io_test.html) 点击 `Load from ../assets/data/skills_melee_v4_5.json`
2. 点击 `Inject Runtime Probe Sample`
3. 观察点：
   - 技能列表中 `skill_heal` 的名称变为 `治疗·运行时覆写样本`
4. 点击 `Apply Runtime Override`
5. 观察点：
   - 页面状态栏显示 override 已生效

### 4.2 运行时消费验证

1. 打开或刷新 [mock_ui_v11.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html)
2. 点击 `开始冒险`
3. 点击 `关卡选择`
4. 选择唯一关卡
5. 观察点：
   - 技能区出现 `治疗·运行时覆写样本`
   - 说明运行时消费到的已经是注入后的技能产物，而不是默认磁盘数据

### 4.3 恢复验证

1. 返回 [skill_editor_io_test.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/skill_editor_io_test.html)
2. 点击 `Clear Runtime Override`
3. 观察点：
   - 页面状态栏显示 override 已清除
4. 再次刷新 [mock_ui_v11.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html)
5. 重新进入战斗主流程
6. 观察点：
   - 技能区恢复默认名称 `治疗`
   - 不再显示 `治疗·运行时覆写样本`

### 4.4 回归补充佐证

1. 打开 [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)
2. 确认以下条目为 `PASS`
   - `DataManager 支持技能 pack 运行时覆写桥`
   - `skill_editor_io_test 提供 runtime bridge 验证入口`
3. 确认既有回归没有新增失败

## 5. 通过标准

满足以下全部条件才算通过：

1. I/O 页面中可以通过按钮把 probe 样本注入到运行时桥
2. `mock_ui_v11.html` 中可直接观察到技能名称被覆写
3. 清除 override 后，主流程页面中的技能名称恢复默认
4. 整个验证过程可重复执行，且不会永久污染默认数据
5. 回归页相关补充条目保持 `PASS`

## 6. 备注

1. 本清单聚焦“技能产物是否真的进入运行时消费链”
2. 回归页只用于补充佐证，不替代主入口页面验证

## 7. 批注处理记录

用户原批注：

1. 【我已经提过了，不要进行类似确认代码的验收了】

本次处理结果：

1. 已移除原先的“代码验收”段
2. 已改为页面入口、观察点、恢复验证和回归补充佐证
【通过】
