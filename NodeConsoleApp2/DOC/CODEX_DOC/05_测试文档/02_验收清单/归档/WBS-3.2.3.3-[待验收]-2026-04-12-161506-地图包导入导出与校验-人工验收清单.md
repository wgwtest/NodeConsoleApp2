# WBS-3.2.3.3 地图包导入导出与校验人工验收清单

状态：`待验收`【不通过】

对应 WBS：`#103 / WBS-3.2.3.3 地图包导入导出与校验`

## 1. 本清单在验什么

只验证 `#103 / WBS-3.2.3.3`：

1. 是否已有独立地图包 IO 校核页
2. 是否提供合法样例与非法样例
3. 是否能执行 round-trip 检查
4. 是否把 `WBS-3.2.3.2` 的地图编辑职责和本页 IO 职责分开说清楚

## 2. 验收入口

1. 页面入口：
   - ``
2. 共享回归辅助入口：
   - `http://127.0.0.1:3101/test/codex_regression_runner.html?scope=WBS-3.2.3.3`
3. 计划文档：
   - `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/03_研制计划/22-WBS-3.2.3.3-地图包导入导出与校验.md`
4. 自测报告：
   - `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/01_自测报告/2026-04-12-161506-WBS-3.2.3.3-地图包导入导出与校验自测报告.md`

## 3. 为什么这些步骤能证明 #103

`#103` 验的不是地图编辑体验，而是：

1. 地图包是否合法
2. 地图包在导入 / 导出 / round-trip 过程里会不会坏
3. 非法地图包能不能稳定打出结构问题码

所以本清单只要求你看：

1. 页面有没有把合法样例、非法样例和 round-trip 三块说清楚
2. 点击不同按钮后，问题码和摘要会不会按预期变化
3. 页面有没有越权承担地图编辑页职责

## 4. 人工验收步骤

### 4.1 页面用途与职责边界检查

步骤：

1. 打开 `test/level_map_editor_io_test.html`

观察点：

1. 页头明确写出 `地图包导入导出与校验`
2. 页面明确解释：
   - `这是个啥工具`
   - `为什么点这些按钮`
   - `与 3.2.3.2 的关系`
3. 页面明确写出：
   - `WBS-3.2.3.2`
   - `WBS-3.2.3.3`
   - `合法样例`
   - `非法样例`
   - `round-trip`

### 4.2 合法样例检查

步骤：

1. 点击 `Load 合法样例`

观察点：

1. 状态栏显示 `合法样例 已完成分析`
2. `问题码 = 0`
3. 摘要中能看到：
   - `地图数 = 1`
   - `节点 / 边 = 4 / 4`
4. 问题列表显示 `当前输入未发现问题码`

### 4.3 非法样例检查

步骤：

1. 点击 `Load 非法样例`

观察点：

1. 状态栏显示 `非法样例 已完成分析`
2. `问题码 = 4`
3. 问题列表中至少出现：
   - `missing_entry_node`
   - `missing_level_ref`
   - `missing_background_ref`
   - `missing_node_ref`

### 4.4 Round-trip 检查

步骤：

1. 先点击 `Load 合法样例`
2. 再点击 `Run round-trip`

观察点：

1. 状态栏显示 `合法样例 的 round-trip 已执行`
2. `前置问题码 = 0`
3. `回读后问题码 = 0`
4. `结构是否稳定 = 是`

### 4.5 共享回归辅助确认

步骤：

1. 打开 `test/codex_regression_runner.html?scope=WBS-3.2.3.3`

观察点：

1. 页面明确显示当前 scope 为 `WBS-3.2.3.3`
2. 至少能看到以下断言通过：
   - `DataManager 会把地图编辑器纳入作者工具摘要`
   - `level_map_editor_io_test 提供地图包导入导出与校验入口`

说明：

1. 这一步只是辅助确认共享入口没有坏
2. 不能替代你对 IO 页是否足够可理解的判断

## 5. 通过标准

以下全部满足才算通过：

1. 已存在独立地图包 IO 校核页，而不是把逻辑继续塞在地图编辑页里
2. 合法样例归零问题码
3. 非法样例稳定给出关键问题码
4. round-trip 结果稳定
5. 页面说明已经讲清楚用途、按钮原因与边界

## 6. 批注区

请只在本文件使用 `【】` 写结论和批注，例如：

1. `【通过】`
2. `【4.2 合法样例归零；4.3 非法样例问题码正常；4.4 round-trip 稳定】`
