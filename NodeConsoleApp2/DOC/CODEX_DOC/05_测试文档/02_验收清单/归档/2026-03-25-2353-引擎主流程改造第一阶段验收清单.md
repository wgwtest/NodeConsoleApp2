# 引擎主流程改造第一阶段验收清单

创建时间：2026-03-25 23:53

## 1. 验收对象

当前阶段针对战斗引擎运行时的第一轮改造，重点包括：

- 技能执行从旧字段迁移到 `actions[].effect`
- `buffRefs.apply/remove` 接入主流程
- Buff wrapped 数据接入运行时
- Buff action/payload 兼容层
- 敌人行动规划器
- 行动前拦截与 AP 生命周期修正

## 2. 验收范围

代码范围：

- `script/engine/CoreEngine.js`
- `script/engine/DataManagerV2.js`
- `script/engine/EnemyActionPlanner.js`
- `script/engine/buff/BuffRegistry.js`
- `script/engine/buff/BuffManager.js`
- `script/engine/buff/BuffSystem.js`

文档范围：

- `DOC/CODEX_DOC/01_需求分析/00-工程总体分析.md`
- `DOC/CODEX_DOC/06_过程文档/02_历史计划/01-一周工作计划.md`
- `DOC/CODEX_DOC/04_研发文档/02-当前阶段验收说明.md`

## 3. 环境记录

- 项目路径：`/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2`
- 当前阶段性质：引擎层改造，尚未完成整场实战联调
- 已完成静态检查：`eslint`

## 4. 建议验收顺序

1. 文档验收
2. 静态检查验收
3. 代码边界验收
4. 浏览器端最小链路验收
5. 记录问题并决定下一轮修正范围

## 5. 分组验收项

### A. 文档完整性

- [ ] 是否已存在总体分析文档
- [ ] 是否已存在一周工作计划文档
- [ ] 是否已存在当前阶段验收说明
- [ ] 本验收清单是否能独立说明当前阶段范围

失败判定：

- 缺任何一份关键文档
- 文档无法说明“做了什么、没做什么、如何验收”

### B. 静态检查

建议命令：

```bash
npx eslint script/engine/CoreEngine.js \
  script/engine/EnemyActionPlanner.js \
  script/engine/DataManagerV2.js \
  script/engine/buff/BuffRegistry.js \
  script/engine/buff/BuffManager.js \
  script/engine/buff/BuffSystem.js
```

- [ ] 命令可执行
- [ ] 无语法错误
- [ ] 无阻塞级静态错误

失败判定：

- `eslint` 报 error
- 文件无法解析

### C. 运行时协议改造

- [ ] `CoreEngine` 中不再依赖 `skillConfig.type`
- [ ] `CoreEngine` 中不再依赖 `skillConfig.value`
- [ ] `CoreEngine` 中不再依赖 `skillConfig.targetType`
- [ ] 技能执行入口已改为按 `actions[].effect` 执行
- [ ] `buffRefs.apply/remove` 已接入技能执行链路

失败判定：

- 仍存在旧字段主依赖
- 技能执行仍无法消费新 schema

### D. Buff 接入

- [ ] `buffs_v2_7.json` wrapped 结构已正确拆包
- [ ] `BuffRegistry` 可拿到 `buffId -> buffDef`
- [ ] `BuffSystem` 支持大写 action
- [ ] `BuffSystem` 支持 `payload.value/valueType`
- [ ] `BuffManager` 支持数组 `statModifiers`

失败判定：

- Buff 定义仍取不到
- Buff action 无法匹配
- Buff 数值结构无法被运行时消费

### E. 敌人行为

- [ ] 已新增独立的 `EnemyActionPlanner`
- [ ] 敌人不再固定取 `skills[0]`
- [ ] 敌人技能缺失已有运行时 alias 兜底
- [ ] 敌人回合开始会恢复 AP
- [ ] 敌人执行技能会消耗 AP

失败判定：

- 敌人仍然是占位脚本
- 敌人技能因缺失而无法规划

### F. 行动前控制与 AP 生命周期

- [ ] `BATTLE_ACTION_PRE` 已进入真实执行链路
- [ ] 行动被取消时不会误扣 AP
- [ ] 玩家回合 AP 正确初始化
- [ ] 敌人回合 AP 正确初始化

失败判定：

- 控制类 Buff 只存在于数据，不影响真实链路
- 动作取消后 AP 状态错误

### G. 最小浏览器链路

建议验收目标：

- 启动本地静态服务
- 打开测试页
- 进入 `level_1_1`
- 完成一次最小战斗流程

- [ ] 页面可启动
- [ ] 可进入战斗
- [ ] 可规划技能
- [ ] 可提交规划
- [ ] 可开始执行
- [ ] 战斗日志有真实技能执行结果

失败判定：

- 页面打不开
- 进入战斗即报错
- 规划与执行无法贯通

## 6. 当前明确未闭环项

以下内容当前不应按“已通过”验收：

- `ATTACK` 类 Buff 的真实反击链路
- 完整存档恢复中的 Buff 运行态
- 全量敌人技能正式数据化
- `level_1_1` 的完整实战回归报告

## 7. 对应实现文件

- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/CoreEngine.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/EnemyActionPlanner.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/buff/BuffRegistry.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/buff/BuffManager.js`
- `/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/buff/BuffSystem.js`

## 8. 验收记录区

验收人：

验收时间：

验收结果：

- [ ] 通过
- [ ] 不通过
- [ ] 部分通过

问题记录：

后续处理建议：
