# 阶段 1 固定人工验收脚本验收清单

创建时间：2026-03-28 11:14

状态：`待人工验收`

## 1. 自动回归核对

打开：

- `http://127.0.0.1:3101/test/codex_regression_runner.html`

核对：
【自动核对我就不测试了，看console输出感觉意义不大】
- [ ] 页面显示 `通过 21 / 21，失败 0`
- [ ] 页面中无 `FAIL`
- [ ] `PASS BuffSystem 直伤护甲也会触发护甲免伤`
- [ ] `PASS BuffSystem 直伤护甲前也会先触发受击获甲`
- [ ] `PASS DataManager.instantiateLevel 保留验收敌人的残甲状态`

## 2. 关卡选择页核对

打开：

- `http://127.0.0.1:3101/mock_ui_v11.html`

操作：

1. 点击 `开始冒险`
2. 点击 `关卡选择`

核对：

- [√] `人工验收样本` 显示描述
- [√] `受击获甲验收样本` 显示描述
- [√] `敌人修甲验收样本` 显示描述
- [√] `敌人回血验收样本` 显示描述
- [√] `护甲免伤验收样本` 显示描述

## 3. HP免伤验收

操作：

1. 选择 `人工验收样本`
2. 进入战斗后点击 `HP免伤演示`
3. 放入 `global/self`
4. 点击 `提交规划`
5. 点击 `执行`
6. 等待进入 `回合 2`

预期：

- [√] 玩家 `HP 100 / 100`
- [√] 玩家头部护甲 `0 / 20`
- [√] 敌人为 `验收守卫`

## 4. 护甲免伤验收

操作：
【我无法通过右上角的小齿轮图表返回，我只能刷新界面来重新选择关卡】
1. 返回并选择 `护甲免伤验收样本`
2. 点击 `护甲免伤演示`
3. 放入 `global/self`
4. 点击 `提交规划`
5. 点击 `执行`
6. 等待进入 `回合 2`

预期：

- [√] 玩家 `HP 100 / 100`
- [√] 玩家头部护甲 `20 / 20`
- [√] 敌人为 `验收轻击守卫`

## 5. 受击获甲验收

操作：

1. 返回并选择 `受击获甲验收样本`
2. 点击 `受击获甲演示`
3. 放入 `global/self`
4. 点击 `提交规划`
5. 点击 `执行`
6. 等待进入 `回合 2`

预期：

- [√] 玩家 `HP 100 / 100`
- [√] 玩家头部护甲 `1 / 20`
- [√] 敌人为 `验收轻击守卫`

## 6. 敌人修甲验收

操作：

1. 返回并选择 `敌人修甲验收样本`
2. 点击 `治疗`
3. 放入 `global/self`
4. 点击 `提交规划`
5. 点击 `执行`
6. 等待进入 `回合 2`

预期：

- [√] 敌人名称为 `验收修理守卫`
- [√] 敌人胸部护甲从开战的 `4 / 24` 提升为 `14 / 24`
- [√] 玩家战斗流程未卡住

## 7. 敌人回血验收

操作：
【我没理解这个测试样本的意义，我点击治疗，敌人回血？按照你的逻辑通过了，但是我不懂你的逻辑】
1. 返回并选择 `敌人回血验收样本`
2. 点击 `治疗`
3. 放入 `global/self`
4. 点击 `提交规划`
5. 点击 `执行`
6. 等待进入 `回合 2`

预期：

- [√] 敌人名称为 `验收医护守卫`
- [√] 敌人 HP 从开战的 `18 / 90` 提升为 `38 / 90`
- [√] 玩家战斗流程未卡住

## 8. 本轮通过判定

满足以下条件可判定本轮通过：

- [ ] 自动回归 `21 / 21`
- [ ] 关卡选择页描述正确显示
- [ ] `HP免伤` 可人工复现
- [ ] `护甲免伤` 可人工复现
- [ ] `受击获甲` 可人工复现
- [ ] 敌人修甲可人工复现
- [ ] 敌人回血可人工复现

## 9. 验收记录区

验收人：

- wgw

验收时间：

- 

验收结论：

- [ ] 通过
- [ ] 不通过

备注：

- 
