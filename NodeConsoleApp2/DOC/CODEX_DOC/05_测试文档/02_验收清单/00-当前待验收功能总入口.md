# 当前待验收功能总入口

最后整理时间：2026-04-10 23:40:42 +0800

状态：`当前有效`

## 1. 本页用途

本页只保留“当前仍需要你人工验收或重测”的节点，不再重复列出已经通过的项目。

使用规则：

1. 先按本页推荐顺序打开对应清单。
2. 人工结论只写回各自 WBS 清单，用 `【】` 记录。
3. 本页只做导航和范围说明，不替代正式批注记录。

## 2. 统一前置准备

1. 在工程目录启动本地静态服务：

```bash
cd /home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2
PORT=3101 node app.js
```

2. 主流程入口统一使用：
   - `http://127.0.0.1:3101/mock_ui_v11.html`
3. 自动回归辅助页统一使用：
   - `http://127.0.0.1:3101/test/codex_regression_runner.html`
5. 所有页面都必须通过 `http://127.0.0.1:3101/...` 打开，不要直接双击本地文件。

## 3. 今日建议测试顺序

当前无 `待验收` 或 `待重测` 节点。

## 4. 当前结论

1. 你刚完成的这一轮人工验收，已明确结论为“本次功能全部通过”。
2. 原本挂在本页的 `7` 个复测节点，已全部移出当前入口并转入已通过记录。
3. 后续只有当新的 `待验收` / `待重测` 节点产生时，本页才会重新列出测试顺序。

## 5. 当前待复测节点

当前共有 `0` 个待复测节点。

1. 无

## 6. 回查入口

如需回查历史结果或全部清单，请进入：

1. [验收清单 README](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/02_验收清单/README.md)
