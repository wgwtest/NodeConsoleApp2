# 当前待验收功能总入口

最后整理时间：2026-04-13 11:35:00 +0800

状态：`当前有效`

## 1. 使用方式

1. 先只按本页列出的顺序验收当前直接可测节点。
2. 人工结论只写回对应 WBS 清单，用 `【】` 记录。
3. 当前本页只列 `待验收` 或 `待重测` 节点；已通过节点不再回流进来。

## 2. 统一前置准备

1. 在工程目录启动本地静态服务：

```bash
cd /home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2
PORT=3101 node app.js
```

2. 主流程默认入口：
   - `http://127.0.0.1:3101/mock_ui_v11.html`
3. 共享回归辅助入口：
   - `http://127.0.0.1:3101/test/codex_regression_runner.html`
4. 所有页面都必须通过 `http://127.0.0.1:3101/...` 打开，不要直接双击本地文件。

## 3. 今日建议测试顺序

1. `当前无直接待验收节点`
2. `#73 / WBS-4.3.2.1` 与 `#82 / WBS-4.3.5` 仍需先重写清单，暂不进入本轮直接验收入口。

## 4. 当前结论

1. 当前无直接待验收节点。
2. `#100 / #102 / #103` 已根据既有 `【通过】` 结论完成状态同步并移出本入口。
3. `#97 / WBS-4.3.7`、`#89 / WBS-4.1.3`、`#98 / WBS-3.3.3` 已根据既有通过结论移出本入口。

## 5. 补充说明

1. `#73 / WBS-4.3.2.1` 与 `#82 / WBS-4.3.5` 仍是未关闭节点，但当前清单说明还不够可直接执行，所以不放进本轮入口。
2. 如需回查已通过结果或其他历史清单，请进入：
   - [验收清单 README](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/02_验收清单/README.md)
