# 当前待验收功能总入口

最后整理时间：2026-05-17 22:42:39 +0800

状态：`当前有效`

## 1. 使用方式

1. 当前无直接待验收节点。
2. 已通过节点请从 [验收清单 README](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/02_验收清单/README.md) 回查。
3. 后续新增待验收节点时，本页只列 `待验收` 或 `待重测` 节点；已通过节点不得回流进来。

## 2. 统一前置准备

1. 在工程目录启动本地静态服务：

```bash
cd /home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2
PORT=3101 node app.js
```

2. 主流程默认入口：
   - `http://127.0.0.1:3101/mock_ui_v11.html`
3. 共享回归辅助入口：
   - `http://127.0.0.1:3101/test/codex_regression_runner.html`
4. 所有页面都必须通过 `http://127.0.0.1:3101/...` 打开，不要直接双击本地文件。

## 3. 今日建议测试顺序

当前无直接待验收节点，不再要求用户按顺序逐项人工点击。

若后续需要复核本轮自动点击证据，建议按以下自动化顺序执行：

1. 共享浏览器回归：
   - `test/codex_regression_runner.html`
2. 主流程点击路径：
   - `新游戏 -> 游戏菜单 -> 关卡选择 -> 进入战斗 -> 禁用执行确认阻塞反馈 -> 选择技能 -> 部署槽位 -> 提交规划 -> 执行回合`
3. 关卡编辑器 smoke：
   - `node tools/level_editor_cdp_smoke.mjs`
4. 演出验证页与配置器：
   - `test/battle_presentation_probe.html`
   - `test/battle_presentation_configurator.html`

## 4. 当前结论

1. 当前无直接待验收节点。
2. 11 个节点已根据自动点击验收结果转入已通过：
   - `#90 / WBS-4.2.1`
   - `#91 / WBS-4.2.2`
   - `#92 / WBS-4.2.3`
   - `#99 / WBS-3.3.3.1`
   - `#104 / WBS-3.2.2.1`
   - `#105 / WBS-3.2.2.2`
   - `#106 / WBS-3.2.2.3`
   - `#93 / WBS-5.1.1`
   - `#94 / WBS-5.1.2`
   - `#95 / WBS-5.2.1`
   - `#96 / WBS-5.2.2`
3. 当前仍有 `2` 个节点不适合直接人工验收，需要先重写清单再判断是否进入验收：
   - `#73 / WBS-4.3.2.1`
   - `#82 / WBS-4.3.5`

## 5. 自动点击验收事实源

1. 用户已在 2026-05-17 会话中同意采用 Codex 自动点击验收结果替代本轮按顺序人工点击验收。
2. 本轮结论来源是自动化主流程点击、专项页面点击和回归测试结果。
3. 该结论不伪造为用户在每份节点清单内逐条手写的 `【通过】` 批注。
4. 已通过节点的具体证据已写回对应节点清单的“自动点击验收回写”章节。

## 6. 补充说明

1. `#73 / WBS-4.3.2.1` 与 `#82 / WBS-4.3.5` 仍是未关闭节点，但当前清单说明还不够可直接执行，所以不放进本轮直接验收入口。
2. 当前版本候选结论请回查：
   - [01-当前版本候选结论页](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/04_验收结论/01-当前版本候选结论页.md)
