# 2026-04-07 00:39 B3.2 统一保存协调层与存档回归补强人工验收清单
【通过】
## 1. 验收目标

确认 `#68 / B3.2 统一保存协调层与存档回归补强` 已把技能学习、战斗结算、手动槽位保存等主链路收口到统一保存协调层，并补齐对应自动回归。

## 2. 验收入口

1. 主页面：
   [mock_ui_v11.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html)
2. 回归页：
   [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)

## 3. 人工检查项

### 3.1 技能学习提交后的自动存档

验证目的：

确认技能树提交后，进度会通过统一保存协调层写回自动存档，而不是只留在当前内存里。

操作：

1. 打开主页面并点击 `新游戏`
2. 在主菜单点击 `技能树 / 构筑`
3. 任选一个当前可学习节点，点击学习
4. 点击 `提交并关闭`
5. 刷新页面
6. 在登录页点击 `读取存档`
7. 读取 `自动存档`
8. 回到主菜单或再次打开技能树，确认刚才的学习结果仍在

通过标准：

1. 学习提交后刷新页面，重新读取 `自动存档` 仍能看到已学习结果
2. 不需要手动点击“保存到槽位”才能保留这次学习

为什么这能证明本项通过：

1. 这直接验证了技能学习已经不再绕过统一保存链路。

### 3.2 战斗中的手动槽位保存

验证目的：

确认战斗运行态在点击手动槽位保存时，仍然会被正确快照并写入对应手动槽位。

操作：

1. 从主菜单进入任意故事关卡
2. 确认已进入战斗主界面
3. 点击齿轮菜单按钮
4. 点击 `存档 / 读档`
5. 选择任意手动槽位，例如 `手动槽位 1`
6. 点击 `保存`
7. 刷新页面
8. 在登录页点击 `读取存档`
9. 读取刚才保存的手动槽位

通过标准：

1. 手动槽位会显示非空时间、关卡名和回合信息
2. 读取该手动槽位后，能恢复到刚才保存的战斗上下文，而不是跳回空白主菜单

为什么这能证明本项通过：

1. 这说明统一保存协调层没有把“战斗手动保存”打坏，battle runtime 仍然能进入持久化快照。

### 3.3 回归页核对

验证目的：

确认统一保存协调层和相关存档边界已经被自动回归固定。

操作：

1. 打开 `http://127.0.0.1:3101/test/codex_regression_runner.html`
2. 等待页面自动执行完成

通过标准：

1. 页面总结果为 `通过 78 / 78，失败 0`
2. 以下条目为 `PASS`：
   - `CoreEngine.learnSkill 仅消费 DataManager 技能边界`
   - `CoreEngine.endBattle 会发出带 settlement 的结算事件`
   - `CoreEngine 自然执行链会触发胜利结算`
   - `CoreEngine 自然执行链会触发失败结算`
   - `DataManager 显式保存后会刷新存档槽元数据`
   - `CoreEngine.startTurn 仅刷新内存 runtime snapshot`
   - `CoreEngine.commitPlanning 仅刷新内存已提交规划快照`

为什么这能证明本项通过：

1. 前四项证明保存动作已经走统一协调层。
2. 后三项证明“该自动保存的会保存，不该自动保存的不会偷写盘”这条边界仍然成立。

## 4. 说明

本清单不要求人工逐项证明所有内部调用都经过 `requestSave()`。

人工验收关注的是用户可观察结果：

1. 学习提交后是否真的持久化
2. 手动保存是否真的保住战斗上下文
3. 自动回归是否守住“不该落盘的不会偷偷落盘”

【待批注】
