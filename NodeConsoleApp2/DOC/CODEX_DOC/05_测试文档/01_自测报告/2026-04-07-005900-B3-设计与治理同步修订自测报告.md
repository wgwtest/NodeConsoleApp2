# B3 设计与治理同步修订自测报告

时间：2026-04-07 00:59:00 +0800

对应节点：

1. `#67 / B3 本地存档与读档系统收口`

## 1. 自测范围

本轮不新增 B3 代码功能，只验证以下三类修订已经和当前实现一致：

1. 本地正式设计与计划文档
2. GitHub Issue / Project 治理层同步
3. B3 已有代码链路没有被本轮修订破坏

## 2. 修订内容

本轮完成：

1. 重写 `18-本地存档(local_save_design)-设计说明.md`
2. 修正总计划、计划 README、Issue 契约口径文档中的旧编号与旧状态语义
3. 同步 GitHub：
   - `#67` 的依赖由旧 `#38/#39` 改为正式 `#26/#27`
   - `#26/#27` 的阶段父节点由旧 `#25` 改为正式 `#37`
   - Project README 的当前焦点由旧 `#19 / A1.2` 切换到当前 `#67 / B3`

## 3. 自测过程

### 3.1 本地文档一致性检查

检查项：

1. 本地存档设计说明是否准确描述当前实现
2. 总计划是否仍引用旧阶段 B 骨架 `#25/#38/#39`
3. 计划 README 是否仍使用旧的 `Accepted / Pending User Acceptance` Project 状态语义

结果：

1. 设计说明已明确：
   - `save_game` = 自动存档槽位
   - 自动槽位 = 当前工作线权威快照
   - 手动读档会镜像回自动槽位
   - `requestSave(...)` 是统一保存协调入口
2. 总计划当前已使用：
   - `#37` 作为正式阶段 B
   - `#26/#27/#67` 作为当前阶段 B 正式工作包
3. 计划 README 当前已区分：
   - Project 粗粒度状态：`Todo / In Progress / Hum Check / Done`
   - Issue 细粒度状态：`待开发 / 开发中 / 已自测 / 待人工验收 / 已验收 / 阻塞中`

结论：

1. 本地正式文档已与当前实现和当前 GitHub 治理口径一致

### 3.2 GitHub 治理层同步检查

检查项：

1. `#67` issue 正文依赖是否已改为 `#26/#27`
2. `#26/#27` issue 正文是否已改为挂接 `#37`
3. Project README 是否已切换到 `#67 / B3`

结果：

1. `#67` 已显示：
   - `Depends On: #26 B1 / #27 B2 / #17 A3`
2. `#26`、`#27` 已显示：
   - `Depends On: #37 阶段B 单局体验闭环`
   - `上级节点: #37 阶段B 单局体验闭环`
3. Project README 已显示：
   - `Active WBS phase: #37 / Phase B`
   - `Active package: #67 / B3`
   - `Legacy shadow nodes #25 / #38 / #39 are deprecated`

结论：

1. 三层协同中的 Project / Issue 层已和本地正式文档同步

### 3.3 代码回归验证

执行命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=15000 --dump-dom \
  http://127.0.0.1:3101/test/codex_regression_runner.html \
  | sed -n '/<pre id="log">/,/<\\/pre>/p;/<div id="summary">/,/<\\/div>/p'
```

结果：

1. `通过 78 / 78，失败 0`

结论：

1. 本轮治理层修订没有破坏 B3 既有实现与回归边界

### 3.4 主页面烟测

执行命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=10000 --dump-dom \
  http://127.0.0.1:3101/mock_ui_v11.html \
  | rg -n "新游戏|读取存档|开始冒险|当前没有可读取的本地存档|游戏菜单|自动存档|返回战斗"
```

结果：

1. 登录页出现 `新游戏`
2. 登录页出现 `读取存档`
3. 不再出现 `开始冒险`
4. 干净存档环境下 `读取存档` 为禁用态
5. 页面提示 `当前没有可读取的本地存档，请先创建新游戏。`

结论：

1. B3.1 的登录入口语义仍然成立

## 4. 自测结论

本轮修订通过自测。

确认如下：

1. 本地存档设计说明已和真实代码一致
2. 正式计划文档已不再引用旧阶段 B 骨架编号
3. GitHub Issue / Project README 已同步到当前真实焦点
4. B3 既有代码回归仍为 `78 / 78`

## 5. 当前建议

下一步按既有 B3 验收口径继续：

1. 人工验收 `B3.1`
2. 人工验收 `B3.2`
3. 若用户明确 `【通过】`，同轮同步：
   - 验收清单命名状态
   - Issue `当前状态`
   - Project `Status`
