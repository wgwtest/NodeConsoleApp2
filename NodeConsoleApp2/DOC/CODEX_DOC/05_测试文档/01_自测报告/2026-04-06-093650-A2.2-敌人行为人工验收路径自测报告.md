# A2.2 敌人行为人工验收路径自测报告

记录时间：2026-04-06 09:36:50 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#21 / A2.2 固化敌人行为的人工验收路径`

## 1. 本轮目标

把 `A2.1` 已经进入真实关卡的敌人行为，收口成稳定、低摩擦、可重复的人工验收路径：

1. 在 `mock_ui_v11.html -> 开始冒险 -> 验收样本` 中提供完整入口。
2. 页面明确解释：
   - 这是干什么的
   - 为什么建议用 `等待`
   - 修甲 / 回血 / 弱点追击分别应该观察什么
3. 为三类敌人行为都提供稳定样本：
   - 敌人修甲
   - 敌人回血
   - 敌人弱点追击

## 2. 本轮实现

### 2.1 验收样本数据

`assets/data/enemies.json`

1. 新增 `enemy_acceptance_pursuit`
   - 技能：`skill_bite` + `skill_throw_stone`
   - AP：`4`
   - 目标：提供稳定的弱点追击样本敌人

`assets/data/levels.json`

1. 新增 `pool_acceptance_pursuit`
2. 新增 `level_1_7_acceptance_enemy_pursuit`
   - 注入 `skill_demo_wait`
   - 通过 `battlePlayerState` 固定玩家头部高价值目标
3. 更新 `level_1_4_acceptance_enemy_repair` 描述
   - 明确“先只部署等待，再执行”
   - 明确观察点是“胸部残甲先被修补”
4. 更新 `level_1_5_acceptance_enemy_heal` 描述
   - 明确“先只部署等待，再执行”
   - 明确观察点是“敌人先回复自身 HP”
5. 验收样本排序调整为：
   - 修甲 -> 回血 -> 弱点追击 -> 护甲免伤

### 2.2 验收页说明文案

`script/ui/UI_SystemModal.js`

1. 验收样本页 introText 改为多行说明
2. 明确写出：
   - 本页不影响故事推进
   - 敌人行为样本建议优先不部署攻击技能，或只部署 `等待`
   - `修甲 / 回血 / 弱点追击` 三类观察点
   - 这是对 story 行为的稳定复核入口
3. `introText` 支持 `pre-line` 显示，避免说明挤成一行

### 2.3 自动回归

`test/codex_regression_runner.html`

本轮先红后绿，新增并通过 3 条断言：

1. `验收关卡提供确定性的玩家与敌人状态`
   - 补充弱点追击样本关卡与敌人数据断言
2. `EnemyActionPlanner 会在敌人行为验收样本中选出对应策略`
   - 修甲样本应选择 `skill_block`
   - 回血样本应选择 `skill_heal`
   - 弱点追击样本应选择 `skill_throw_stone`
3. `UI_SystemModal 提供独立验收样本入口`
   - 补充 `等待 / 修甲 / 回血 / 弱点追击` 文案断言

## 3. 自测方法

### 3.1 自动回归

命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=25000 --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html
```

结果：

1. `通过 67 / 67，失败 0`
2. A2.2 新增的 3 条断言均为 `PASS`

### 3.2 真实页面烟测

主入口：

1. `http://127.0.0.1:3101/mock_ui_v11.html`

本轮使用本地临时烟测页驱动真实验收路径：

1. 本地临时烟测页：`test-results/a2_2_acceptance_enemy_smoke.html`
2. 说明：该页仅用于本轮本地自测，未纳入版本管理
3. 驱动路径：
   - `开始冒险`
   - `验收样本`
   - 目标样本关卡
   - 选择 `等待`
   - `提交规划`
   - `执行`
4. 每个样本都在第一回合结束后检查真实运行时状态

命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=120000 --dump-dom http://127.0.0.1:3101/test-results/a2_2_acceptance_enemy_smoke.html
```

结果：

1. `通过 3 / 3，失败 0`

## 4. 真实页面结果

### 4.1 敌人修甲样本

关卡：

1. `敌人修甲验收样本`

结果：

1. 玩家用 `等待` 交出首回合
2. 敌人首回合规划为 `skill_block`
3. 修复部位为 `chest`
4. 执行后敌人胸部护甲从 `4` 变为 `14`

结论：

1. 敌人修甲已有稳定、低摩擦的人工验收路径

### 4.2 敌人回血样本

关卡：

1. `敌人回血验收样本`

结果：

1. 玩家用 `等待` 交出首回合
2. 敌人首回合规划为 `skill_heal`
3. 执行后敌人 HP 从 `18` 变为 `38`

结论：

1. 敌人回血已有稳定、低摩擦的人工验收路径

### 4.3 敌人弱点追击样本

关卡：

1. `敌人弱点追击验收样本`

结果：

1. 玩家用 `等待` 交出首回合
2. 敌人首回合规划为 `skill_throw_stone`
3. 命中部位为 `head`
4. 执行后玩家头部护甲从 `20` 变为 `0`

结论：

1. 敌人弱点追击已有稳定、低摩擦的人工验收路径

## 5. 风险与边界

本轮没有做的事：

1. 不重新验证 `A2.1` 的 story 关卡是否仍然全部成立
2. 不处理 `A3` 的保存 / 刷新 / 恢复链路
3. 不扩展新的敌人策略类别，只固化三类已有行为的验收路径

因此本轮结论是：

1. `A2.2` 解决的是“如何稳定地验”
2. `A2.1` 解决的是“真实 story 里已经有”
3. 两者职责仍然分离，没有互相替代

## 6. 结论

本轮可确认：

1. `mock_ui_v11.html` 已具备独立的敌人行为验收入口。
2. 页面文案已经说明样本用途、操作原因、观察点和与 story 的关系。
3. 修甲 / 回血 / 弱点追击三类敌人行为都可用 `等待 -> 提交规划 -> 执行` 稳定复核。
4. 自动回归与真实页面烟测均已通过。
5. `#21 / A2.2` 已完成实现与自测，可进入人工验收。
