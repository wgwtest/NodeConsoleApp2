# C2.1 故事关卡内容元数据与选择页提示收口自测报告

时间：2026-04-07 01:33:44

状态：`通过`

对应节点：

1. `#69 WBS L3: C2.1 故事关卡内容元数据与选择页提示收口`

## 1. 自测目标

确认 story 关卡选择入口已经具备正式的内容提示信息，并且没有破坏原有关卡解锁态与主流程入口。

## 2. 本轮改动范围

1. `assets/data/levels.json`
2. `script/engine/DataManagerV2.js`
3. `script/ui/UI_SystemModal.js`
4. `mock_ui_v11.css`
5. `test/codex_regression_runner.html`

## 3. 红绿过程

先补失败测试：

1. `DataManager 透出故事关卡内容元数据与奖励预览`
2. `UI_SystemModal 会渲染故事关卡内容提示`

红灯结果：

1. `difficultyLabel 未透出`
2. `故事关卡卡片缺少难度提示`

随后补实现并复跑。

## 4. 执行结果

执行命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=15000 --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html | rg -n "通过 [0-9]+ /|FAIL |PASS DataManager 透出故事关卡内容元数据与奖励预览|PASS UI_SystemModal 会渲染故事关卡内容提示"
```

结果摘要：

1. `通过 80 / 80，失败 0`
2. `PASS DataManager 透出故事关卡内容元数据与奖励预览`
3. `PASS UI_SystemModal 会渲染故事关卡内容提示`

补充烟测：

执行动作：

1. 使用临时 CDP 烟测脚本驱动 `mock_ui_v11.html` 真实入口
2. 自动完成：
   - `新游戏 -> 关卡选择`

结果摘要：

1. `SMOKE PASS`
2. 页面文本中已确认出现：
   - `难度`
   - `敌人风格`
   - `奖励预览`
   - `构筑提示`
   - `弱点追击 / 低血自疗 / 残甲修补`

## 5. 自测结论

可以确认：

1. story 关卡正式透出 `selectionMeta + rewards`
2. 关卡选择卡片会展示难度、敌人风格、奖励预览、构筑提示
3. 新增信息未破坏既有回归页覆盖的主流程契约
4. `mock_ui_v11.html` 真实入口已验证新增提示可见

## 6. 残余说明

1. 本轮主要依赖自动回归页与针对性 UI 断言
2. `mock_ui_v11.html` 的人工观察仍需在后续人工验收中确认页面理解是否符合预期
