# D1.1 主入口与主流程按钮语言统一基线自测报告

记录时间：2026-04-09 17:20:36 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#87 / D1.1 主入口与主流程按钮语言统一基线`

## 1. 本轮目标

把主入口和主流程中最容易混淆的三处按钮语义固定成统一口径：

1. 时间线控制区不再显示脱离上下文的 `开始`
2. 战斗主界面的技能树入口与主菜单统一为 `技能树 / 构筑`
3. 回合主执行按钮明确为 `执行回合`，不再与 `提交规划` 混淆

## 2. 本轮实现

涉及文件：

1. `mock_ui_v11.html`
2. `script/ui/UI_TimelineBlock.js`
3. `test/codex_regression_runner.html`
4. `DOC/CODEX_DOC/01_需求分析/05-mock_ui_v11主流程测试基线.md`
5. `DOC/CODEX_DOC/03_研制计划/12-WBS-L2-D1-UI统一.md`
6. `DOC/CODEX_DOC/03_研制计划/README.md`

本轮变更：

1. `mock_ui_v11.html`
   - `timelineStartBtn` 从 `开始` 改为 `时间轴开始`
   - `btnOpenSkillTree` 从 `打开技能树` 改为 `技能树 / 构筑`
   - `btnExecute` 从 `执行` 改为 `执行回合`
2. `UI_TimelineBlock`
   - 播放状态文案从 `开始 / 暂停` 改为 `时间轴开始 / 时间轴暂停`
3. `codex_regression_runner.html`
   - 新增 D1.1 专项断言，固定三处按钮文案基线
4. `05-mock_ui_v11主流程测试基线.md`
   - 同步主流程文案，避免页面与测试基线分叉

## 3. 自测方法

### 3.1 红灯基线

命令：

```bash
node <<'NODE'
const fs = require('fs');
const html = fs.readFileSync('mock_ui_v11.html', 'utf8');
const timelineSource = fs.readFileSync('script/ui/UI_TimelineBlock.js', 'utf8');
function assert(condition, message) {
  if (!condition) throw new Error(message);
}
assert(/id="timelineStartBtn">时间轴开始</.test(html), 'mock_ui_v11.html 缺少“时间轴开始”按钮文案');
assert(!/id="timelineStartBtn">开始</.test(html), 'mock_ui_v11.html 仍保留语义过泛的“开始”按钮文案');
assert(/id="btnOpenSkillTree">技能树\\s*\\/\\s*构筑</.test(html), 'mock_ui_v11.html 未统一技能树入口为“技能树 / 构筑”');
assert(!/id="btnOpenSkillTree">打开技能树</.test(html), 'mock_ui_v11.html 仍保留“打开技能树”旧文案');
assert(/id="btnExecute">执行回合</.test(html), 'mock_ui_v11.html 未统一主执行按钮为“执行回合”');
assert(!/id="btnExecute">执行</.test(html), 'mock_ui_v11.html 仍保留模糊的“执行”按钮文案');
assert(timelineSource.includes("isPlaying ? '时间轴暂停' : '时间轴开始'"), 'UI_TimelineBlock 未统一时间轴按钮文案');
NODE
```

结果：

1. 修改前红灯首个失败为：`mock_ui_v11.html 缺少“时间轴开始”按钮文案`

### 3.2 绿灯复核

命令：

```bash
node <<'NODE'
const fs = require('fs');
const html = fs.readFileSync('mock_ui_v11.html', 'utf8');
const timelineSource = fs.readFileSync('script/ui/UI_TimelineBlock.js', 'utf8');
function assert(condition, message) {
  if (!condition) throw new Error(message);
}
assert(/id="timelineStartBtn">时间轴开始</.test(html), 'mock_ui_v11.html 缺少“时间轴开始”按钮文案');
assert(!/id="timelineStartBtn">开始</.test(html), 'mock_ui_v11.html 仍保留语义过泛的“开始”按钮文案');
assert(/id="btnOpenSkillTree">技能树\\s*\\/\\s*构筑</.test(html), 'mock_ui_v11.html 未统一技能树入口为“技能树 / 构筑”');
assert(!/id="btnOpenSkillTree">打开技能树</.test(html), 'mock_ui_v11.html 仍保留“打开技能树”旧文案');
assert(/id="btnExecute">执行回合</.test(html), 'mock_ui_v11.html 未统一主执行按钮为“执行回合”');
assert(!/id="btnExecute">执行</.test(html), 'mock_ui_v11.html 仍保留模糊的“执行”按钮文案');
assert(timelineSource.includes("isPlaying ? '时间轴暂停' : '时间轴开始'"), 'UI_TimelineBlock 未统一时间轴按钮文案');
console.log('PASS D1.1 label baseline');
NODE
```

结果：

1. `PASS D1.1 label baseline`

### 3.3 语法检查

命令：

```bash
awk '/<script type="module">/{flag=1;next}/<\/script>/{if(flag){flag=0;exit}}flag' test/codex_regression_runner.html > /tmp/codex_regression_runner.module.js
node --check /tmp/codex_regression_runner.module.js
```

结果：

1. `PASS`

### 3.4 浏览器级补充验证说明

命令：

```bash
google-chrome --headless=new --no-sandbox --disable-gpu --disable-dev-shm-usage \
  --user-data-dir="$tmpdir" \
  --virtual-time-budget=40000 \
  --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html \
  > /tmp/d1_1_green.html
```

结果：

1. 页面资源可正常访问
2. 但当前本机 headless Chrome 持续出现 `Failed global descriptor lookup: 7`，无法稳定导出结果区
3. 已按工程策略回退到“本地断言 + 语法检查”作为本轮最小自测证据，不把浏览器链路作为单点阻塞

## 4. 结论

本轮可确认：

1. 主流程按钮文案已经统一到新的 D1.1 基线。
2. `提交规划` 与 `执行回合` 的动作语义已经明确分离。
3. 技能树入口在主菜单和战斗主界面已统一为 `技能树 / 构筑`。
4. `#87 / D1.1` 已完成实现与自测，可进入人工验收。
