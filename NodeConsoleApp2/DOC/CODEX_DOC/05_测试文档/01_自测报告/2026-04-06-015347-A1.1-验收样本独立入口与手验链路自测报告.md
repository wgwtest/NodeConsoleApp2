# A1.1 验收样本独立入口与手验链路自测报告

记录时间：2026-04-06 01:53:47 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#18 / A1.1 补齐 Buff 人工验收入口与真实主流程样本`

## 1. 本轮目标

在不破坏 `B2` 已通过约束的前提下，补齐 `A1.1` 当前真正缺失的人工验收入口：

1. 故事关卡选择页继续只展示 `story` 关卡。
2. `mock_ui_v11.html` 主菜单新增独立 `验收样本` 入口。
3. `HP免伤 / 护甲免伤 / 受击获甲` 三条 Buff 手验链路重新具备真实页面入口。

说明：

1. 本轮不把验收样本重新塞回故事关卡选择页。
2. 这是为了同时满足：
   - `A1.1` 需要可手验入口
   - `B2` 需要故事关卡选择页不暴露验收样本

## 2. 本轮实现

### 2.1 DataManager

`script/engine/DataManagerV2.js`

1. 抽出 `_getLevelsListByKind(kind)`。
2. 保留 `_getStoryLevelsList()` 给故事关卡选择使用。
3. 新增 `_getAcceptanceLevelsList()`。
4. 新增 `getAcceptanceLevelSelectEntries()`，专门为人工验收样本页提供卡片数据。

### 2.2 UI_SystemModal

`script/ui/UI_SystemModal.js`

1. 主菜单在检测到存在验收样本时，新增 `验收样本` 按钮。
2. 新增 `_renderLevelCardsView()`，复用故事关卡与验收样本的卡片渲染逻辑。
3. 保持 `renderLevelSelect()` 继续只消费 `getLevelSelectEntries()`。
4. 新增 `renderAcceptanceLevelSelect()`：
   - 标题：`选择验收样本`
   - 说明文案：`本页用于人工验收样本，不影响故事推进。`

### 2.3 自动回归

`test/codex_regression_runner.html`

本轮新增并转绿 2 条断言：

1. `DataManager 构建独立验收样本选择入口`
2. `UI_SystemModal 提供独立验收样本入口`

## 3. 自测方法

### 3.1 自动回归

命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=20000 --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html
```

结果：

1. `通过 64 / 64，失败 0`
2. 新增的 2 条 A1.1 专项断言均为 `PASS`

### 3.2 真实页面烟测

入口：

1. `http://127.0.0.1:3101/mock_ui_v11.html`

方法：

1. 使用 headless Chrome + CDP 临时脚本复测真实页面。
2. 先验证故事关卡选择页仍只显示 `3` 张 `story` 关卡卡片。
3. 再验证 `验收样本` 入口可以进入 `5` 张验收样本卡片。
4. 核心按 `A1.1` 范围复测 3 条 Buff 手验链路。
5. 额外复测 `敌人修甲 / 敌人回血` 两张样本卡片仍可进入，作为后续 `A2.2` 兼容性旁证。

## 4. 真实页面结果

### 4.1 故事关卡选择仍然隔离

结果：

1. `开始冒险 -> 关卡选择`
2. 模态标题为 `选择关卡`
3. 共显示 `3` 张故事关卡卡片：
   - `幽暗森林边缘`
   - `密林前哨`
   - `废墟关隘`
4. 第二、第三张卡片显示 `未解锁`
5. 页面中不再混入任何 `验收样本` 关卡

结论：

1. `B2` 的故事关卡选择约束未被破坏

### 4.2 验收样本入口恢复

结果：

1. `开始冒险 -> 验收样本`
2. 模态标题为 `选择验收样本`
3. 页面正文显示：
   - `本页用于人工验收样本，不影响故事推进。`
4. 共显示 `5` 张验收样本卡片：
   - `人工验收样本`
   - `受击获甲验收样本`
   - `敌人修甲验收样本`
   - `敌人回血验收样本`
   - `护甲免伤验收样本`

结论：

1. `A1.1` 所需的独立人工验收入口已恢复

### 4.3 A1.1 核心 Buff 手验链路

#### 4.3.1 HP免伤

结果：

1. 关卡：`人工验收样本`
2. 技能：`HP免伤演示`
3. 回合 2 时：
   - 玩家 `HP = 100`
   - 玩家头部护甲 `= 0`
   - 敌人名称 `= 验收守卫`

#### 4.3.2 护甲免伤

结果：

1. 关卡：`护甲免伤验收样本`
2. 技能：`护甲免伤演示`
3. 回合 2 时：
   - 玩家 `HP = 100`
   - 玩家头部护甲 `= 20`
   - 敌人名称 `= 验收轻击守卫`

#### 4.3.3 受击获甲

结果：

1. 关卡：`受击获甲验收样本`
2. 技能：`受击获甲演示`
3. 回合 2 时：
   - 玩家 `HP = 100`
   - 玩家头部护甲 `= 1`
   - 敌人名称 `= 验收轻击守卫`

结论：

1. `A1.1` 范围内的 3 条 Buff 手验链路都已恢复

### 4.4 兼容性旁证

以下两项不作为 `A1.1` 的正式通过标准，只作为共享验收入口未破坏后续 `A2.2` 的旁证：

1. `敌人修甲验收样本`
   - 初始敌人胸甲 `4`
   - 回合 2 敌人胸甲 `14`
2. `敌人回血验收样本`
   - 初始敌人 HP `18`
   - 回合 2 敌人 HP `38`

## 5. 结论

本轮可确认：

1. `A1.1` 的真实缺口已经收口，不再依赖把验收样本重新暴露到故事关卡选择页。
2. `B2` 已通过的故事关卡流转约束仍然保持成立。
3. `mock_ui_v11.html` 里现在同时具备：
   - 正常故事推进入口
   - 独立人工验收样本入口
4. `#18 / A1.1` 已完成实现与自测，可进入人工验收。
