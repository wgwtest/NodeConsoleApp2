# C1.5 关前构筑摘要与技能池差异预览自测报告

记录时间：2026-04-08 15:11:53 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#75 / C1.5 关前构筑摘要与技能池差异预览`

## 1. 本轮目标

把进入关卡前的成长信息从“只知道要点关卡”推进到“先知道这局会带着什么技能进去”：

1. 关卡选择页新增 `关前构筑摘要`。
2. 明确说明当前版本没有单独的预装配步骤，已学技能会自动带入本局。
3. 展示当前技能池数量与样本技能名。
4. 展示最近学习带来的技能池差异，并在 `PLAYER_SKILLS` 更新后即时刷新。

## 2. 本轮实现

涉及文件：

1. `script/ui/UI_SystemModal.js`
2. `test/codex_regression_runner.html`

本轮新增：

1. 关卡选择页顶部 `关前构筑摘要` 区块。
2. `当前技能池 / 预装配说明 / 最近学习带来的技能池差异` 三组信息。
3. 技能 ID 到显示名的解析逻辑，避免摘要里只出现内部 ID。
4. `LEVEL_SELECT` 状态下对 `PLAYER_SKILLS` 的即时刷新支持。
5. 自动回归新增 2 条专项断言：
   - `UI_SystemModal 会在关卡选择页展示关前构筑摘要与技能池差异预览`
   - `UI_SystemModal 会在 PLAYER_SKILLS 更新后刷新关前构筑摘要`

## 3. 自测方法

### 3.1 C1.5 目标验证

命令：

```bash
node --input-type=module <<'EOF'
// 使用 jsdom 加载 UI_SystemModal
// 校验关卡选择页出现关前构筑摘要
// 校验 PLAYER_SKILLS 更新后摘要立即刷新
EOF
```

结果：

1. `PASS C1.5 关卡选择页存在关前构筑摘要`
2. `PASS C1.5 摘要会解释当前没有单独预装配步骤`
3. `PASS C1.5 摘要会展示最近学习差异技能名`
4. `PASS C1.5 PLAYER_SKILLS 更新后会刷新技能池数量与差异`

### 3.2 基线回归

命令：

```bash
node --test test/spine_bundle_loader.test.mjs
```

结果：

1. `6 / 6 PASS`

### 3.3 页面脚本语法检查

命令：

```bash
awk '/<script type="module">/{flag=1;next}/<\/script>/{if(flag){flag=0;exit}}flag' test/codex_regression_runner.html >/tmp/codex_regression_runner.module.js
node --check /tmp/codex_regression_runner.module.js
```

结果：

1. `PASS`

## 4. 结论

本轮可确认：

1. 关卡选择页已经能在进入战斗前解释“这局会自动带入什么技能”。
2. 当前版本没有引入额外的装备/预装配系统，而是把现有“已学即带入”的规则明确表达出来。
3. 最近学习带来的技能池差异已经能在关前直接看到。
4. `#75 / C1.5` 已完成实现与自测，可进入人工验收。
