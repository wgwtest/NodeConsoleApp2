# B2 关卡流转自测报告

记录时间：2026-04-04 08:09:51 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#27 / B2 关卡流转`

## 1. 本轮目标

围绕阶段 B 的 `B2 关卡流转`，收口一个最小但可连续游玩的故事关卡推进切片：

1. 故事关卡从单关扩展为 `3` 关连续路径。
2. 主关卡选择页只展示故事关卡，不把验收样本关卡暴露给正常流程。
3. 首次通关后会解锁下一故事关卡，并写回存档。
4. 结算页提供 `再次挑战`、`前往下一关`、`返回主菜单` 三个去向。
5. 允许从 `BATTLE_SETTLEMENT` 直接进入下一关或重开当前关。

说明：

1. 本轮聚焦“关卡流转闭环”，不把技能平衡、整局自然击杀耗时和关卡数值调优混入 `B2`。
2. 主页面烟测仍以 `mock_ui_v11.html` 为主入口，必要时允许使用 `window.GameEngine.endBattle(true)` 作为结算辅助注入。

## 2. 测试环境

- 工程目录：`NodeConsoleApp2/NodeConsoleApp2`
- 本地服务：`PORT=3101 node app.js`
- 浏览器自动化：`puppeteer-core + /usr/bin/google-chrome`
- 页面入口：
  - `http://127.0.0.1:3101/test/codex_regression_runner.html`
  - `http://127.0.0.1:3101/mock_ui_v11.html`

## 3. 测试范围

### 3.1 回归页

本轮新增并转绿 6 条 B2 专项回归：

1. `故事关卡提供连续推进路径`
2. `DataManager 构建故事关卡选择入口`
3. `DataManager 首次通关会解锁下一故事关卡`
4. `CoreEngine 结算态允许重新选择关卡`
5. `UI_SystemModal 会渲染结算去向按钮`
6. `UI_SystemModal 关卡选择遵守解锁状态`

### 3.2 主页面烟测

基于 [05-mock_ui_v11主流程测试基线](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/01_需求分析/05-mock_ui_v11主流程测试基线.md) 做两组真实页面烟测：

1. `开始冒险 -> 关卡选择`
2. 确认故事关卡选择页出现 `3` 张关卡卡片，且第二、第三关处于 `未解锁`
3. 点击未解锁关卡，确认不会离开关卡选择
4. 进入第一关 `幽暗森林边缘`
5. 触发胜利结算
6. 确认结算页展示三种去向按钮与下一关名称
7. 验证 `再次挑战` 会重新进入 `level_1_1`
8. 验证 `返回主菜单` 会回到 `MAIN_MENU`
9. 再次触发结算并点击 `前往下一关`，确认进入 `level_1_2_story`
10. 校验 `save_game` 中的 `unlockedLevels / completedLevels / lastSettlement`

## 4. 总体结果

### 4.1 回归页

- 结果：PASS
- 通过数：`55`
- 失败数：`0`
- 本轮新增的 6 条 B2 回归全部通过

### 4.2 主页面烟测

- 结果：PASS
- 关卡选择页：
  - 共显示 `3` 张故事关卡卡片
  - 第一关 `幽暗森林边缘` 已解锁
  - 第二关 `密林前哨`、第三关 `废墟关隘` 显示 `未解锁`
  - 点击第二关不会启动战斗，模态窗仍停留在 `选择关卡`
- 第一关结算页：
  - 标题为 `战斗胜利`
  - 三个按钮全部出现：
    - `再次挑战`
    - `前往下一关：密林前哨`
    - `返回主菜单`
  - `save_game.global.progress.unlockedLevels` 包含 `level_1_2_story`
  - `save_game.global.progress.completedLevels` 包含 `level_1_1`
  - `save_game.global.progress.lastSettlement.nextLevelId === 'level_1_2_story'`
- 去向按钮验证：
  - 点击 `再次挑战` 后，重新进入 `level_1_1`，状态为 `BATTLE_LOOP`
  - 点击 `返回主菜单` 后，状态切回 `MAIN_MENU`，模态标题为 `游戏菜单`
  - 点击 `前往下一关` 后，进入 `level_1_2_story`，状态为 `BATTLE_LOOP`

## 5. 关键结论

### 5.1 故事关卡流转链已经成立

本轮验证表明：

1. `levels.json` 中的 `flow.kind / flow.order` 已可表达故事关卡顺序。
2. `DataManagerV2` 能从配置中构建故事关卡列表与关卡选择入口。
3. 首次通关时，`applyBattleSettlement()` 会解锁下一故事关卡并写入 `lastSettlement`。

### 5.2 结算态已经成为新的关卡跳转入口

本轮验证表明：

1. `CoreEngine.selectLevel()` 已允许从 `BATTLE_SETTLEMENT` 进入下一关或重开当前关。
2. `UI_SystemModal.renderBattleSettlement()` 已不再只有“确认返回主菜单”这一条去向。
3. `再次挑战 / 前往下一关 / 返回主菜单` 三条路径都能回到有效状态机节点。

### 5.3 主流程和验收样本已经分层

本轮验证表明：

1. 主关卡选择页优先显示故事关卡，不再把验收样本关卡暴露给正常推进流程。
2. 已完成关卡会显示 `已完成`，未解锁关卡会显示 `未解锁`。
3. 锁定关卡点击不会误启动战斗。

## 6. 边界与观察

1. 本轮只收口 `B2` 的最小关卡推进链，不扩展更多故事关卡内容量。
2. 主页面烟测在进入战斗规划态后使用 `window.GameEngine.endBattle(true)` 触发结算，这是为了把验证重点放在“结算后去向与存档写入”，而不是整局自然战斗耗时。
3. 当前只验证到 `level_1_2_story` 的进入链；第三关 `level_1_3_story` 的解锁已由回归页覆盖，后续可在更完整内容量下继续做真实流程烟测。

## 7. 结论

本轮可得出以下结论：

1. `#27 / B2 关卡流转` 的最小故事推进闭环已经自测通过。
2. 回归页中的 6 条 B2 专项回归均已转绿，总体回归保持 `55 PASS / 0 FAIL`。
3. `mock_ui_v11.html` 中的故事关卡展示、首次解锁、结算去向与下一关跳转已形成真实页面闭环。

因此本轮结论为：

- `#27 / B2 关卡流转` 已完成最小实现与自测，可进入人工验收。
