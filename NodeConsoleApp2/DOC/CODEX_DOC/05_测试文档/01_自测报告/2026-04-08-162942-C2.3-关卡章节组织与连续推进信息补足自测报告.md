# C2.3 关卡章节组织与连续推进信息补足自测报告

记录时间：2026-04-08 16:29:42 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#78 / C2.3 关卡章节组织与连续推进信息补足`

## 1. 本轮目标

让玩家在进入故事关卡前就能看懂：

1. 当前处于哪个章节。
2. 当前章节已完成到哪里。
3. 当前推荐推进哪一关。
4. 后续节点与解锁方向是什么。

## 2. 本轮实现

涉及文件：

1. `assets/data/levels.json`
2. `script/engine/DataManagerV2.js`
3. `script/ui/UI_SystemModal.js`
4. `mock_ui_v11.css`
5. `test/codex_regression_runner.html`

本轮新增：

1. 故事关卡 `flow` 补齐章节元数据：
   - `chapterId`
   - `chapterOrder`
   - `chapterLabel`
   - `chapterTitle`
   - `nodeLabel`
   - `objectiveText`
2. `DataManagerV2` 新增 `getLevelSelectOverview()`，输出：
   - 当前章节标签与标题
   - 已完成 / 已解锁数量
   - 当前推荐关卡
   - 当前推进目标
   - 后续待解锁节点
   - 章节节点状态列表
3. `getLevelSelectEntries()` 新增 `progression`：
   - `status`
   - `stateLabel`
   - `unlockHint`
4. `UI_SystemModal.renderLevelSelect()` 新增：
   - `章节推进总览` 顶部面板
   - 每张故事关卡卡片的 `章节节点`
   - 每张故事关卡卡片的 `推进关系`
5. 自动回归新增 2 条专项断言：
   - `DataManager 构建故事关卡章节总览与当前推荐`
   - `UI_SystemModal 会渲染章节推进总览与节点关系`

## 3. 自测方法

### 3.1 模块语法检查

命令：

```bash
awk '/<script type="module">/{flag=1;next}/<\/script>/{if(flag){flag=0;exit}}flag' test/codex_regression_runner.html > /tmp/codex_regression_runner.module.js
node --check /tmp/codex_regression_runner.module.js
```

结果：

1. `PASS`，无语法错误。

### 3.2 回归页整轮验证

命令：

```bash
tmpdir=$(mktemp -d)
google-chrome --headless=new --disable-gpu --user-data-dir="$tmpdir" --virtual-time-budget=40000 --dump-dom \
  http://127.0.0.1:3101/test/codex_regression_runner.html
rm -rf "$tmpdir"
```

结果：

1. 回归页总结：`通过 102 / 102，失败 0`
2. 新增专项断言均为 `PASS`：
   - `DataManager 构建故事关卡章节总览与当前推荐`
   - `UI_SystemModal 会渲染章节推进总览与节点关系`
3. 既有 story 关卡回归继续通过：
   - `故事关卡提供连续推进路径`
   - `DataManager 构建故事关卡选择入口`
   - `UI_SystemModal 会渲染故事关卡内容提示`

## 4. 结论

本轮可确认：

1. `C2.3` 已把章节组织、节点顺序和连续推进目标收口为正式的选择前可见信息。
2. 本轮没有改故事关卡战斗执行链，只扩充关卡选择边界与展示层。
3. 章节总览和节点关系都已进入自动回归覆盖，可进入人工验收。
