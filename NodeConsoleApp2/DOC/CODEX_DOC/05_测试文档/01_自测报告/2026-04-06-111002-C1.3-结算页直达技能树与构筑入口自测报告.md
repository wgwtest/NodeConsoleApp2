# C1.3 结算页直达技能树与构筑入口自测报告

记录时间：2026-04-06 11:10:02 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#65 / C1.3 结算页直达技能树与构筑入口`

## 1. 本轮目标

把阶段 B 已成立的“胜利结算页”与阶段 C 的“技能树 / 构筑入口”连起来：

1. 胜利结算页新增 `前往技能树 / 构筑` 按钮。
2. 点击后会先返回主菜单。
3. 返回主菜单后会自动打开技能树弹层。
4. 原有：
   - `再次挑战`
   - `前往下一关`
   - `返回主菜单`
   仍然可用。

说明：

1. 本轮聚焦“奖励反馈后的导航连通性”。
2. 不改奖励结算公式，不重做胜利判定，不把完整自然战斗耗时混入 `C1.3`。

## 2. 本轮实现

### 2.1 代码落点

实现文件：

1. `script/ui/UI_SystemModal.js`

核心改动：

1. 在 `renderBattleSettlement()` 的胜利分支中新增 `前往技能树 / 构筑` 按钮。
2. 点击逻辑分两步：
   - 先走既有 `confirmSettlement()`，返回主菜单
   - 再延迟发出 `UI:OPEN_SKILL_TREE`
3. 主菜单入口仍保持同步打开技能树，不改变 `C1.1` 的原有事件契约。

### 2.2 自动回归

实现文件：

1. `test/codex_regression_runner.html`

本轮专项断言：

1. `UI_SystemModal 会在胜利结算后直达技能树入口`
   - 胜利结算页存在新按钮
   - 点击后先走 `confirmSettlement`
   - 然后发出 `UI:OPEN_SKILL_TREE`
   - 主菜单标题与成长摘要已回到可见状态

## 3. 测试环境

- 工程目录：`NodeConsoleApp2/NodeConsoleApp2`
- 本地服务：`PORT=3101 node app.js`
- 浏览器自动化：`/usr/bin/google-chrome --headless=new`
- 页面入口：
  - `http://127.0.0.1:3101/test/codex_regression_runner.html`
  - `http://127.0.0.1:3101/mock_ui_v11.html`

## 4. 自测方法

### 4.1 自动回归

命令：

```bash
tmpdir=$(mktemp -d)
google-chrome --headless=new --disable-gpu --user-data-dir="$tmpdir" --virtual-time-budget=30000 --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html
rm -rf "$tmpdir"
```

结果：

1. 当前最新回归集为 `70 / 70`，失败 `0`
2. `C1.3` 专项断言为 `PASS`

### 4.2 主页面烟测

本轮新增本地临时烟测页：

1. `test-results/c1_3_settlement_skill_tree_smoke.html`
2. 说明：该页仅用于本轮本地自测，未纳入版本管理

驱动路径：

1. 以 `mock_ui_v11.html` 为主入口
2. 进入 `开始冒险 -> 游戏菜单`
3. 在页面已完成初始化后，注入一份受控胜利结算态
4. 点击 `前往技能树 / 构筑`
5. 观察是否先回到主菜单，再自动打开技能树

为什么允许使用受控结算注入：

1. `C1.3` 的验证重点是“结算页到技能树的导航链”。
2. 胜利结算本身的生成、奖励写入和原有三条去向，已在 `B1 / B2` 自测与验收中成立。
3. 因此本轮沿用相同策略，把验证焦点放在新增按钮的连通性，而不是重复打一整局自然战斗。

命令：

```bash
tmpdir=$(mktemp -d)
google-chrome --headless=new --disable-gpu --user-data-dir="$tmpdir" --virtual-time-budget=180000 --dump-dom http://127.0.0.1:3101/test-results/c1_3_settlement_skill_tree_smoke.html
rm -rf "$tmpdir"
```

结果：

1. `PASS C1.3 结算页直达技能树 / 构筑入口烟测通过`
2. 日志确认：
   - 已进入 `游戏菜单`
   - 已注入受控胜利结算态
   - 已确认结算页出现新去向按钮
   - 点击后已返回主菜单
   - 技能树弹层自动打开

## 5. 关键结论

### 5.1 结算页与技能树入口已经接通

本轮验证表明：

1. 胜利结算页已新增 `前往技能树 / 构筑`。
2. 新按钮不会绕过既有结算确认，而是先回主菜单。
3. 返回主菜单后，技能树弹层会自动打开。

### 5.2 原有结算去向未被破坏

本轮验证表明：

1. 现有 `再次挑战 / 前往下一关 / 返回主菜单` 断言仍在回归中保持 `PASS`。
2. `C1.3` 只是在胜利结算页增加新去向，不重写已有去向逻辑。

## 6. 边界与观察

1. 本轮不重复验证自然战斗胜利本身，这属于 `B1 / B2` 的既有覆盖范围。
2. 本轮不验证学习提交后的主菜单摘要刷新，这属于 `#64 / C1.2`。
3. 本轮不验证结算奖励是否会改变技能树可学内容，这属于成长资源与构筑差异的后续节点。

## 7. 结论

本轮可得出以下结论：

1. `#65 / C1.3` 已完成最小实现与自测。
2. 胜利结算页已能直接把玩家带到主菜单下的技能树 / 构筑入口。
3. 自动回归与主页面受控烟测均已通过。

因此本轮结论为：

- `#65 / C1.3 结算页直达技能树与构筑入口` 已完成实现与自测，可进入人工验收。
