# WBS-3.2.3.2 / WBS-3.2.3.3 地图资源化整改自测报告

创建时间：2026-04-12 17:34:00 +0800

状态：`已完成`

## 1. 本轮自测目标

本轮自测不再只验证“地图编辑页能点”“地图包能过结构校验”，而是针对远端评论中的核心失败点补强：

1. `WBS-3.2.3.2`
   - 编辑页必须真实消费背景图、节点素材图、立绘图
   - 中央视图不能再依赖程序生成占位图
   - 页面说明要把 `JSON + image assets` 方案讲清楚
2. `WBS-3.2.3.3`
   - 地图包 IO 页必须明确展示图片资源承载方案
   - 非法样例必须稳定报出缺失背景图 / 节点素材图 / 立绘图引用

## 2. 关键实现核对

已完成以下整改：

1. 新增 `assets/images/level_map/` 目录，提供样例背景图、节点素材图、立绘图
2. 新增 `script/editor/level/LevelMapAssetResolver.js`
   - 统一解析 `assetLibrary`
   - 统一解析 `backgroundRef / artRefs.nodeArt / artRefs.portrait`
3. `LevelMapWorkspace.js`
   - 补齐 `nodeArts / portraits` 集合支持
   - 补齐 `missing_node_art_ref / missing_portrait_ref` 问题码
4. `LevelMapEditorPage.js`
   - 改为真实消费图片资源路径
   - 新增背景图、节点素材图、立绘图配置入口与预览
5. `LevelMapPackIO.js`
   - 补齐图片资源摘要计数与问题细节输出
6. 样例数据
   - `level_map_pack_v1.example.json` 已切到 `JSON + image assets`
   - `level_map_pack_v1.invalid.example.json` 已补齐图片引用缺失样例

## 3. 自动化验证

执行命令：

```bash
cd /home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2
node --test test/level_map_workspace.test.mjs test/level_map_editor_page.test.mjs test/level_map_pack_io.test.mjs test/level_authoring_summary.test.mjs test/wbs_3_2_3_1_level_map_mock.test.mjs
```

结果：

1. `19/19 PASS`
2. 覆盖范围：
   - 地图工作区结构与问题码
   - 编辑页图片资源消费与配置入口
   - IO 页图片资源口径与问题码
   - 预览页主链未回归损坏
   - 作者工具摘要入口未损坏

## 4. 本地服务与资源可达性验证

执行命令：

```bash
curl -I -s http://127.0.0.1:3101/test/level_map_editor_v1.html | head
curl -I -s http://127.0.0.1:3101/test/level_map_editor_io_test.html | head
curl -I -s http://127.0.0.1:3101/assets/images/level_map/backgrounds/bg_forest_canopy.svg | head -n 5
curl -I -s http://127.0.0.1:3101/assets/images/level_map/nodes/node_forest_watch.svg | head -n 5
curl -I -s http://127.0.0.1:3101/assets/images/level_map/portraits/enemy_goblin_hunter.svg | head -n 5
```

结果：

1. 页面入口返回 `HTTP/1.1 200 OK`
2. 三类图片资源返回 `HTTP/1.1 200 OK`
3. 资源 mime 正常为 `image/svg+xml`

## 5. 可视化复核

执行命令：

```bash
google-chrome --headless=new --disable-gpu --window-size=1600,1200 --screenshot=...
google-chrome --headless=new --disable-gpu --window-size=1366,1800 --screenshot=...
```

核对结论：

1. `level_map_editor_v1.html`
   - 首屏未再出现明显文本溢出
   - 中央视图宽度不再被侧栏压成窄条
   - 路线总览、地图舞台、左右检查区层次清晰
2. `level_map_editor_io_test.html`
   - 页头、按钮说明、资源承载说明已能顺序阅读
   - 资源承载方案区已独立展示背景图资源、节点素材图、立绘图

说明：

1. Chrome headless 运行时出现共享内存相关日志，但截图产物正常生成
2. DevTools MCP 在本轮尝试新建页面时出现 `Transport closed`，已按策略降级为 headless 浏览器截图与 CLI 校验

## 6. 当前结论

本轮自测结论为：

1. `WBS-3.2.3.2` 已从“程序占位图主承载”切换为“真实图片资源主承载”
2. `WBS-3.2.3.3` 已能把 `JSON + image assets` 方案解释清楚，并校验图片引用缺失问题码
3. 当前可进入新一轮人工重测
