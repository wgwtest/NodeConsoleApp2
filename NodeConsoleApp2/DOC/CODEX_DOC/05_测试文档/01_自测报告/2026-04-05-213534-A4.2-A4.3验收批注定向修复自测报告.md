# 2026-04-05 21:35 A4.2 / A4.3 验收批注定向修复自测报告

状态：`当前有效`

## 1. 本轮目标

1. 修复 `buff_editor_io_test.html` 与 `level_editor_io_test.html` 跨页协作说明相关的回归假阴性。
2. 刷新活跃验收清单，避免旧批注继续污染当前待验收入口。
3. 同步 `A4.2.1 / #57` 已收到的人工通过结论。

## 2. 变更范围

1. `test/codex_regression_runner.html`
2. `test/buff_editor_io_test.html`
3. `test/level_editor_io_test.html`
4. `DOC/CODEX_DOC/05_测试文档/02_验收清单/*`
5. `DOC/CODEX_DOC/06_过程文档/03_验收意见处理/*`

## 3. 自测项与结果

### 3.1 自动回归页

命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=30000 --dump-dom \
  http://127.0.0.1:3101/test/codex_regression_runner.html
```

结果：

1. 回归页汇总为 `通过 58 / 58，失败 0`
2. 新增关注条目均为 `PASS`
   - `buff_editor_io_test 提供 runtime bridge 验证入口`
   - `level_editor_io_test 提供 runtime bridge 验证入口`

结论：

1. 本轮对页面说明与回归断言的收口没有引入新的自动回归失败。

### 3.2 Buff override 链路浏览器级验证

工具：

1. `playwright-core`
2. 浏览器：`/usr/bin/google-chrome`

验证路径：

1. 打开 `test/buff_editor_io_test.html`
2. 校验“与其他页面关系”说明文案
3. `Load from ../assets/data/buffs_v2_7.json`
4. `Inject Runtime Probe Sample`
5. `Apply Runtime Override`
6. 打开 `test/buff_runtime_probe.html`
7. 验证 `override -> Apply Probe Buff -> Active Buffs`
8. 返回 I/O 页执行 `Clear Runtime Override`
9. 在 probe 页 `Reload Runtime` 并确认恢复默认值

结果：

1. I/O 页默认成功加载 `19` 条 Buff
2. 注入后 `buff_counter` 名称变为 `反击·运行时覆写样本`
3. probe 页 `Pack Source=override`
4. probe 页 `Resolved Name=反击·运行时覆写样本`
5. 清理 override 后恢复为 `Pack Source=file`、`Resolved Name=反击`

结论：

1. Buff 作者工具链的 override 写入、跨页消费与恢复链路成立。

### 3.3 Level override 链路浏览器级验证

验证路径：

1. 打开 `test/level_editor_io_test.html`
2. 校验“与其他页面关系”说明文案
3. `Load from ../assets/data/levels.json`
4. `Inject Runtime Probe Sample`
5. `Apply Runtime Override`
6. 打开 `test/level_runtime_probe.html`
7. 验证 `override -> Instantiate Probe Level`
8. 返回 I/O 页执行 `Clear Runtime Override`
9. 在 probe 页 `Reload Runtime` 并确认恢复默认值

结果：

1. I/O 页默认成功加载 `8` 条关卡
2. 注入后 `level_1_1` 名称变为 `幽暗森林边缘·运行时覆写样本`
3. probe 页 `Pack Source=override`
4. probe 页 `Resolved Enemy Members=enemy_acceptance_repair@1`
5. `Instantiate Probe Level` 后敌人名为 `验收修理守卫`
6. 清理 override 后恢复为 `Pack Source=file`、`Resolved Level Name=幽暗森林边缘`

结论：

1. 关卡作者工具链的 override 写入、跨页消费与恢复链路成立。

### 3.4 mock_ui_v11 主流程入口烟测

验证路径：

1. 打开 `mock_ui_v11.html`
2. 点击 `开始冒险`
3. 点击 `关卡选择`
4. 选择 `幽暗森林边缘`

结果：

1. 成功进入战斗规划态
2. 页面显示 `当前回合=1`
3. 页面可观察到敌人 `哥布林战士`

结论：

1. 本轮修补未破坏从主入口进入战斗规划态的关键入口。

## 4. 发现与修复

1. 初次执行回归页时，`buff_editor_io_test` 与 `level_editor_io_test` 两条检查失败。
2. 原因不是页面说明缺失，而是回归页直接匹配原始 HTML 文本，遇到 `<code>` 标记后把一句话切断，产生假阴性。
3. 已将回归页修正为“源码匹配或渲染文本匹配”双保险方式。

## 5. 当前结论

1. 本轮定向修复已通过自动回归与浏览器级验证。
2. `A4.2.1` 的本地状态已经可以从待验收体系移出。
3. `A4.2.2 / A4.2.3 / A4.3.1 / A4.3.2 / A4.3.3` 仍保持待人工验收状态，等待用户下一轮批注。

