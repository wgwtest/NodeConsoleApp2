# WBS-3.2.3.2 地图编辑工作区与节点边背景编辑自测报告

记录时间：2026-04-12 15:58:25 +0800

状态：`已自测`

对应 WBS：`#102 / WBS-3.2.3.2 地图编辑工作区与节点边背景编辑`

## 1. 本轮目标

验证以下能力已经落地：

1. 地图编辑器已从方案页推进为正式工作区
2. 已支持节点增删改、节点拖拽、边编辑、背景切换与节点样式编辑
3. 页面已明确说明本页用途、按钮原因和与其他节点的关系
4. 作者工具摘要与共享回归页已能读到本节点入口
5. 中央视图已放大，右侧已拆分为节点检查器 / 边检查器，边在画布中可见且可直接点选
6. 中央舞台已新增路线总览图例，点选路线图例可直接切到边检查器

## 2. 本轮实现

涉及文件：

1. `script/editor/level/LevelMapWorkspace.js`
2. `script/editor/level/LevelMapEditorPage.js`
3. `script/editor/level/index.js`
4. `test/level_map_editor_v1.html`
5. `test/level_map_workspace.test.mjs`
6. `test/level_map_editor_page.test.mjs`
7. `test/level_authoring_summary.test.mjs`
8. `script/engine/DataManagerV2.js`
9. `test/codex_regression_runner.html`
10. `DOC/CODEX_DOC/03_研制计划/21-WBS-3.2.3.2-地图编辑工作区与节点边背景编辑.md`
11. `DOC/CODEX_DOC/03_研制计划/20-WBS-3.2.3-地图编辑器基础能力与地图包导出链.md`

## 3. 自测方法

### 3.1 地图工作区契约测试

命令：

```bash
node --test test/level_map_workspace.test.mjs
```

结果：

1. `PASS 3 / 3，FAIL 0`
2. 已确认：
   - 可新增节点、移动节点、创建边并切换背景
   - 删除节点会修剪关联边与预览模式残留
   - 缺失 `levelId / entryNodeId / nodeRef` 会产出稳定问题码

### 3.2 地图编辑页交互测试

命令：

```bash
node --test test/level_map_editor_page.test.mjs
```

结果：

1. `PASS 7 / 7，FAIL 0`
2. 已确认：
   - 页面可加载地图包与 `levels.json`
   - 可新增节点、编辑边并同步到导出预览
   - 画布拖拽会实际改写节点坐标
   - 画布会渲染高对比边线与边标签，点击边后右侧会切换到边检查器
   - 页面文案已明确解释用途、按钮原因、边编辑原因与节点关系
   - 中央舞台会渲染 `路线总览` 图例，并支持从图例直接切到边检查器

### 3.3 作者工具摘要回归

命令：

```bash
node --test test/level_authoring_summary.test.mjs
```

结果：

1. `PASS 1 / 1，FAIL 0`
2. 已确认：
   - `DataManager.getLevelContentSourceOverview()` 已把 `test/level_map_editor_v1.html` 纳入作者工具摘要

### 3.4 地图编辑相关套件联跑

命令：

```bash
node --test test/level_map_workspace.test.mjs test/level_map_editor_page.test.mjs test/level_authoring_summary.test.mjs test/wbs_3_2_3_1_level_map_mock.test.mjs
```

结果：

1. `PASS 14 / 14，FAIL 0`
2. 已确认：
   - `WBS-3.2.3.1` 预览页与 `WBS-3.2.3.2` 编辑页的关系未被本轮 UI 调整破坏
   - `LevelMapWorkspace`、编辑页交互、作者工具摘要与预览页样本链路仍保持一致

### 3.5 共享回归页静态接入检查

本轮同时更新：

1. `test/codex_regression_runner.html`

已确认：

1. 新增 `level_map_editor_v1` 正式入口断言
2. 新增 `WBS-3.2.3.2` scope 注册
3. 共享回归页中的作者工具摘要断言已同步计数为 `4`

### 3.6 页面可访问性与 headless 加载验证

命令：

```bash
curl -I --max-time 5 http://127.0.0.1:3101/test/level_map_editor_v1.html
curl -I --max-time 5 'http://127.0.0.1:3101/test/codex_regression_runner.html?scope=WBS-3.2.3.2'
google-chrome --headless=new --disable-gpu --virtual-time-budget=8000 --dump-dom \
  http://127.0.0.1:3101/test/level_map_editor_v1.html | \
  rg -n "已加载地图包与关卡定义|地图编辑工作区|level_map_pack_v1"
```

结果：

1. 两个页面入口均返回 `HTTP 200`
2. headless DOM 中已出现：
   - `地图编辑工作区`
   - `已加载地图包与关卡定义。`
3. 说明页面不只是静态文件存在，而且已经能完成默认地图包加载

### 3.7 浏览器实页自测

本轮同时使用浏览器对 `http://127.0.0.1:3101/test/level_map_editor_v1.html` 进行了实页检查。

已确认：

1. 中央地图画布已明显放大，不再是窄列居中显示
2. 右侧已拆分为 `节点检查器 / 边检查器`，切换边编辑时不需要滚动穿过整段节点表单
3. 画布中的边标签可见，点击边后：
   - `selectedEdgeId` 会更新
   - 右侧切换到 `边检查器`
   - 被选中的边会高亮
4. 中央舞台已出现 `路线总览` 图例区，路线不再只是埋在画布中的细线
5. 导出预览已改为折叠块，不再压缩首屏舞台视野

## 4. 本轮确认点

本轮已确认：

1. `#102` 不再只是说明页计划，而是已有独立编辑工作区
2. 地图编辑页只消费 `levels.json` 中已有 `levelId`
3. 地图编辑页不直接改战斗逻辑、不改主流程入口
4. 本页与 `level_map_selection_mock.html`、`level_editor_v1.html`、`WBS-3.2.3.3` 的关系已明确写清
5. `#103` 可以直接消费本工作区的导出预览状态，继续承接正式 IO 与 round-trip
6. 本页的主要可用性问题已完成一轮修正：
   - 中央视图过窄
   - 节点 / 边表单堆叠过长
   - 边在画布中不可见
7. 本页的第二轮视觉与交互修正已完成：
   - 中央舞台加入 `路线总览`
   - 边线改为分路线配色，高亮更明显
   - 导出预览折叠，首屏视线回归主舞台

## 5. 当前结论

`#102 / WBS-3.2.3.2` 已完成二轮 UI 整改、自测与浏览器实页核验，具备进入人工验收的条件。
