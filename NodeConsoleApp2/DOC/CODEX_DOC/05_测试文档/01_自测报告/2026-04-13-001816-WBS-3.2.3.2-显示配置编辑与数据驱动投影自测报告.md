# WBS-3.2.3.2 显示配置编辑与数据驱动投影自测报告

记录时间：2026-04-13 00:18:16 +0800

状态：`已自测`

对应 WBS：`#102 / WBS-3.2.3.2 地图编辑工作区与节点边背景编辑`

## 1. 本轮目标

验证地图编辑器已经把以下字段从“底层默认值”推进为“可编辑显示配置”：

1. `space.logicalWidth`
2. `space.logicalHeight`
3. `display.viewportAspect`
4. `display.backgroundFit`
5. `display.nodeScale`
6. `display.edgeLabelMode`

并确认这些字段：

1. 只影响显示映射
2. 能同步写入导出结果
3. 不会破坏既有的逻辑地图坐标链路

## 2. 本轮实现

涉及文件：

1. `script/editor/level/LevelMapEditorPage.js`
2. `script/editor/level/LevelMapPreviewPage.js`
3. `script/engine/DataManagerV2.js`
4. `script/ui/UI_SystemModal.js`
5. `test/level_map_editor_v1.html`
6. `test/level_map_selection_mock.html`
7. `test/level_map_editor_page.test.mjs`
8. `test/level_map_preview_page.test.mjs`
9. `test/level_authoring_summary.test.mjs`
10. `test/c_growth_ui_regression.test.mjs`
11. `DOC/CODEX_DOC/03_研制计划/21-WBS-3.2.3.2-地图编辑工作区与节点边背景编辑.md`

## 3. 自测方法

### 3.1 编辑页交互与导出一致性

命令：

```bash
node --test test/level_map_editor_page.test.mjs
```

结果：

1. `PASS 12 / 12，FAIL 0`
2. 已确认：
   - 编辑页可保存 `logicalWidth / logicalHeight / viewportAspect`
   - 编辑页可保存 `backgroundFit / nodeScale / edgeLabelMode`
   - 编辑页可保存 `nodeAnchor / edgeAnchor`
   - 主舞台比例会跟随 `viewportAspect`
   - 逻辑地图空间变化时，节点投影会跟随变化
   - `mapStage` 背景铺放会跟随 `backgroundFit`
   - 节点显示层会消费 `nodeScale`
   - `edgeLabelMode=none` 时边标签会隐藏
   - 锚点策略变化时，节点投影和边路径会跟随变化
   - 导出预览中的 `maps[0].display.*` 会同步更新

### 3.2 预览页兼容回归

命令：

```bash
node --test test/level_map_preview_page.test.mjs
node --test test/wbs_3_2_3_1_level_map_mock.test.mjs
```

结果：

1. `PASS 4 / 4，FAIL 0`
2. 已确认：
   - 预览页会消费 `logicalWidth / logicalHeight / viewportAspect`
   - 预览页会消费 `backgroundFit / nodeScale / nodeAnchor / edgeAnchor / edgeLabelMode`
   - 预览舞台的节点投影与边标签策略会跟随地图显示配置
   - 本轮显示配置改动没有破坏 `WBS-3.2.3.1` 预览页入口
   - `LevelMapPreviewPage.js` 仍保持 handoff payload 与状态渲染能力

### 3.3 作者工具摘要联动回归

命令：

```bash
node --test test/level_authoring_summary.test.mjs test/c_growth_ui_regression.test.mjs
```

结果：

1. `PASS 7 / 7，FAIL 0`
2. 已确认：
   - `DataManagerV2.getLevelContentSourceOverview()` 的 `authoring` 项已输出 `details`
   - 作者工具摘要会显式说明“地图编辑页已支持逻辑空间与显示配置编辑”
   - 系统菜单中的“内容入口说明”会把该说明展示给使用者
   - 该联动只补充说明信息，不改变 story / acceptance / authoring 的既有归类口径

### 3.4 相关链路联跑

命令：

```bash
node --test test/level_map_workspace.test.mjs test/level_map_editor_page.test.mjs test/level_map_pack_io.test.mjs test/level_map_preview_page.test.mjs test/wbs_3_2_3_1_level_map_mock.test.mjs test/level_authoring_summary.test.mjs test/c_growth_ui_regression.test.mjs
```

结果：

1. `PASS 32 / 32，FAIL 0`
2. 已确认：
   - 工作区逻辑空间模型仍稳定
   - 编辑页显示配置修改不会破坏地图包 round-trip
   - 预览页、编辑页、地图包 IO 仍保持同一数据口径
   - 作者工具摘要链路未被本轮改动破坏

## 4. 本轮确认点

本轮已确认：

1. `display` 不再只是文档字段，而是已有可见编辑入口
2. `space` 也已进入地图设置区，不再写死为单一 `1600 x 900`
3. 页面会明确解释“地图真值”和“显示配置”的区别
4. 画布尺寸变化仍只改变投影，不会改写 `nodes[].position`
5. 背景铺放、节点缩放、边标签显示都已纳入地图作者工作区职责
6. 节点锚点与边锚点已进入地图作者工作区职责
7. 系统菜单中的作者工具摘要已开始同步说明地图编辑器当前已交付的关键能力

## 5. 当前结论

`WBS-3.2.3.2` 在“数据驱动地图模型”基础上，已经进一步完成第一批显示配置字段的可编辑化，当前可继续向：

1. 更完整的显示配置检查器
2. `display` 字段与作者工具摘要/验收入口的进一步联动
3. `space` 与地图包正式交付契约的进一步联动
4. 与后续地图编辑器正式交付页的视觉收口

继续推进。
