# Graphify 接入与知识图谱链路自测报告

时间：2026-04-11 23:30:05 +0800

状态：`已自测`

## 1. 本轮目标

验证 `NodeConsoleApp2` 已完成 Graphify 的项目级正式接入，且不依赖 Git hooks、不影响游戏运行时。

## 2. 自测范围

1. Codex 接入入口是否生成
2. 项目内稳定脚本入口是否可执行
3. 首轮知识图谱是否可成功生成
4. 本地图谱查询是否可返回有效结构结果
5. 生成产物是否已从 git 提交面排除

## 3. 执行命令与结果

### 3.1 重建图谱

执行目录：

`/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2`

执行命令：

```bash
./tools/graphify_rebuild.sh
```

结果：

```text
[graphify watch] Rebuilt: 795 nodes, 1633 edges, 38 communities
[graphify watch] graph.json and GRAPH_REPORT.md updated in graphify-out
```

结论：

1. 项目内稳定入口有效
2. `graphify-out/GRAPH_REPORT.md`
3. `graphify-out/graph.json`

已成功刷新。

### 3.2 图谱问答

执行命令：

```bash
./tools/graphify_query.sh "Which files are the core engine modules in this repository?" --budget 1200
```

结果摘要：

1. 返回 `script/engine/CoreEngine.js`
2. 返回 `script/engine/EnemyActionPlanner.js`
3. 返回 `CoreEngine` 下多项关键方法节点

结论：

1. 查询链路有效
2. 当前图谱可用于快速结构理解

### 3.3 生成物忽略验证

执行命令：

```bash
git -C /home/wgw/CodexProject/NodeConsoleApp2 check-ignore -v NodeConsoleApp2/graphify-out/graph.json
```

结果：

```text
.gitignore:287:NodeConsoleApp2/graphify-out/	NodeConsoleApp2/graphify-out/graph.json
```

结论：

`graphify-out/` 已按仓库级 `.gitignore` 排除，不会混入业务代码提交面。

## 4. 本轮确认的接入结果

1. `AGENTS.md` 已生成并改为使用项目内稳定入口
2. `.codex/hooks.json` 已生成为 Codex 提示钩子
3. `.graphifyignore` 已落库
4. `tools/graphify_rebuild.sh` 与 `tools/graphify_query.sh` 可执行
5. `DOC/CODEX_DOC/04_研发文档/11-Graphify接入与知识图谱使用说明.md` 已补齐
6. `/home/wgw/.codex/config.toml` 已启用 `multi_agent = true`

## 5. 风险与后续建议

1. 当前只补齐了本工程实际需要的 `JavaScript + Python` 最小依赖
2. 如果后续工程引入新的主要代码语言，应同步补齐对应 `tree-sitter-*` 依赖
3. Graphify 仅用于开发辅助理解，不应替代正式设计、计划和验收结论
