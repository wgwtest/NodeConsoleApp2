# 2026-04-01 12:43 A4.3.2 关卡装载与实例化边界收口自测报告

## 1. 自测范围

本轮对应 `#61 WBS L4: A4.3.2 关卡装载与实例化边界收口`，目标是让运行时通过 `DataManager` 的关卡边界 API 消费关卡数据，而不是继续直连 `gameConfig.levels`。

验证对象：

1. [DataManagerV2.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js)
2. [CoreEngine.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/CoreEngine.js)
3. [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)
4. [mock_ui_v11.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html)

## 2. 执行的检查

1. DataManager 关卡目录边界检查
   - 结果：[DataManagerV2.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js) 新增 `levelCatalog`
   - 结果：新增 `getLevelPackMeta / getLevelCatalog / getLevelDefinitions`
   - 结果：`getLevelConfig / getLevels / instantiateLevel` 已统一走关卡边界
2. CoreEngine 关卡消费边界检查
   - 结果：[CoreEngine.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/CoreEngine.js) 的 `_resolveSlotLayoutIdForCurrentBattle()` 已改为通过 `data.getLevelConfig(levelId)` 取关卡战斗规则
3. 自动化回归检查
   - 结果：[codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html) 中以下条目为 `PASS`
   - `DataManager 暴露关卡目录边界 API`
   - `CoreEngine 通过关卡边界解析战斗布局`
   - `DataManager.instantiateLevel 保留验收敌人的残甲状态`
4. 主流程烟测
   - 入口：[mock_ui_v11.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/mock_ui_v11.html)
   - 流程：`开始冒险 -> 关卡选择 -> 点击 level_1_1`
   - 结果：关卡列表正常展示 6 个关卡卡片
   - 结果：进入战斗后，页面进入 `阶段：PLANNING`，`当前回合 = 1`
   - 结果：敌方名称正确显示为 `哥布林战士`

## 3. 关键证据

1. 回归页控制台
   - 结果：关卡边界 API、CoreEngine 消费边界、实例化残甲保留三条新增回归均为 `PASS`
2. mock_ui 主流程
   - 结果：控制台出现 `Loaded levels from DataManager`
   - 结果：点击关卡卡片后进入 `BATTLE_LOOP`，未出现新的运行时错误

## 4. 自测结论

结论：`通过`

已确认：

1. 关卡运行时消费已经通过 `DataManager` 关卡边界收口
2. 核心引擎不再依赖直接读取 `gameConfig.levels`
3. 主流程中的关卡选择与进入战斗未被本轮收口打断

## 5. 未覆盖项

1. 本轮不覆盖关卡 pack override 局部验证入口，这属于 `#62 / A4.3.3`
2. 本轮仍未引入产品级关卡编辑器，只完成运行时边界收口
