# D3.6 HUD比例与动作可观察性校准自测报告

记录时间：2026-04-09 01:07:45 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#86 / D3.6 HUD比例与动作可观察性校准`

## 1. 本轮目标

把上一轮人工批注里已经暴露出来的两个表现层问题收口成可重复验证的正式能力：

1. 低血量时 HP 血条比例必须与真实数值一致，不能再被样式最小宽度撑坏。
2. 时间线启动时必须给出动作名提示，让敌人的回血 / 修甲 / 追击行为不再只靠猜测。
3. 护甲修复必须有可见反馈，不能只看到数值悄悄变化。
4. 所有增强都只能停留在展示层或只读元数据层，不能改战斗结算规则。

## 2. 本轮实现

涉及文件：

1. `mock_ui_v11.css`
2. `test/battle_presentation_probe.html`
3. `script/ui/presentation/BattlePresentationController.js`
4. `script/engine/EnemyActionPlanner.js`
5. `script/engine/CoreEngine.js`
6. `test/codex_regression_runner.html`
7. `DOC/CODEX_DOC/03_研制计划/08-WBS-L2-D3-对战演出与动画展示层.md`
8. `DOC/CODEX_DOC/03_研制计划/README.md`

本轮新增：

1. `mock_ui_v11.css`
   - 把 `.hud-stat span` 改为 `.hud-stat > span`
   - 解除血条填充层被 `min-width: 140px` 误命中的问题
2. `BattlePresentationController`
   - 时间线启动时额外显示动作标签飘字
   - 护甲回升时额外显示 `护甲 +N` 飘字
   - 记录最近一次动作标签到 `battle-scene.dataset`
3. `EnemyActionPlanner`
   - 规划结果补充 `skillName`
4. `CoreEngine`
   - 玩家规划冻结到队列时补充 `skillName`
5. `codex_regression_runner.html`
   - 新增 3 条 D3.6 专项回归

## 3. 自测方法

### 3.1 自动回归

命令：

```bash
google-chrome --headless=new --disable-gpu \
  --virtual-time-budget=30000 \
  --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html \
  > /tmp/codex_runner_dom.html
```

结果：

1. 从页面日志提取结果：`PASS 109 / 109，FAIL 0`
2. D3.6 新增专项均为 `PASS`：
   - `BattlePresentationController 会在时间线启动时显示动作标签`
   - `EnemyActionPlanner 返回规划时会附带可读技能名`
   - `mock_ui_v11 低血量时血条填充不受 HUD 文本最小宽度影响`

### 3.2 低血量比例根因复核

核对点：

1. `mock_ui_v11.css` 已不再存在 `.hud-stat span { ... min-width: 140px; }`
2. HUD 文本样式现在只作用于 `.hud-stat > span`

结果：

1. 根因已被样式级修复
2. 回归已覆盖该选择器误命中问题

### 3.3 语法检查

命令：

```bash
npx eslint script/engine/EnemyActionPlanner.js script/engine/CoreEngine.js script/ui/presentation/BattlePresentationController.js
awk '/<script type="module">/{flag=1;next}/<\/script>/{if(flag){flag=0;exit}}flag' test/codex_regression_runner.html >/tmp/codex_regression_runner.module.js
node --check /tmp/codex_regression_runner.module.js
```

结果：

1. `PASS`

## 4. 结论

本轮可确认：

1. HP 血条在低血量时不再被 HUD 文本最小宽度强行撑宽。
2. 时间线动作启动时现在会直接显示动作名，敌人的回血 / 修甲行为可直接观察。
3. 护甲修复现在不仅会改数值，还会出现 `护甲 +N` 飘字与部位高亮。
4. `#86 / D3.6` 已完成实现与自测，可进入人工验收。
