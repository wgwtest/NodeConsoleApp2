# C2.4 story / acceptance / 编辑器样本层级口径清理自测报告

记录时间：2026-04-08 17:13:26 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#79 / C2.4 story / acceptance / 编辑器样本层级口径清理`

## 1. 本轮目标

进一步把 3 类内容来源的口径固定下来：

1. `story`
   - 正式推进关卡
2. `acceptance`
   - 人工复核样本
3. `authoring / 编辑器样本`
   - 作者工具页中的 override 注入与运行时消费验证链

目标不是新增一套运行时玩法，而是确保入口、命名和边界都不会互相串位。

## 2. 本轮实现

涉及文件：

1. `script/engine/DataManagerV2.js`
2. `script/ui/UI_SystemModal.js`
3. `test/level_editor_io_test.html`
4. `test/level_runtime_probe.html`
5. `test/codex_regression_runner.html`

本轮新增：

1. `DataManagerV2.getLevelContentSourceOverview()`
   - 固定 `story / acceptance / authoring` 三类来源摘要
   - 明确：
     - `story` 与 `acceptance` 属于运行时入口
     - `authoring` 只属于工具页，不属于游戏菜单
2. `UI_SystemModal.renderMainMenu()`
   - 新增 `内容入口说明`
   - 显式说明：
     - `关卡选择` 是 story 正式推进入口
     - `验收样本` 是人工复核入口
     - `作者样本工具页` 不在游戏菜单
3. `UI_SystemModal.renderAcceptanceLevelSelect()`
   - 新增作者样本链路提示，避免把作者工具页误解成验收菜单的一部分
4. `level_editor_io_test.html`
   - 固定为 `作者样本工具页`
   - 明确说明：
     - 不属于故事推进关卡
     - 不属于验收样本入口
5. `level_runtime_probe.html`
   - 同步固定为 `作者样本工具页`
   - 明确说明其职责只是消费作者写入的 override 产物
6. 自动回归新增 2 条专项断言，并补强 2 条作者工具页断言：
   - `DataManager 构建关卡内容来源分层摘要`
   - `UI_SystemModal 会解释 story/验收样本/作者样本入口分工`
   - `level_editor_io_test 提供 runtime bridge 验证入口`
   - `level_runtime_probe 提供运行时消费入口`

## 3. 自测方法

### 3.1 模块语法检查

命令：

```bash
awk '/<script type="module">/{flag=1;next}/<\/script>/{if(flag){flag=0;exit}}flag' test/codex_regression_runner.html > /tmp/codex_regression_runner.module.js
node --check /tmp/codex_regression_runner.module.js
```

结果：

1. `PASS`，无语法错误。

### 3.2 回归页整轮验证

命令：

```bash
tmpdir=$(mktemp -d)
google-chrome --headless=new --disable-gpu --user-data-dir="$tmpdir" --virtual-time-budget=40000 --dump-dom \
  http://127.0.0.1:3101/test/codex_regression_runner.html
rm -rf "$tmpdir"
```

结果：

1. 回归页总结：`通过 104 / 104，失败 0`
2. 本轮新增 / 补强的专项断言均为 `PASS`：
   - `level_editor_io_test 提供 runtime bridge 验证入口`
   - `level_runtime_probe 提供运行时消费入口`
   - `DataManager 构建关卡内容来源分层摘要`
   - `UI_SystemModal 会解释 story/验收样本/作者样本入口分工`

## 4. 结论

本轮可确认：

1. `story`、`acceptance` 与 `authoring` 三类来源已经有稳定命名与稳定说明。
2. 作者样本入口仍然保留在独立工具页，没有被错误并入游戏菜单。
3. 本轮没有改变 story 关卡战斗规则，也没有把编辑器样本拉进正式运行时关卡列表。
4. `#79 / C2.4` 已完成实现与自测，可进入人工验收。
