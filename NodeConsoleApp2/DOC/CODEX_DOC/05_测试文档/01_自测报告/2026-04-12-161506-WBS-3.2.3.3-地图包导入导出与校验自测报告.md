# WBS-3.2.3.3 地图包导入导出与校验自测报告

记录时间：2026-04-12 16:15:06 +0800

状态：`已自测`

对应 WBS：`#103 / WBS-3.2.3.3 地图包导入导出与校验`

## 1. 本轮目标

验证以下能力已经落地：

1. 已存在独立的地图包 IO 工具层，而不是把导入导出逻辑继续塞在 `WBS-3.2.3.2` 页面里
2. 已提供一个合法样例和一个非法样例
3. 已提供独立 IO 校核页，可直观看到合法样例、非法样例和 round-trip 结果
4. 已接入作者工具摘要与共享回归范围

## 2. 本轮实现

涉及文件：

1. `script/editor/level/LevelMapPackIO.js`
2. `assets/data/level_map_pack_v1.example.json`
3. `assets/data/level_map_pack_v1.invalid.example.json`
4. `test/level_map_editor_io_test.html`
5. `test/level_map_pack_io.test.mjs`
6. `test/level_authoring_summary.test.mjs`
7. `test/codex_regression_runner.html`
8. `script/engine/DataManagerV2.js`
9. `DOC/CODEX_DOC/02_设计说明/26-关卡地图选择与地图包(level_map_selection)-设计说明.md`
10. `DOC/CODEX_DOC/03_研制计划/22-WBS-3.2.3.3-地图包导入导出与校验.md`

## 3. 自测方法

### 3.1 地图包 IO 套件

命令：

```bash
node --test test/level_map_pack_io.test.mjs
```

结果：

1. `PASS 5 / 5，FAIL 0`
2. 已确认：
   - 合法样例导入后问题码为 `0`
   - 非法样例稳定打出 `missing_entry_node / missing_level_ref / missing_node_ref / missing_background_ref`
   - round-trip 前后摘要保持稳定
   - `level_map_editor_io_test.html` 文案已写清用途与边界
   - 共享回归页已纳入 `WBS-3.2.3.3`

### 3.2 作者工具摘要回归

命令：

```bash
node --test test/level_authoring_summary.test.mjs
```

结果：

1. `PASS 1 / 1，FAIL 0`
2. 已确认：
   - `DataManager.getLevelContentSourceOverview()` 已把 `test/level_map_editor_io_test.html` 纳入作者工具摘要
   - 作者工具页数量已从 `4` 更新为 `5`

### 3.3 地图编辑相关联跑

命令：

```bash
node --test test/level_map_workspace.test.mjs test/level_map_editor_page.test.mjs test/level_authoring_summary.test.mjs test/wbs_3_2_3_1_level_map_mock.test.mjs test/level_map_pack_io.test.mjs
```

结果：

1. `PASS 19 / 19，FAIL 0`
2. 已确认：
   - `WBS-3.2.3.1`、`WBS-3.2.3.2`、`WBS-3.2.3.3` 三者当前链路没有互相破坏
   - 作者工具摘要、地图编辑页、地图预览页与地图包 IO 页处于一致状态

### 3.4 浏览器实页自测

页面：

1. `http://127.0.0.1:3101/test/level_map_editor_io_test.html`

已确认：

1. 点击 `Load 合法样例` 后：
   - 页面显示 `合法样例 已完成分析`
   - `问题码 = 0`
   - `地图数 = 1`
   - `节点 / 边 = 4 / 4`
2. 点击 `Run round-trip` 后：
   - 页面显示 `合法样例 的 round-trip 已执行`
   - `前置问题码 = 0`
   - `回读后问题码 = 0`
   - `结构是否稳定 = 是`
3. 点击 `Load 非法样例` 后：
   - 页面显示 `非法样例 已完成分析`
   - `问题码 = 4`
   - 问题列表中出现：
     - `missing_entry_node`
     - `missing_level_ref`
     - `missing_background_ref`
     - `missing_node_ref`

## 4. 本轮确认点

本轮已确认：

1. `WBS-3.2.3.3` 已从纯计划推进成独立工具页与工具层模块
2. 地图包导入导出链不再依赖地图编辑页本身承载
3. 非法样例不是口头说明，而是可重复触发的结构事实
4. 共享回归页已可以通过 `scope=WBS-3.2.3.3` 独立查看本节点

## 5. 当前结论

`#103 / WBS-3.2.3.3` 已完成基础实现与自测，具备进入人工验收的条件。
