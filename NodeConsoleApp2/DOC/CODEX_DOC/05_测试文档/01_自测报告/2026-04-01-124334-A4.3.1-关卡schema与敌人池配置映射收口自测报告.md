# 2026-04-01 12:43 A4.3.1 关卡 schema 与敌人池配置映射收口自测报告

## 1. 自测范围

本轮对应 `#60 WBS L4: A4.3.1 关卡 schema 与敌人池配置映射收口`，目标是把关卡数据统一收口为 wrapped pack，并确认敌人池映射在配置层与运行时装载层保持一致。

验证对象：

1. [levels.json](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/levels.json)
2. [config.json](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/config.json)
3. [DataManagerV2.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js)
4. [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)

## 2. 执行的检查

1. 关卡 pack 结构检查
   - 结果：[levels.json](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/levels.json) 已切换为 `{ $schemaVersion, meta, enemyPools, levels }` wrapped 结构
   - 结果：`$schemaVersion = levels_v1_wrapped`
   - 结果：`levels.*.waves[]` 统一通过 `enemyPoolId` 引用敌人池
2. registry 装载口径检查
   - 结果：[config.json](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/assets/data/config.json) 中 `contentRegistry.levels` 已声明 `schemaVersion = levels_v1_wrapped`
   - 结果：`contentRegistry.levels.rootKey = levels`
3. DataManager 映射展开检查
   - 结果：[DataManagerV2.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js) 新增 `_unwrapLevelDefinitions / _getLevelEnemyPools / _expandLevelEnemyPools`
   - 结果：运行时装载后，`wave.enemyPoolId` 会展开为 `wave.enemies`
4. 自动化回归检查
   - 结果：[codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html) 中 `关卡 pack schema 与敌人池配置映射对齐` 为 `PASS`
   - 结果：`验收关卡与样本敌人/技能已配置` 为 `PASS`
   - 结果：`验收关卡提供确定性的玩家与敌人状态` 为 `PASS`

## 3. 关键证据

1. 关卡 pack
   - 结果：`enemyPools.pool_story_goblin_edge.members[0].templateId = goblin_01`
   - 结果：`levels.level_1_1.waves[0].enemyPoolId = pool_story_goblin_edge`
2. 回归页控制台
   - 结果：`关卡 pack schema 与敌人池配置映射对齐` 为 `PASS`

## 4. 自测结论

结论：`通过`

已确认：

1. 关卡配置已经切换到稳定的 wrapped pack 契约
2. 敌人池映射从配置层到运行时展开层是一致的
3. 验收关卡、敌人样本与技能样本能通过当前关卡 pack 被稳定引用

## 5. 未覆盖项

1. 本轮不覆盖关卡运行时边界 API 与核心引擎消费，这属于 `#61 / A4.3.2`
2. 本轮不覆盖关卡编辑链局部验证入口，这属于 `#62 / A4.3.3`
