# C3.2 技能运行时批量回归与异常归档自测报告

记录时间：2026-04-09 01:32:15 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#85 / C3.2 技能运行时批量回归与异常归档`

## 1. 本轮目标

把 `C3.1` 的读取 / 摘要 / 问题码能力，进一步落成一个可直接使用的批量校核入口，并产出首轮异常归档。

本轮目标包括：

1. 建立独立的批量校核页；
2. 明确页面用途、按钮原因和与其他页面的关系；
3. 固定首轮异常统计；
4. 形成正式异常归档文档。

## 2. 本轮实现

涉及文件：

1. `test/skill_contract_probe.html`
2. `test/codex_regression_runner.html`
3. `DOC/CODEX_DOC/04_研发文档/07-技能运行时一致性首轮异常归档.md`
4. `DOC/CODEX_DOC/03_研制计划/11-WBS-L2-C3-技能运行时一致性与说明校核.md`

本轮新增：

1. 独立批量校核页 `skill_contract_probe.html`
2. 页内摘要卡、问题码说明、异常技能列表、契约矩阵
3. `C3.2` 的回归页说明断言
4. 首轮异常归档文档

## 3. 自测方法

### 3.1 全量自动回归

命令：

```bash
google-chrome --headless=new --disable-gpu \
  --virtual-time-budget=30000 \
  --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html \
  > /tmp/codex_runner_dom_c32_green_v2.html
```

结果：

1. `PASS 114 / 114，FAIL 0`
2. `C3.2` 相关专项均为 `PASS`

### 3.2 独立页面渲染复核

命令：

```bash
google-chrome --headless=new --disable-gpu \
  --virtual-time-budget=30000 \
  --dump-dom http://127.0.0.1:3101/test/skill_contract_probe.html \
  > /tmp/skill_contract_probe_dom_v2.html
```

结果：

1. 页面日志给出：
   - `entries=52`
   - `alias=11`
   - `skillsWithIssues=29`
   - `issueCount=80`
2. 页面日志已输出首轮 issue code 分布：
   - `unexpected_effect_tag=28`
   - `missing_effect_tag=20`
   - `tag_scope_mismatch=11`
   - `tag_subject_mismatch=10`
   - `selection_count_exceeds_candidates=6`
   - `single_selection_count_invalid=5`

### 3.3 静态检查

命令：

```bash
awk '/<script type="module">/{flag=1;next}/<\/script>/{if(flag){flag=0;exit}}flag' test/skill_contract_probe.html >/tmp/skill_contract_probe.module.js
node --check /tmp/skill_contract_probe.module.js
```

结果：

1. `PASS`

## 4. 本轮确认点

本轮已确认：

1. 页面用途、读数顺序、与主流程 / 回归页关系已经写清楚；
2. 页面不会直接修改技能配置，只做读取与批量校核；
3. 首轮异常已从“口头样本”转成“问题码 + 统计 + 代表性技能”；
4. `C3.2` 的输出已经可以作为后续逐项修复的输入。

## 5. 结论

`#85 / C3.2` 已完成实现与自测，可进入人工验收。
