# 技能与 Buff 设计说明目录清理自测报告

时间：2026-05-28 19:11:55

范围：

1. `02_设计说明/S4_技能系统与编辑器`
2. `02_设计说明/S5_Buff系统与编辑器`
3. `02_设计说明/README.md`

## 1. 清理结论

S4 中发现两个需要处理的文档入口：

1. `07-技能测试(skill_test_design)-设计说明.md`
   - 结论：过时。
   - 原因：仍描述旧 `skills.json`、旧测试页、装备/Skill/Buff 混合测试方案，与当前 S4 主设计和现有测试矩阵不一致。
   - 处理：删除活动文档；将仍有价值的“技能验证分层”并入 `03-技能系统(skill_design)-设计说明.md` 第 13 章。

2. `25-旧技能附加字段退役(skill_legacy_field_retirement)-设计说明.md`
   - 结论：已被主文档吸收，不再作为当前活动设计入口。
   - 原因：旧字段退役口径已经并入 `03-技能系统` 第 12 章；该文件仍被研制计划、验收记录和历史过程文档引用。
   - 处理：保留文件但标记为 `历史参考`；从 S4 活动 README 和总设计 README 的当前文档列表中移除。

S5 中未发现需要删除或合并的活动设计文档：

1. `09-Buff系统(buff_design)-设计说明.md` 仍是 Buff 主设计。
2. `10-Buff编辑器(buff_editor_design)-设计说明.md` 仍是 Buff 编辑器子设计。
3. S5 README 的文档组织仍合理。

## 2. 口径修订

同步修订了 S4 子文档中的过时表述：

1. `04-技能编辑器`
   - `buffs.json` 更新为 `buffs_v2_7.json`。
   - `applySelf` 改为历史导入迁移口径，保存时不再输出。
   - 去掉把旧 `scaling` 当作当前扩展字段的表述。
   - “标签”改为“派生分析维度”，不写回活动 skill pack。

2. `05-技能规划`
   - AP 成本来源更新为 `costs.ap` 与 `_getSkillApCostStrict()`。
   - 规划主链路更新为 `UI_SkillPanel.planningDraftBySkill -> CoreEngine.commitPlanning() -> TurnPlanner.planMany()`。
   - “待办”改为当前实现状态与后续优化建议。

3. `06-技能平衡`
   - 删除把标签体系作为 `skills.json` / editor 元数据字段的表述。
   - 数值类型改为当前 `amountType` 口径：`ABS / PCT_MAX / PCT_CURRENT / BUFF_STACKS / BUFF_REMAINING`。
   - 技能侧字段示例改为 `skills_melee_v4_5.json` / `skills_enemy_v1.json` 的 `requirements` 与 `costs`。

4. `08-技能头脑风暴`
   - “标签模板/标签建议”改为“分析维度模板/分析维度建议”。
   - 删除同步到 `skills_melee_v3.json` 的 `meta` 的旧建议，改为沉淀到 `06-技能平衡` 的派生分析维度。

## 3. 验证项

已执行命令：

```bash
rg -n '07-技能测试|skill_test_design' NodeConsoleApp2/DOC/CODEX_DOC/02_设计说明
rg -n 'buffs\.json|applySelf|scaling|SCALING|当前代码尚未|待办|TODO|TBD' NodeConsoleApp2/DOC/CODEX_DOC/02_设计说明/S4_技能系统与编辑器 NodeConsoleApp2/DOC/CODEX_DOC/02_设计说明/S5_Buff系统与编辑器
git diff --check
```

结果：

1. `07-技能测试|skill_test_design`：无输出，`02_设计说明` 当前活动索引与 S4 主文档不再引用该过时文档。
2. 过时关键词扫描：剩余 2 处，均为有意保留的历史/兼容说明。
   - `S5_Buff系统与编辑器/10-Buff编辑器`：说明不追求对旧版 `buffs.json` 的兼容。
   - `S4_技能系统与编辑器/04-技能编辑器`：说明历史 `applySelf` 导入时迁移为 `buffRefs.apply[target=self]`，保存时不再输出。
3. `git diff --check`：通过，无空白错误。

人工验收建议：

1. 打开 `S4_技能系统与编辑器/README.md`，确认当前文档列表只保留活动设计入口。
2. 打开 `03-技能系统(skill_design)-设计说明.md` 第 12、13 章，确认旧字段退役口径和技能验证分层已经并入主文档。
3. 打开 `25-旧技能附加字段退役(skill_legacy_field_retirement)-设计说明.md`，确认它已标记为历史参考。
