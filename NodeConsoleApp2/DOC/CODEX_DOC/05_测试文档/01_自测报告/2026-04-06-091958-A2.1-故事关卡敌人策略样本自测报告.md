# A2.1 故事关卡敌人策略样本自测报告

记录时间：2026-04-06 09:19:58 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#20 / A2.1 扩展敌人策略样本到真实关卡`

## 1. 本轮目标

把敌人策略从“只在回归或验收样本里成立”推进到“真实 story 关卡里可观察”：

1. 首关出现 `弱点追击型` 敌人。
2. 第二关出现 `低血量回血型` 敌人。
3. 第三关出现 `残甲修复型` 敌人。
4. 上述三类行为都要在 `mock_ui_v11.html` 的真实主流程中可复现，不破坏故事关卡选择与现有 Buff 主流程基线。

## 2. 本轮实现

### 2.1 敌人数据

`assets/data/enemies.json`

新增 3 个 story 专用样本敌人：

1. `goblin_story_headhunter`
   - 技能：`skill_bite` + `skill_throw_stone`
   - 目标：在真实关卡中体现“弱点追击/定点压迫”
2. `goblin_story_medic`
   - 技能：`skill_bite` + `skill_heal`
   - 初始 HP：`18 / 70`
   - 目标：首回合优先回血
3. `skeleton_story_repair_guard`
   - 技能：`skill_shield_bash` + `skill_bone_repair`
   - 初始胸甲：`8 / 40`
   - 目标：首回合优先修甲

### 2.2 关卡数据

`assets/data/levels.json`

1. `pool_story_goblin_edge` 改为接入 `goblin_story_headhunter`
2. `pool_story_goblin_outpost` 改为接入 `goblin_story_medic`
3. `pool_story_ruins_gate` 改为接入 `skeleton_story_repair_guard`
4. `level_1_1` 增加 battlePlayerState：
   - 让首关玩家头部成为更高价值的可追击目标
   - 避免规划器始终退回到“打任意裸露部位”的泛用攻击
5. 三张 story 关卡描述文案同步改为可解释的策略说明

### 2.3 敌人规划器

`script/engine/EnemyActionPlanner.js`

1. 新增 `_scoreFocusedPartPressure(...)`
   - 对单一候选部位的定点压迫技能补充评分
   - 让“定点破甲/弱点追击”能在真实样本中胜过泛用攻击
2. 提高低血量自救权重：
   - `healHp` 的评分系数由 `1.6 - hpRatio` 提高为 `1.8 - hpRatio`
   - `hpRatio <= 0.3` 时追加额外保命权重

### 2.4 自动回归

`test/codex_regression_runner.html`

本轮新增并转绿 2 条断言：

1. `故事关卡已接入三类真实敌人策略样本`
2. `EnemyActionPlanner 会在故事关卡样本中选出对应策略`

## 3. 自测方法

### 3.1 自动回归

命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=25000 --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html
```

结果：

1. `通过 66 / 66，失败 0`
2. 新增的 2 条 A2.1 专项断言均为 `PASS`

### 3.2 真实页面烟测

入口：

1. `http://127.0.0.1:3101/mock_ui_v11.html`

本轮使用本地烟测页驱动真实主流程：

1. 本地临时烟测页：`test-results/a2_1_story_enemy_smoke.html`
   - 说明：该页仅用于本轮本地自测，未纳入版本管理
2. 驱动方式：
   - iframe 加载 `mock_ui_v11.html`
   - 真实点击 `开始冒险 -> 关卡选择 -> 关卡卡片 -> 提交规划 -> 执行`
3. 每个场景都在第一回合结束后检查真实运行时状态

命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=90000 --dump-dom http://127.0.0.1:3101/test-results/a2_1_story_enemy_smoke.html
```

结果：

1. `通过 3 / 3，失败 0`

## 4. 真实页面结果

### 4.1 首关：弱点追击

关卡：

1. `幽暗森林边缘`

结果：

1. 敌人首回合规划为 `skill_throw_stone`
2. 命中部位为 `head`
3. 第一回合执行结束后，玩家头部护甲从 `20` 下降到 `0`

结论：

1. 弱点追击型敌人已经进入真实 story 关卡

### 4.2 第二关：低血量回血

关卡：

1. `密林前哨`

结果：

1. 敌人首回合规划为 `skill_heal`
2. 第一回合执行结束后，敌人 HP 从 `18` 回到 `38`

结论：

1. 恢复型敌人已经进入真实 story 关卡

### 4.3 第三关：残甲修复

关卡：

1. `废墟关隘`

结果：

1. 敌人首回合规划为 `skill_bone_repair`
2. 命中部位为 `chest`
3. 第一回合执行结束后，敌人胸部护甲从 `8` 回到 `18`

结论：

1. 修甲型敌人已经进入真实 story 关卡

## 5. 风险与边界

本轮没有做的事：

1. 还没有把敌人行为收口成独立人工验收入口
2. 还没有提供专门面向人工点击的敌人行为说明页
3. `A2.2` 仍需继续把这些 story 行为沉淀为稳定可验路径

因此本轮结论是：

1. `A2.1` 的真实关卡接入已经成立
2. `A2.2` 仍然必要，不应视为被本轮替代

## 6. 结论

本轮可确认：

1. 三类敌人策略已经进入 story 关卡，不再只停留在验收样本或单元回归层。
2. `mock_ui_v11.html` 的真实流程里可以稳定复现三种首回合敌人行为差异。
3. 自动回归与真实页面烟测均已通过。
4. `#20 / A2.1` 已完成实现与自测，可进入人工验收。
