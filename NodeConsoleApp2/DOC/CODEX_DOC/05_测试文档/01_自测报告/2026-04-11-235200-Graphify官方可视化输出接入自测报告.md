# Graphify 官方可视化输出接入自测报告

时间：2026-04-11 23:52:00 +0800

状态：`已自测`

## 1. 本轮目标

验证当前工程的 Graphify 能够产出官方浏览器可视化页面，而不只是 `graph.json` 与 `GRAPH_REPORT.md`。

## 2. 本轮改动范围

1. 升级 `tools/graphify_rebuild.sh`
2. 让重建流程同时生成：
   - `graphify-out/graph.html`
   - `graphify-out/wiki/index.md`
3. 更新 `AGENTS.md`
4. 更新 Graphify 使用说明文档

## 3. 执行命令与结果

### 3.1 重建图谱与可视化产物

执行命令：

```bash
./tools/graphify_rebuild.sh
```

结果：

```text
[graphify rebuild] Rebuilt: 795 nodes, 1633 edges, 38 communities
[graphify rebuild] graph.json, graph.html and GRAPH_REPORT.md updated in graphify-out
[graphify rebuild] wiki articles updated in graphify-out/wiki (48 articles)
```

结论：

本轮重建已成功生成官方浏览器可视化页面和 wiki 文档目录。

### 3.2 产物存在性验证

检查结果：

1. `graphify-out/graph.html` 存在，大小约 `735586` 字节
2. `graphify-out/wiki/index.md` 存在，大小约 `1990` 字节

### 3.3 本地静态服务可访问性验证

执行命令：

```bash
curl -I --max-time 5 http://127.0.0.1:3101/graphify-out/graph.html
```

结果：

```text
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8
```

结论：

在当前本机服务端口 `3101` 下，`graph.html` 已可直接通过浏览器访问。

## 4. 浏览器验证说明

本轮尝试了两条浏览器验证路径：

1. Chrome DevTools MCP
   - 失败，原因是 transport closed
2. Headless Chrome 截图
   - 失败，原因是当前运行环境共享内存描述符异常

因此本轮按工程策略采用非浏览器阻塞回退证据：

1. 实际生成 `graph.html`
2. 本地静态服务 `HTTP 200`
3. 产物内容头部与 `vis-network` 脚本引用存在

这足以说明页面已具备被浏览器打开的基本条件，不应因本机 Chrome 自动化链异常阻塞本轮接入。

## 5. 当前可用入口

在当前工程静态服务运行于 `3101` 时，可直接打开：

`http://127.0.0.1:3101/graphify-out/graph.html`

## 6. 结论

Graphify 在当前工程中已经不只是“默认给 Codex 看结构”的辅助层，也已经具备了面向人工浏览的官方可视化页面入口。
