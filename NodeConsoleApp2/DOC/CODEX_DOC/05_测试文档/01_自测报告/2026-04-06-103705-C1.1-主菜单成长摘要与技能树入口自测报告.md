# C1.1 主菜单成长摘要与技能树入口自测报告

记录时间：2026-04-06 10:37:05 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#63 / C1.1 主菜单成长摘要与技能树入口`

## 1. 本轮目标

把 `#29 / C1 技能树与构筑` 收敛成第一条可直接落地的 Prep 切片：

1. 在 `mock_ui_v11.html -> 开始冒险 -> 游戏菜单` 中直接展示当前成长摘要。
2. 在主菜单中提供 `技能树 / 构筑` 入口，而不是只允许战斗内打开技能树。
3. 保证新增入口不破坏原有：
   - `关卡选择`
   - `验收样本`
   - `存档 / 读档`

## 2. 本轮实现

### 2.1 执行来源

本轮先补齐 GitHub WBS 执行来源，再进入编码：

1. 在 `#29 / C1 技能树与构筑` 下新增叶子任务 `#63 / C1.1 主菜单成长摘要与技能树入口`
2. 将本轮范围固定为 `Prep`
   - 只解决主菜单可见性与入口连通性
   - 不改“构筑持久化语义”
   - 不在本轮引入新的成长资源规则

### 2.2 主菜单成长摘要

实现文件：

1. `script/ui/UI_SystemModal.js`

落地内容：

1. 在 `renderMainMenu()` 中新增 `成长摘要` 区块
2. 摘要区当前展示：
   - `知识点 KP`
   - `已学技能`
3. 摘要区增加简短解释文案：
   - 进入关卡前先确认当前成长状态
   - 再决定本局的构筑方向

### 2.3 主菜单技能树入口

实现文件：

1. `script/ui/UI_SystemModal.js`

落地内容：

1. 主菜单新增 `技能树 / 构筑` 按钮
2. 点击后通过 `eventBus.emit('UI:OPEN_SKILL_TREE', { source: 'MAIN_MENU' })` 打开现有技能树弹层
3. 不接管技能树实现本身，仍复用：
   - `script/ui/UI_SkillTreeOverlay.js`
   - `script/ui/UI_SkillTreeModal.js`

### 2.4 自动回归

实现文件：

1. `test/codex_regression_runner.html`

本轮先红后绿，新增并通过 1 条断言：

1. `UI_SystemModal 会渲染成长摘要并提供技能树入口`
   - 主菜单出现 `成长摘要`
   - 主菜单显示 `KP` 与 `已学技能`
   - 主菜单出现 `技能树 / 构筑`
   - 点击后发送 `UI:OPEN_SKILL_TREE`
   - 原有 `关卡选择` 不被破坏

## 3. 自测方法

### 3.1 自动回归

命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=25000 --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html
```

结果：

1. 首轮红灯：`通过 67 / 68，失败 1`
   - 失败项：`UI_SystemModal 会渲染成长摘要并提供技能树入口`
2. 修复后绿灯：`通过 68 / 68，失败 0`

### 3.2 真实页面烟测

主入口：

1. `http://127.0.0.1:3101/mock_ui_v11.html`

本轮使用本地临时烟测页驱动真实点击链：

1. 本地临时烟测页：`test-results/c1_1_main_menu_smoke.html`
2. 说明：该页仅用于本轮本地自测，未纳入版本管理
3. 驱动路径：
   - `开始冒险`
   - `技能树 / 构筑`
   - 关闭技能树
4. 验证目标：
   - 成长摘要真实渲染在主菜单中
   - 技能树弹层能被主菜单入口直接打开
   - 主菜单内容在弹层前后保持稳定

命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=120000 --dump-dom http://127.0.0.1:3101/test-results/c1_1_main_menu_smoke.html
```

结果：

1. `PASS C1.1 主菜单成长摘要与技能树入口烟测通过`

## 4. 真实页面结果

### 4.1 主菜单成长摘要

结果：

1. 点击 `开始冒险` 后进入 `游戏菜单`
2. 主菜单正文出现 `成长摘要`
3. 摘要中能看到：
   - `KP`
   - `已学技能`

结论：

1. 成长信息已经从“只能在技能树里看”前移到主菜单

### 4.2 主菜单技能树入口

结果：

1. 主菜单存在 `技能树 / 构筑` 按钮
2. 点击后现有技能树弹层正常打开
3. 弹层标题显示为 `技能树`

结论：

1. 玩家已经可以在进入关卡前直接调整成长/构筑

### 4.3 入口兼容性

结果：

1. 打开技能树后，主菜单仍保留 `关卡选择`
2. 关闭技能树后，主菜单未被破坏

结论：

1. 本轮新增入口没有破坏现有菜单导航

## 5. 风险与边界

本轮没有做的事：

1. 不验证“学习技能后主菜单摘要是否即时刷新”
2. 不引入新的构筑持久化协议
3. 不改变关卡选择或战斗内技能树入口
4. 不扩展成长资源获取来源

因此本轮结论是：

1. `C1.1` 解决的是“进入关卡前看得到、点得开”
2. 还没有解决“学习结果如何持续反馈到下一局”

## 6. 结论

本轮可确认：

1. `#29 / C1` 已拆出正式叶子执行节点 `#63 / C1.1`
2. `mock_ui_v11.html` 主菜单已能展示成长摘要
3. 主菜单已能直接打开技能树弹层
4. 自动回归与真实页面烟测均已通过
5. `#63 / C1.1` 已完成实现与自测，可进入人工验收
