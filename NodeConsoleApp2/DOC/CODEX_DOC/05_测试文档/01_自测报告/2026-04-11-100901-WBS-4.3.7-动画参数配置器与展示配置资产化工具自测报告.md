# WBS-4.3.7 动画参数配置器与展示配置资产化工具自测报告

记录时间：2026-04-11 10:09:01 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#97 / WBS-4.3.7 动画参数配置器与展示配置资产化工具`

## 1. 本轮目标

把对战展示层从“参数写死在实现里”推进到“配置资产 + 独立配置器 + 主流程消费”的正式工具形态，同时保持：

1. 不改战斗规则
2. 不阻断主流程
3. 为 `SpineAssets` 接入保留桥接面

## 2. 本轮实现

涉及文件：

1. `assets/data/battle_presentation_profiles_v1.json`
2. `script/ui/presentation/BattlePresentationAssetStore.js`
3. `script/ui/presentation/BattlePresentationController.js`
4. `script/ui/presentation/BattleAnimationDriver.js`
5. `script/ui/presentation/FighterPresenter.js`
6. `script/ui/UI_BattleRow.js`
7. `mock_ui_v11.html`
8. `mock_ui_v11.css`
9. `test/battle_presentation_probe.html`
10. `test/battle_presentation_configurator.html`
11. `test/d3_presentation_assetization.test.mjs`
12. `test/codex_regression_runner.html`
13. `DOC/CODEX_DOC/02_设计说明/20-对战演出与动画展示层(battle_presentation)-设计说明.md`
14. `DOC/CODEX_DOC/03_研制计划/08-WBS-L2-D3-对战演出与动画展示层.md`
15. `DOC/CODEX_DOC/04_研发文档/09-WBS-4.3.7-动画参数配置器与展示配置资产化工具.md`

## 3. 自测方法

### 3.1 Node 级契约测试

命令：

```bash
node --test test/d3_presentation_assetization.test.mjs test/d3_battle_presentation_contract.test.mjs test/c3_skill_contract_remediation.test.mjs
```

结果：

1. `PASS 11 / 11，FAIL 0`
2. 已确认：
   - 展示配置资产与装载层存在
   - `BattlePresentationController` 与 `UI_BattleRow` 已消费展示配置资产
   - 配置器页已提供可理解说明
   - 既有展示层契约未被打坏
   - `WBS-3.3.3` 回归项未被本轮改动破坏

### 3.2 共享回归页范围验证

命令：

```bash
google-chrome --headless --disable-gpu --virtual-time-budget=8000 --dump-dom \
  http://127.0.0.1:3000/test/codex_regression_runner.html?scope=WBS-4.3.7
```

结果：

1. `当前运行范围：WBS-4.3.7，因此本次只执行 4 / 126 个断言`
2. 断言全部 `PASS`：
   - `BattlePresentationController 会应用展示配置资产桥接面`
   - `battle_presentation_configurator 提供可理解的展示参数工具说明`
   - `battle_presentation_probe 提供演出配置资产读数入口`
   - `mock_ui_v11 提供展示配置资产读数入口`

### 3.3 页面级 headless smoke

命令：

```bash
google-chrome --headless --disable-gpu --virtual-time-budget=6000 --dump-dom \
  http://127.0.0.1:3000/test/battle_presentation_configurator.html

google-chrome --headless --disable-gpu --virtual-time-budget=6000 --dump-dom \
  http://127.0.0.1:3000/test/battle_presentation_probe.html

google-chrome --headless --disable-gpu --virtual-time-budget=6000 --dump-dom \
  http://127.0.0.1:3000/mock_ui_v11.html
```

结果：

1. 配置器页已渲染：
   - `当前 profile：默认平衡（default_balanced）`
   - `演出配置资产：默认平衡（asset）`
   - `写入浏览器 workspace`
2. probe 页已渲染：
   - `当前演出配置资产：默认平衡（asset）`
   - `重新读取演出配置`
3. 主流程页已渲染：
   - `演出配置资产：默认平衡（asset）`

说明：

1. Chrome headless 输出里有已知的共享内存噪声日志，但页面 DOM 已成功渲染目标内容，不影响本轮结论。

## 4. 本轮确认点

本轮已确认：

1. 展示参数已从写死常量提升为独立 JSON 配置资产。
2. `battle_presentation_configurator.html` 已形成独立的编辑 / 预览 / 保存 / 导出入口。
3. `battle_presentation_probe.html` 与 `mock_ui_v11.html` 已开始消费同一份展示配置资产。
4. 当前桥接面仍限定在展示层，不会改写伤害、Buff 或敌人行为。

## 5. 结论

`#97 / WBS-4.3.7` 已完成实现与自测，可进入人工验收。
