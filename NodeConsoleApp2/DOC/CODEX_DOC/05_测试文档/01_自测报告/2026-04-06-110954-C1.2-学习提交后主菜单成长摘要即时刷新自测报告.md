# C1.2 学习提交后主菜单成长摘要即时刷新自测报告

记录时间：2026-04-06 11:09:54 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#64 / C1.2 学习提交后主菜单成长摘要即时刷新`

## 1. 本轮目标

把 `C1.1` 的“主菜单可见、可进入技能树”继续推进到“学习结果能立即回流主菜单”：

1. 从主菜单打开技能树后，提交学习应直接刷新主菜单中的 `知识点 KP`。
2. 同一次提交应直接刷新主菜单中的 `已学技能` 数量。
3. 整个过程不要求玩家重新打开系统菜单，也不应破坏 `关卡选择` 等原有入口。

说明：

1. 本轮聚焦的是 `PLAYER_SKILLS` 更新回流主菜单。
2. 不把结算页新入口、构筑差异的战斗反馈和长期成长资源规则混入 `C1.2`。

## 2. 本轮实现

### 2.1 代码落点

实现文件：

1. `script/ui/UI_SystemModal.js`

核心改动：

1. 在 `handleDataUpdate()` 中补齐：
   - 当 `currentView === 'MAIN_MENU'`
   - 且 `type === 'PLAYER_SKILLS'`
   - 立即调用 `renderMainMenu()`
2. 保持原有 `SAVE_LOAD + SAVE_LIST` 刷新逻辑不变。
3. 不修改技能树提交协议，仍沿用 `UI_SkillTreeModal._handleCommitAndClose()` 发出的：
   - `DATA_UPDATE { type: 'PLAYER_SKILLS', data: skillsObj }`

### 2.2 自动回归

实现文件：

1. `test/codex_regression_runner.html`

本轮专项断言：

1. `UI_SystemModal 会在 PLAYER_SKILLS 更新后刷新成长摘要`
   - 主菜单初始 `KP / 已学技能` 显示正确
   - 注入 `PLAYER_SKILLS` 更新后，主菜单摘要立即变化
   - `关卡选择` 入口不丢失

## 3. 测试环境

- 工程目录：`NodeConsoleApp2/NodeConsoleApp2`
- 本地服务：`PORT=3101 node app.js`
- 浏览器自动化：`/usr/bin/google-chrome --headless=new`
- 页面入口：
  - `http://127.0.0.1:3101/test/codex_regression_runner.html`
  - `http://127.0.0.1:3101/mock_ui_v11.html`

## 4. 自测方法

### 4.1 自动回归

命令：

```bash
tmpdir=$(mktemp -d)
google-chrome --headless=new --disable-gpu --user-data-dir="$tmpdir" --virtual-time-budget=30000 --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html
rm -rf "$tmpdir"
```

结果：

1. 当前最新回归集为 `70 / 70`，失败 `0`
2. `C1.2` 专项断言为 `PASS`

### 4.2 主页面烟测

本轮新增本地临时烟测页：

1. `test-results/c1_2_main_menu_refresh_smoke.html`
2. 说明：该页仅用于本轮本地自测，未纳入版本管理

驱动路径：

1. `开始冒险`
2. 进入 `游戏菜单`
3. 记录当前成长摘要
4. 点击 `技能树 / 构筑`
5. 选择一个根节点技能并点击 `学习`
6. 点击 `提交并关闭`
7. 不重新打开系统菜单，直接观察主菜单摘要

为什么这条烟测成立：

1. `C1.2` 要验证的不是“技能树能不能打开”，而是“学习提交后，主菜单是否立即感知到变化”。
2. 因此烟测必须在主菜单打开技能树、提交学习后直接回看同一层主菜单，而不是刷新页面或重新进菜单。

命令：

```bash
tmpdir=$(mktemp -d)
google-chrome --headless=new --disable-gpu --user-data-dir="$tmpdir" --virtual-time-budget=120000 --dump-dom http://127.0.0.1:3101/test-results/c1_2_main_menu_refresh_smoke.html
rm -rf "$tmpdir"
```

结果：

1. `PASS C1.2 学习提交后主菜单成长摘要即时刷新烟测通过`
2. 日志确认：
   - 初始成长摘要：`KP=30`，`已学技能=3`
   - 提交后成长摘要：`KP=29`，`已学技能=4`
   - `关卡选择` 入口仍存在

## 5. 关键结论

### 5.1 学习结果已经能即时回流主菜单

本轮验证表明：

1. 技能树提交学习后，不需要重新打开系统菜单。
2. 主菜单中的 `知识点 KP` 会立即扣减。
3. 主菜单中的 `已学技能` 数量会立即增加。

### 5.2 主菜单原有入口未被破坏

本轮验证表明：

1. `关卡选择` 仍保留在主菜单中。
2. 本轮改动只补了成长摘要刷新，不影响 `C1.1` 已成立的入口结构。

## 6. 边界与观察

1. 本轮不验证结算页跳转到技能树，这属于 `#65 / C1.3`。
2. 本轮不验证新学技能是否已经在下一局形成可观察构筑差异，这属于 `#29 / C1` 的后续节点。
3. 本轮烟测使用新 profile 跑新档，验证重点是“刷新链路”，不是“兼容所有历史存档数据分布”。

## 7. 结论

本轮可得出以下结论：

1. `#64 / C1.2` 已完成最小实现与自测。
2. `PLAYER_SKILLS` 更新已经能立即回流主菜单成长摘要。
3. 自动回归与真实主页面烟测均已通过。

因此本轮结论为：

- `#64 / C1.2 学习提交后主菜单成长摘要即时刷新` 已完成实现与自测，可进入人工验收。
