# WBS-4.1.3 主界面信息优先级与状态摘要版式基线自测报告

记录时间：2026-04-11 10:50:31 +0800

对应 WBS：`#89 / WBS-4.1.3 主界面信息优先级与状态摘要版式基线`

结论：`通过`

## 1. 自测范围

本轮只验证 `WBS-4.1.3`：

1. 主流程页是否出现统一摘要条。
2. 摘要条是否会随 `PLANNING / EXECUTION` 状态变化刷新。
3. 系统菜单与关卡选择页是否挂载统一摘要语义类。
4. 共享回归页是否支持 `?scope=WBS-4.1.3`。

## 2. 自动化结果

### 2.1 Node 单测

命令：

```bash
node --test test/d1_information_priority.test.mjs
```

结果：

1. `PASS 4 / 4`

覆盖：

1. `UI_AttentionGuide` 渲染主界面摘要条
2. `UI_AttentionGuide` 随队列与时间线状态刷新
3. `UI_SystemModal` 挂统一摘要语义类
4. `mock_ui_v11.html` 提供稳定挂载点

### 2.2 关联回归

命令：

```bash
node --test test/c_growth_ui_regression.test.mjs test/d1_ui_modal_semantics.test.mjs test/d1_information_priority.test.mjs
```

结果：

1. `PASS 14 / 14`

结论：

1. 旧的成长摘要、关前构筑摘要、模态语义测试未被新摘要改造打坏。

### 2.3 共享回归页 scope

命令：

```bash
google-chrome --headless --disable-gpu --virtual-time-budget=8000 --dump-dom \
  "http://127.0.0.1:3000/test/codex_regression_runner.html?scope=WBS-4.1.3"
```

结果：

1. DOM 中显示 `通过 3 / 3，失败 0`
2. 通过项为：
   - `UI_AttentionGuide 提供主界面摘要条`
   - `UI_SystemModal 主菜单与关卡选择页使用统一摘要语义类`
   - `mock_ui_v11 提供主界面摘要条挂载点`

### 2.4 主页面 smoke

命令：

```bash
google-chrome --headless --disable-gpu --virtual-time-budget=8000 --dump-dom \
  "http://127.0.0.1:3000/mock_ui_v11.html"
```

观察结果：

1. `#battleStateSummary` 已渲染。
2. 初始 `IDLE` 态可见三张摘要卡：
   - `当前目标`
   - `战斗态势`
   - `当前操作`
3. 初始态不再显示误导性的 `AP 0 / 0`，而是显示“进入关卡后显示 AP 预算”。
4. 原有 `演出配置资产：默认平衡（asset）` 读数仍存在，说明本节点未打坏 `WBS-4.3.7` 的展示配置接入。

## 3. 风险复核

本轮未见以下回归：

1. 主流程按钮文案回退。
2. 模态返回语义回退。
3. 摘要改造导致 `mock_ui_v11.html` 无法初始化。
4. 战斗展示配置读数丢失。

## 4. 当前结论

`WBS-4.1.3` 当前已达到“可进入人工验收”的状态。
