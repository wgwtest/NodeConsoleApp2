# 2026-04-07 D3.2 SpineAssets协作规则与导入契约基线自测报告

## 1. 范围

本轮自测对应以下节点：

1. `#72 / D3.2 SpineAssets协作规则与导入契约基线`
2. 对口协作节点：
   - `wgwtest/NodeConsoleApp2-SpineAssets #4 / A2`
   - `wgwtest/NodeConsoleApp2-SpineAssets #5 / A2.1`
   - `wgwtest/NodeConsoleApp2-SpineAssets #6 / A2.2`

本轮目标不是接入 `Spine` 运行时，而是把主工程侧的消费边界、失败降级和 GitHub 协作入口先固定下来。

## 2. 自测环境

1. 主工程目录：`/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2`
2. 素材工程目录：`/home/wgw/CodexProject/NodeConsoleApp2-SpineAssets`
3. GitHub 仓库：
   - `wgwtest/NodeConsoleApp2`
   - `wgwtest/NodeConsoleApp2-SpineAssets`
4. GitHub Project：
   - `#1 NodeConsoleApp2 Delivery Roadmap`
   - `#6 NodeConsoleApp2-SpineAssets Delivery Roadmap`

## 3. 执行记录

### 3.1 主工程协作文档语义核对

检查对象：

1. `DOC/CODEX_DOC/02_设计说明/22-主工程与SpineAssets协作规则(game_spine_assets_collaboration)-设计说明.md`
2. `DOC/CODEX_DOC/03_研制计划/09-WBS-L3-D3.2-SpineAssets协作规则与导入契约基线.md`

核对结论：

1. 已明确写清主工程只消费正式导出产物，不消费 `.spine` 源文件
2. 已明确写清 bundle 缺失、schema 不兼容、单角色 manifest 缺失时的降级策略
3. 已明确写清即使 `Spine` 全部不可用，`新游戏 -> 关卡选择 -> 提交规划 -> 执行 -> 下一回合` 的主流程也必须继续成立

### 3.2 跨仓库镜像文档核对

执行命令：

```bash
rg -n "breaking change|bundle 缺失|schema 不兼容|character_manifest|\\.spine" \
  "DOC/CODEX_DOC/02_设计说明/22-主工程与SpineAssets协作规则(game_spine_assets_collaboration)-设计说明.md"
```

结果：

1. 主工程文档中能直接定位到消费边界和降级语义
2. 对照素材工程协作文档后，未发现事实冲突

### 3.3 GitHub 协作面核对

执行命令：

```bash
gh api repos/wgwtest/NodeConsoleApp2/issues/72 --jq '{number,title,state}'
gh api repos/wgwtest/NodeConsoleApp2-SpineAssets/issues/4 --jq '{number,title,state}'
gh api repos/wgwtest/NodeConsoleApp2-SpineAssets/issues/5 --jq '{number,title,state}'
gh api repos/wgwtest/NodeConsoleApp2-SpineAssets/issues/6 --jq '{number,title,state}'
gh project item-list 1 --owner wgwtest --limit 200 --format json --jq '.items[] | select(.content.number == 72) | {status:.status,phase:.phase,contributes:."contributes To"}'
gh project item-list 6 --owner wgwtest --limit 100 --format json --jq '.items[] | select(.content.number == 4 or .content.number == 5 or .content.number == 6) | {number:.content.number,status:.status,phase:.phase,contributes:."contributes To"}'
```

结果：

1. 主工程 issue `#72` 存在
2. 素材工程 issue `#4 / #5 / #6` 存在
3. 双方 Project 中都能找到对应 item
4. `D3.2` 与 `A2 / A2.1 / A2.2` 的镜像关系已经能在 GitHub 协作面追踪

## 4. 已知边界

本轮未做：

1. 主工程真实 bundle 载入代码
2. 战斗页 `Spine` 渲染接入
3. 样本角色资源的实际导入和导出

判断：

1. 这些内容依赖素材工程后续 `B1 / C1`
2. 不阻断 `D3.2` 作为主工程协作与导入契约基线进入人工验收

## 5. 自测结论

1. 主工程侧已明确“消费什么 / 不消费什么”
2. 主工程侧已明确“失败时如何降级，不影响主流程”
3. 对口素材工程 issue / project 节点已经接好
4. 建议同步状态为：`已自测 / 待人工验收`

## 6. 相关证据

1. [D3.2 人工验收清单](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/05_测试文档/02_验收清单/D3.2-[已通过]-2026-04-07-145702-SpineAssets协作规则与导入契约基线-人工验收清单.md)
2. [D3.2 会话交接记录](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/DOC/CODEX_DOC/06_过程文档/01_会话交接/2026-04-07-145702-D3.2-SpineAssets协作规则与导入契约基线实现与自测记录.md)
