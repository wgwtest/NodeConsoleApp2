# D3.1 动画展示驱动与验证页基线自测报告

时间：2026-04-07 02:43:54

状态：`通过`

对应节点：

1. `#71 WBS L3: D3.1 动画展示驱动与验证页基线`

## 1. 自测目标

确认本轮新增的对战展示层满足以下约束：

1. 动画层只消费事件和只读快照
2. 动画层可以在独立 probe 页单独验证
3. 动画层接入 `mock_ui_v11.html` 后不会阻塞主流程点击链
4. 新战斗开始、重置或关闭动画时，不会残留上一轮瞬态演出

## 2. 本轮改动范围

1. `script/ui/presentation/BattleAnimationDriver.js`
2. `script/ui/presentation/FighterPresenter.js`
3. `script/ui/presentation/BattlePresentationController.js`
4. `script/ui/UI_BattleRow.js`
5. `mock_ui_v11.css`
6. `test/battle_presentation_probe.html`
7. `test/codex_regression_runner.html`
8. `DOC/CODEX_DOC/02_设计说明/20-对战演出与动画展示层(battle_presentation)-设计说明.md`
9. `DOC/CODEX_DOC/03_研制计划/08-WBS-L2-D3-对战演出与动画展示层.md`

## 3. 红绿过程

先补失败测试：

1. `BattlePresentationController 会在新战斗开始时清理上一轮瞬态演出`
2. `battle_presentation_probe 提供一键可验证的治疗演示基线`

红灯结果：

1. 新战斗开始后仍残留上一轮飘字
2. probe 页缺少治疗按钮验证依据说明

随后补实现：

1. 为展示层增加瞬态类和飘字清理
2. 在 `BATTLE_START / TURN_START / setEnabled(false)` 时执行清理
3. 把 probe 页治疗演示改成轻伤基线，并补说明文字

## 4. 自动回归结果

执行命令：

```bash
google-chrome --headless=new --disable-gpu --virtual-time-budget=30000 --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html | rg -n "通过 [0-9]+ /|FAIL |PASS BattlePresentationController 会在新战斗开始时清理上一轮瞬态演出|PASS battle_presentation_probe 提供一键可验证的治疗演示基线"
```

结果摘要：

1. `通过 85 / 85，失败 0`
2. `PASS BattlePresentationController 会在新战斗开始时清理上一轮瞬态演出`
3. `PASS battle_presentation_probe 提供一键可验证的治疗演示基线`

## 5. 浏览器级自测结果

### 5.1 probe 页

入口：

1. `http://127.0.0.1:3101/test/battle_presentation_probe.html`

检查结果：

1. `攻击前冲`
   - 观察到 `acting = true`
2. `玩家打敌人`
   - 观察到飘字 `-18 / 护甲 -4`
   - 观察到敌方受击与护甲受击脉冲
3. `敌人打玩家`
   - 观察到飘字 `-14 / 护甲 -3`
   - 玩家 HP 从 `108 / 120` 下降到 `94 / 120`
4. `玩家治疗`
   - 观察到飘字 `+12`
   - 观察到治疗态脉冲
   - 玩家 HP 回到 `120 / 120`
5. `玩家加 Buff`
   - 观察到 `buffPulse = true`
6. `敌人加 Debuff`
   - 观察到 `debuffPulse = true`
7. `切换动画：关`
   - 敌人 HP 仍会从 `96 / 96` 下降到 `78 / 96`
   - 同时 `floatTexts / acting / hit / armorHit` 均未再出现

### 5.2 主流程页

入口：

1. `http://127.0.0.1:3101/mock_ui_v11.html`

实际点击链：

1. `新游戏`
2. `关卡选择`
3. 选择 `幽暗森林边缘`
4. 关闭模态返回战斗主界面
5. 点击 `重锤`
6. 点击敌方 `胸部` 技能槽
7. 点击 `提交规划`
8. 点击 `执行`

观察结果：

1. 回合从 `1` 正常推进到 `2`，随后再次推进到 `3`
2. 敌人 HP 从 `65 / 65` 降到 `53 / 65`，再降到 `33 / 65`
3. 执行阶段实际观测到：
   - 伤害飘字 `-30`
   - `acting` 瞬态类
   - `hit` 瞬态类
4. 动画播放结束后，系统自动返回下一回合规划态，没有新增人工确认步骤

### 5.3 控制台检查

检查结果：

1. 未发现本轮新增的 `error`
2. 页面存在 1 条既有 `issue`：
   - `A form field element should have an id or name attribute`
3. 该项是登录输入框可访问性提示，不是本轮展示层回归

## 6. 自测结论

可以确认：

1. D3.1 的展示层已形成独立驱动闭环
2. probe 页已能解释“为什么点这个按钮就能证明这项能力”
3. 主流程页已验证动画接入后不阻塞既有点击链
4. 上一轮瞬态演出不会在新战斗开始或关闭动画后继续残留

## 7. 残余说明

1. 本轮仍是展示层基线，不是最终美术表现
2. 当前主流程页的首步文案为 `新游戏`，语义上对应此前讨论中的“开始冒险”
