# 2026-04-01 12:43 A4.3.3 关卡编辑链局部验证入口自测报告

## 1. 自测范围

本轮对应 `#62 WBS L4: A4.3.3 关卡编辑链局部验证入口`，目标是建立一条可复现的“关卡 pack 产物 -> DataManagerV2 -> instantiateLevel 运行时消费”最小验证链。

验证对象：

1. [ContentPackOverrideStore.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/tooling/ContentPackOverrideStore.js)
2. [DataManagerV2.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js)
3. [level_editor_io_test.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/level_editor_io_test.html)
4. [level_runtime_probe.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/level_runtime_probe.html)
5. [codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html)

## 2. 执行的检查

1. 运行时覆写桥检查
   - 结果：沿用 [ContentPackOverrideStore.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/tooling/ContentPackOverrideStore.js) 作为关卡 pack override 的统一读写入口
   - 结果：关卡 pack 使用全局 key `levels`
2. DataManager 运行时消费检查
   - 结果：[DataManagerV2.js](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/script/engine/DataManagerV2.js) 在存在 `levels` override 时优先消费 override pack
   - 结果：`getLevelPackMeta().source` 在 override 命中时返回 `override`
   - 结果：`getLevelConfig(level_1_1).waves[0].enemies[0].templateId` 可读到覆写后的 `enemy_acceptance_repair`
3. 关卡 I/O 页验证入口检查
   - 结果：[level_editor_io_test.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/level_editor_io_test.html) 新增独立导入/导出与 `Runtime Bridge` 区块
   - 结果：新增 4 个动作按钮：
   - `Inject Runtime Probe Sample`
   - `Apply Runtime Override`
   - `Clear Runtime Override`
   - `Open level_runtime_probe.html`
4. 关卡 runtime probe 页检查
   - 结果：[level_runtime_probe.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/level_runtime_probe.html) 可独立重载 `DataManagerV2.js`
   - 结果：页面展示 `Pack Source / Schema Version / Override Key / Resolved Level Name / Resolved Enemy Members / Instantiated Enemy Names`
   - 结果：点击 `Instantiate Probe Level` 后，可直接观察 `instantiateLevel(level_1_1)` 的实例化敌人名称
5. 自动化回归检查
   - 结果：[codex_regression_runner.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/codex_regression_runner.html) 新增回归：
   - `DataManager 支持关卡 pack 运行时覆写桥`
   - `level_editor_io_test 提供 runtime bridge 验证入口`
   - `level_runtime_probe 提供运行时消费入口`
   - 结果：刷新回归页后，上述新增条目全部为 `PASS`
6. 真实浏览器链路检查
   - 入口 1：[level_editor_io_test.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/level_editor_io_test.html)
   - 入口 2：[level_runtime_probe.html](/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2/test/level_runtime_probe.html)
   - 流程：
   - 在 I/O 页点击 `Load from ../assets/data/levels.json`
   - 点击 `Inject Runtime Probe Sample`
   - 点击 `Apply Runtime Override`
   - 刷新 runtime probe 页
   - 点击 `Instantiate Probe Level`
   - 结果：runtime probe 页显示 `Pack Source = override`
   - 结果：`Resolved Level Name` 变为 `幽暗森林边缘·运行时覆写样本`
   - 结果：`Resolved Enemy Members` 变为 `enemy_acceptance_repair@1`
   - 结果：`Instantiated Enemy Names` 变为 `验收修理守卫`
7. 恢复验证
   - 流程：
   - 回到 I/O 页点击 `Clear Runtime Override`
   - 结果：页面状态栏显示 override 已清除
   - 结果：当前默认数据未被长期污染

## 3. 关键证据

1. 回归页控制台
   - 结果：关卡 override 桥、I/O 页入口、runtime probe 页入口 3 条新增回归全部为 `PASS`
2. 关卡 I/O 页运行时桥状态
   - 结果：覆写应用后页面显示 `Runtime override active. scoped key=levels, probe=level_1_1, name=幽暗森林边缘·运行时覆写样本, pool=pool_runtime_probe`
3. 关卡 runtime probe 页
   - 结果：override 生效时显示 `source=override`
   - 结果：实例化后显示 `Instantiated level_1_1: 验收修理守卫`

## 4. 自测结论

结论：`通过`

已确认：

1. 当前项目已经具备可复现的“关卡 pack 产物 -> 运行时消费”局部验证入口
2. 验证链依赖导出的关卡 pack 产物与运行时 override 桥，而不是编辑页内存态
3. override 验证完成后可清理，不会长期污染默认数据

## 5. 未覆盖项

1. 本轮未实现产品级关卡编辑器，仅提供 I/O 与 probe 级局部验证入口
2. 本轮未把该 probe 链直接嵌入 `mock_ui_v11.html` 主流程，保持为独立验证页面
