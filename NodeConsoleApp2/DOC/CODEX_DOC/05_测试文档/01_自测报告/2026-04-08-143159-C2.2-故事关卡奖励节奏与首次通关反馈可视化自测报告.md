# C2.2 故事关卡奖励节奏与首次通关反馈可视化自测报告

记录时间：2026-04-08 14:31:59 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#77 / C2.2 故事关卡奖励节奏与首次通关反馈可视化`

## 1. 本轮目标

让玩家在进入前和完成后都能看懂：

1. 首次通关反馈是什么。
2. 重复通关收益是什么。
3. 两者差别在哪里。

## 2. 本轮实现

涉及文件：

1. `script/engine/DataManagerV2.js`
2. `script/ui/UI_SystemModal.js`
3. `test/codex_regression_runner.html`

本轮新增：

1. `DataManager.getLevelSelectEntries()` 透出 `clearFeedback`：
   - `firstClearText`
   - `repeatClearText`
   - `currentMode`
2. 关卡选择卡片新增：
   - `首次通关反馈`
   - `重复通关收益`
   - 当前通关阶段提示
3. 结算页新增显式反馈：
   - 首次通关：解锁下一关或章节推进
   - 重复通关：仍有常规资源奖励，但不再解锁新章节
4. 自动回归新增 3 条专项断言：
   - `DataManager 透出故事关卡首次通关与重复通关差异说明`
   - `UI_SystemModal 会渲染故事关卡首次通关与重复通关差异`
   - `UI_SystemModal 会在结算页说明重复通关收益差异`

## 3. 自测方法

### 3.1 目标脚本回归

命令：

```bash
node --input-type=module <<'EOF'
// 使用 jsdom 加载 DataManagerV2 / UI_SystemModal
// 校验 clearFeedback、关卡卡片渲染、结算页重复通关反馈
EOF
```

结果：

1. `PASS 关卡入口数据透出首次/重复通关差异`
2. `PASS 关卡选择与结算页渲染通关差异说明`

### 3.2 基线回归

命令：

```bash
node --test test/spine_bundle_loader.test.mjs
```

结果：

1. `6 / 6 PASS`

## 4. 结论

本轮可确认：

1. 关卡选择页已经把首通与重复通关差异做成正式文案，而不是只放在奖励逻辑内部。
2. 结算页会显式说明当前是首次通关反馈还是重复通关收益。
3. 本轮没有改动奖励结算公式，只增强可见性与节奏表达。
4. `#77 / C2.2` 已完成实现与自测，可进入人工验收。
