# 地图编辑器新建入口与章节列表自适应自测报告

创建时间：2026-05-21

## 1. 本轮目标

把地图编辑器里的“新建故事包”入口补到更直观的位置，并修正章节列表、地图列表在内容增多时被挤成小框的问题。

## 2. 改动范围

- `script/editor/level/LevelMapEditorPage.js`
- `test/level_map_editor_v1.html`
- `test/level_map_editor_page.test.mjs`

## 3. 处理结果

1. 顶部工具栏仍保留 `新建故事包`，方便全局操作。
2. 左侧“当前故事包”区新增了更显眼的 `新建故事包` 入口，和包 ID、包标题放在同一块里。
3. `章节列表`、`地图列表`、`当前故事包` 列表不再只靠固定死高度，而是按内容数量和视口高度动态约束。
4. 列表在内容增长时保持内部滚动，不再把整块结构压成一个难用的小容器。

## 4. 验证命令

```bash
cd /home/wgw/CodexProject/NodeConsoleApp2/.worktree/map-optimization-20260518/NodeConsoleApp2
node --test test/level_map_editor_page.test.mjs
node --test test/level_map_workspace.test.mjs
```

## 5. 验证结果

- `test/level_map_editor_page.test.mjs`：`28/28 PASS`
- `test/level_map_workspace.test.mjs`：`7/7 PASS`

## 6. 截图复核

- 截图：`/tmp/map-editor-shot/editor-after.png`

人工目检结论：

1. 顶部保留了全局新建入口。
2. 左侧“当前故事包”区已出现更直接的新建入口。
3. 章节列表与地图列表都能在左侧正常展开，不再挤成死高度小框。
4. 中央地图与左右抽屉的主从关系保持正常。

## 7. 当前状态

已自测，待人工验收。
