# D3.5 演出配置化与内容驱动边界自测报告

记录时间：2026-04-08 17:35:05 +0800

状态：`已自测` / `待人工验收`

对应 WBS：`#82 / D3.5 演出配置化与内容驱动边界`

## 1. 本轮目标

把当前展示层里散落的模板别名、场景脉冲和状态语义，统一收口为正式配置边界：

1. 展示层支持消费结构化 `presentation` 配置，而不是只认零散字段。
2. 模板解析、场景脉冲选择和状态语义统一走一处解析逻辑。
3. 已知别名与未知模板都要留下稳定降级语义。
4. probe 页必须把“配置来源 / 降级语义 / 场景脉冲”直接读给人工验收者看。
5. 这些改动不能影响真实主流程的可玩性。

## 2. 本轮实现

涉及文件：

1. `script/ui/presentation/BattlePresentationConfig.js`
2. `script/ui/presentation/BattleAnimationDriver.js`
3. `script/ui/presentation/BattlePresentationController.js`
4. `test/battle_presentation_probe.html`
5. `test/codex_regression_runner.html`
6. `DOC/CODEX_DOC/02_设计说明/20-对战演出与动画展示层(battle_presentation)-设计说明.md`
7. `DOC/CODEX_DOC/03_研制计划/08-WBS-L2-D3-对战演出与动画展示层.md`

本轮新增：

1. `BattlePresentationConfig.js`
   - 统一解析 `presentation.template / scenePulse / statusKind`
   - 兼容旧字段 `presentationTemplate / template / skillCategory`
   - 统一产出 `template / presenterTemplate / scenePulse / statusKind / fallback`
2. `BattlePresentationController`
   - 新增 `resolveActionPresentation()`
   - 所有动作起手、治疗差异、Buff/Debuff 差异都通过统一配置解析驱动
   - 把最近一次解析结果写入 `battle-scene.dataset`
3. `BattleAnimationDriver`
   - 改为按统一 `scenePulse` 语义触发场景脉冲
4. `battle_presentation_probe.html`
   - 增加“演出配置边界”说明
   - 增加 `最近配置来源 / 最近降级语义 / 最近场景脉冲` 读数
   - 模板按钮改为优先发送结构化 `entry.presentation`
5. `codex_regression_runner.html`
   - 新增结构化配置消费断言
   - 新增未知模板降级元数据断言
   - 新增 probe 页配置边界说明断言

## 3. 自测方法

### 3.1 自动回归

命令：

```bash
tmpdir=$(mktemp -d) && \
google-chrome --headless=new --disable-gpu --user-data-dir="$tmpdir" \
  --virtual-time-budget=40000 \
  --dump-dom http://127.0.0.1:3101/test/codex_regression_runner.html \
  | grep -o '通过 [0-9]\+ / [0-9]\+，失败 [0-9]\+' | tail -n 1 && \
rm -rf "$tmpdir"
```

结果：

1. `通过 106 / 106，失败 0`

### 3.2 probe 页说明与读数验证

命令：

```bash
tmpdir=$(mktemp -d) && \
google-chrome --headless=new --disable-gpu --user-data-dir="$tmpdir" \
  --virtual-time-budget=40000 \
  --dump-dom http://127.0.0.1:3101/test/battle_presentation_probe.html \
  > /tmp/d3_5_probe_dump.html && \
grep -n '演出配置边界\|最近配置来源\|模板：未知类别（降级）' /tmp/d3_5_probe_dump.html | sed -n '1,40p' && \
rm -rf "$tmpdir"
```

结果：

1. probe 页存在 `演出配置边界`
2. probe 页存在 `最近配置来源：无；最近降级语义：无；最近场景脉冲：无`
3. probe 页存在 `模板：未知类别（降级）` 验证按钮

### 3.3 页面脚本语法检查

命令：

```bash
awk '/<script type="module">/{flag=1;next}/<\/script>/{if(flag){flag=0;exit}}flag' test/battle_presentation_probe.html >/tmp/battle_presentation_probe.module.js
node --check /tmp/battle_presentation_probe.module.js
awk '/<script type="module">/{flag=1;next}/<\/script>/{if(flag){flag=0;exit}}flag' test/codex_regression_runner.html >/tmp/codex_regression_runner.module.js
node --check /tmp/codex_regression_runner.module.js
```

结果：

1. `PASS`

## 4. 结论

本轮可确认：

1. 展示层现在有正式的 `presentation` 配置入口，不再把模板映射散落在多个文件里。
2. 已知别名、未知模板和缺失场景脉冲都能稳定降级，并且降级结果可被页面直接观察。
3. probe 页已经能解释“为什么这组按钮证明的是配置边界，而不是伤害公式”。
4. `#82 / D3.5` 已完成实现与自测，可进入人工验收。
