# 当前版本完整自测报告

记录时间：2026-04-04 01:02:28 +0800

状态：`已自测` / `含风险提示`

## 1. 本轮目标

在用户不参与人工验收的前提下，对当前版本执行一轮覆盖主流程、内容编辑链、运行时桥接和敌人行为样本的完整自动化自测，确认当前版本是否可继续作为开发与回归基线。

## 2. 测试环境

- 工程目录：`NodeConsoleApp2/NodeConsoleApp2`
- 分支状态：`master...origin/master`
- 工作树状态：干净
- 本地服务：`python3 -m http.server 8123`
- 浏览器自动化：`puppeteer-core + /usr/bin/google-chrome`
- 结构化结果文件：`/tmp/codex-browser-test/full_selftest_20260404.json`

## 3. 测试范围

本轮覆盖以下链路：

1. `assets/data/*.json` 资源完整性解析
2. `test/codex_regression_runner.html` 回归页
3. `test/skill_editor_io_test.html` 导入 / 导出 / runtime override
4. `test/buff_editor_io_test.html` + `test/buff_runtime_probe.html`
5. `test/level_editor_io_test.html` + `test/level_runtime_probe.html`
6. `mock_ui_v11.html` 主流程
7. `mock_ui_v11.html` 技能树子流程
8. `mock_ui_v11.html` 多选技能真实执行
9. `mock_ui_v11.html` 敌人修甲样本
10. `mock_ui_v11.html` 敌人回血样本

本轮未覆盖：

1. 浏览器真实下载文件动作
2. 文件上传选择器
3. 手工读档 UI

## 4. 总体结果

### 4.1 静态与资源层

- `assets/data/` 下 13 份 JSON 文件全部可解析
- 当前工作树仍为干净状态

已解析文件：

- `buffs_v2_5.json`
- `buffs_v2_6.json`
- `buffs_v2_7.json`
- `config.json`
- `enemies.json`
- `items.json`
- `levels.json`
- `player.json`
- `skills_melee_v3.json`
- `skills_melee_v4.json`
- `skills_melee_v4_4.json`
- `skills_melee_v4_5.json`
- `slot_layouts.json`

### 4.2 自动化总脚本结果

结构化总结果：

- 场景总数：7
- 通过：7
- 失败：0
- 风险提示：1

各场景结果：

| 场景 | 结果 | 关键结论 |
| --- | --- | --- |
| 回归页 40 项回归 | PASS | 控制台记录 40 条 `PASS`，0 条 `FAIL` |
| Skill 编辑器导入/导出/运行时覆写 | PASS | Pack 导出、Legacy Map 导出、runtime override 全通过 |
| Buff 编辑器导入/导出/运行时覆写 | PASS | Pack 导出、runtime probe、Buff 应用全通过 |
| Level 编辑器导入/导出/运行时覆写 | PASS | Pack 导出、runtime probe、关卡实例化全通过 |
| `mock_ui_v11` 主流程/技能树/多选技能 | PASS | 主流程、多选技能、技能树 `×` / `提交并关闭` 语义全部通过 |
| 敌人受损后优先修甲 | PASS | 样本关卡中敌方执行 `skill_block -> DONE` |
| 敌人低血量时优先回血 | PASS | 样本关卡中敌方执行 `skill_heal -> DONE` |

本轮全部场景未发现：

- `pageerror`
- `requestfailed`
- `HTTP 4xx/5xx`

## 5. 关键验证结论

### 5.1 主流程通过

基于 `mock_ui_v11.html` 的真实点击链路完成了以下验证：

1. `开始冒险 -> 关卡选择 -> 幽暗森林边缘` 可稳定进入战斗
2. 回合 1 使用 `裂颅一击`：
   - 时间线出现 2 条待执行动作
   - 敌方执行 `skill_bite -> DONE`
   - 我方执行 `skill_skull_cracker -> DONE`
   - 成功进入回合 2
3. 回合 2 使用 `多攻测试`：
   - 成功放置 4 个敌方部位槽
   - 第 5 个槽位尝试后，已填充数保持 `4`
   - 时间线出现 5 条待执行动作
   - 执行后出现 4 条我方 `skill_1771952147024 -> DONE`
   - 成功进入回合 3

### 5.2 技能树语义通过

技能树真实验证结果：

1. 打开技能树后，选中 `裂颅一击` 并点击 `学习`，再点 `×`
   - 已学技能保持不变
   - 技能点保持 `30`
   - 主界面技能按钮未新增 `裂颅一击`
2. 再次打开技能树，学习 `裂颅一击` 与 `多攻测试`，点击 `提交并关闭`
   - 已学技能新增：
     - `skill_skull_cracker`
     - `skill_1771952147024`
   - 技能点从 `30 -> 27`
   - 主界面新增：
     - `裂颅一击`
     - `多攻测试`

结论：

- `×` 只关闭，不提交学习
- `提交并关闭` 才会真正落盘并进入战斗主界面消费链路

### 5.3 Skill / Buff / Level 编辑链通过

#### Skill

- 导入 `skills_melee_v4_5.json` 后计数为 `40`
- Pack 导出保留 `$schemaVersion = skills_melee_v3`
- Legacy Map 导出包含 `skill_heal`
- 注入 runtime probe 后，`mock_ui_v11.html` 主界面技能名变为 `治疗·运行时覆写样本`

#### Buff

- 导入 `buffs_v2_7.json` 后计数为 `19`
- Pack 导出保留 `$schemaVersion = buffs_v2_1_wrapped`
- 注入 runtime probe 后，`buff_runtime_probe.html` 成功解析 `反击·运行时覆写样本`
- 点击 `Apply Probe Buff` 后，活动 Buff 列表出现：
  - `buff_counter | 反击·运行时覆写样本 | stacks=1 | remaining=1`

#### Level

- 导入 `levels.json` 后计数为 `6`
- Pack 导出保留 `$schemaVersion = levels_v1_wrapped`
- 注入 runtime probe 后，`level_runtime_probe.html` 成功解析：
  - 关卡名：`幽暗森林边缘·运行时覆写样本`
  - 敌人池成员：`enemy_acceptance_repair@1`
- 实例化关卡后，敌人名为：
  - `验收修理守卫`

### 5.4 敌人行为样本通过

在 `mock_ui_v11.html` 中分别进入两个样本关卡：

1. `敌人修甲验收样本`
   - 时间线首个敌方动作：`skill_bl...`
   - 执行日志：`[enemy] skill_block -> DONE`
2. `敌人回血验收样本`
   - 时间线首个敌方动作：`skill_he...`
   - 执行日志：`[enemy] skill_heal -> DONE`

结论：

- 当前敌人规划至少在“受损优先修甲”和“低血量优先回血”两类关键样本上表现正确

## 6. 风险与问题

### R-01 战斗运行态存档快照仍然陈旧

级别：中

现象：

- 主界面已推进到回合 `3`
- 但 `localStorage.save_game.runtime.turn` 仍为 `1`

本轮证据：

- `mock_ui_v11` 主流程场景：
  - `runtimeTurnLabel = 3`
  - `saveRuntimeTurn = 1`

影响判断：

1. 当前主流程战斗执行、技能树学习、多选技能、敌人行为并未因此失效
2. 但“战斗中存档快照”仍不能认为已经完整同步
3. 因此，本轮不能对“战斗中存档/读档恢复一致性”给出完全背书

当前口径：

- 主流程可继续作为当前开发与回归入口
- 若后续要把战斗中断点恢复作为正式可交付能力，必须单独补一轮存档同步修复与专项回归

## 7. 结论

本轮自动化自测表明：

1. 当前版本的主流程、技能树学习、多选技能执行、Buff/Level/Skill 三条内容链、敌人修甲/回血样本，均已通过自动化验证
2. 当前版本可继续作为后续开发和普通回归的有效基线
3. 当前仍保留一项中等级风险：
   - `save_game.runtime.turn` 未随战斗推进同步

因此，本轮结论为：

- `当前版本核心功能通过`
- `战斗中存档快照一致性仍需后续专项处理`
