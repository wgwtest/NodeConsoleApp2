# 2026-04-11 22:00 mock_ui_v11 浏览器主流程深度自测报告

## 1. 自测目标

基于真实浏览器页面，按当前工程约定的 `mock_ui_v11.html` 主流程入口，完成一轮从登录到进入下一回合的完整点击自测。

本轮目标不是只看静态回归，而是确认以下链路在浏览器里真实可走通：

1. `新游戏 -> 游戏菜单 -> 关卡选择 -> 进入战斗`
2. 战斗内 `技能树 / 构筑` 子流程
3. 学习技能后回到战斗页，技能池即时更新
4. 技能部署到技能槽
5. `提交规划 -> 执行回合`
6. 自动执行结束并进入下一回合

## 2. 测试方式

本轮使用：

1. 独立 headless Chrome profile
2. CDP 自动点击脚本
3. 干净浏览器环境

说明：

1. 本轮没有复用已有浏览器 profile。
2. 本轮没有复用已有 localStorage / 存档状态。
3. 本轮没有用运行态注入去伪造学习结果或回合状态。
4. Chrome MCP 因旧 profile 锁冲突不可稳定复用，所以改用 `headless Chrome + CDP` 完成同等浏览器级点击验证。

## 3. 实际执行路径

本轮真实点击顺序如下：

1. 打开 `http://127.0.0.1:3101/mock_ui_v11.html`
2. 在欢迎页点击 `新游戏`
3. 在 `游戏菜单` 点击 `关卡选择`
4. 在 `选择关卡` 点击首个可用关卡 `幽暗森林边缘`
5. 进入战斗页第 `1` 回合
6. 点击 `技能树 / 构筑`
7. 在技能树中选择并学习 `多攻测试`
8. 点击 `提交并关闭`
9. 回到战斗页，确认技能池出现 `多攻测试`
10. 选择 `多攻测试`
11. 将技能部署到 `4` 个敌方槽位
12. 点击 `提交规划`
13. 点击 `执行回合`
14. 等待自动执行结束，确认进入第 `2` 回合

## 4. 关键结果

### 4.1 登录与菜单

结果：

1. 当前实际登录入口按钮文案为 `新游戏`，不是旧口径中的 `开始冒险`。
2. 点击 `新游戏` 后，页面稳定进入标题为 `游戏菜单` 的主菜单。
3. 主菜单中可见：
   - `关卡选择`
   - `技能树 / 构筑`
   - `验收样本`
   - `存档 / 读档`
   - `设置`
   - `注销`

### 4.2 关卡选择

结果：

1. `选择关卡` 页成功渲染 `3` 张 story 关卡卡片。
2. 首张 `幽暗森林边缘` 可点击。
3. 其余两张关卡当前为 `未解锁`，状态符合预期。
4. 卡片中已看到：
   - `难度`
   - `敌人风格`
   - `奖励预览`
   - `首次通关反馈`
   - `重复通关收益`
   - `构筑提示`

### 4.3 战斗页首回合

结果：

1. 成功进入战斗页。
2. 当前回合标签为 `1`。
3. 战斗阶段为 `PLANNING`。
4. 初始可用技能为 `3` 个：
   - `治疗`
   - `重锤`
   - `临期食品`
5. 初始技能点为 `30 KP`。

### 4.4 技能树学习子流程

结果：

1. 点击 `技能树 / 构筑` 后，技能树覆盖层成功打开。
2. 技能树中识别到 `14` 个 `LEARNABLE` 节点。
3. 本轮实际学习技能为：
   - `skill_1771952147024`
   - 名称：`多攻测试`
   - 消耗：`1 KP`
4. 点击 `提交并关闭` 后，技能树成功关闭。
5. 返回战斗页后：
   - 已学技能数从 `3` 变为 `4`
   - 剩余 KP 从 `30` 变为 `29`
   - 技能池新增 `多攻测试`

### 4.5 多选技能规划

结果：

1. 本轮明确使用了多选技能，而不是只测单选技能。
2. 选中的技能为：
   - `多攻测试`
   - `mode = multiple`
   - `selectCount = 4`
3. 本轮实际部署到以下 `4` 个敌方槽位：
   - `enemy / head / slot 0`
   - `enemy / head / slot 1`
   - `enemy / head / slot 2`
   - `enemy / chest / slot 0`
4. 点击 `提交规划` 后：
   - `执行回合` 按钮变为可用
   - 已填充槽位数为 `4`
   - 规划队列长度为 `4`
   - 时间轴阶段为 `READY`

### 4.6 执行与下一回合

结果：

1. 点击 `执行回合` 后，自动执行完成。
2. 页面成功进入第 `2` 回合。
3. 执行完成后：
   - 当前回合 `1 -> 2`
   - 队列长度 `4 -> 0`
   - 时间轴阶段为 `FINISHED`
   - 战斗阶段重新回到 `PLANNING`

### 4.7 HUD 前后读数变化

执行前：

1. 玩家 HP：`100 / 100`
2. 玩家护甲：
   - 头部 `20 / 20`
   - 胸部 `10 / 20`
   - 腹部 `6 / 20`
   - 手部 `6 / 20`
   - 腿部 `12 / 20`
3. 敌人 HP：`65 / 65`
4. 敌人护甲：
   - 头部 `4 / 4`
   - 胸部 `8 / 8`
   - 腹部 `4 / 4`
   - 手部 `0 / 0`
   - 腿部 `0 / 0`

执行后：

1. 玩家 HP：`100 / 100`
2. 玩家护甲：
   - 头部 `0 / 20`
   - 胸部 `10 / 20`
   - 腹部 `6 / 20`
   - 手部 `6 / 20`
   - 腿部 `12 / 20`
3. 敌人 HP：`65 / 65`
4. 敌人护甲：
   - 头部 `0 / 4`
   - 胸部 `5 / 8`
   - 腹部 `4 / 4`
   - 手部 `0 / 0`
   - 腿部 `0 / 0`

说明：

1. 玩家头部护甲从 `20 / 20` 变为 `0 / 20`，说明敌方行为已实际生效。
2. 敌方头部护甲与胸部护甲下降，说明玩家本轮多槽位攻击已实际结算。

## 5. 浏览器级异常检查

结果：

1. 页面运行期未捕获 `window error`
2. 页面运行期未捕获 `unhandledrejection`
3. 本轮脚本记录的浏览器错误列表为空

## 6. 截图产物

本轮浏览器截图产物保存在：

`/tmp/nodeconsoleapp2-selftest-artifacts-TipqOm`

关键截图：

1. `01-login.png`
2. `02-main-menu.png`
3. `03-level-select.png`
4. `04-battle-turn1.png`
5. `05-skill-tree.png`
6. `06-planning-committed.png`
7. `07-turn2.png`

## 7. 结论

本轮结论：`通过`

可以确认：

1. `mock_ui_v11.html` 当前主流程在干净浏览器环境下可真实走通。
2. 技能树学习子流程可走通，并且学习结果会回写到战斗页技能池。
3. 本轮已明确覆盖多选技能，不是只测单选技能。
4. 多选技能可真实部署到多个技能槽，并能提交规划与执行。
5. 执行结束后可自动进入下一回合。
6. 敌我 HUD 读数变化证明本轮执行不是假过场，而是实际产生了攻防结算。

## 8. 限制说明

本轮未覆盖：

1. `×` 直接关闭技能树但不提交的反向分支
2. 单选技能与多选技能同轮混合部署
3. 多回合连续学习后的二次构筑
4. 非主流程入口如 `时间轴开始 / 单步 / 直接结算 / 设置`

这些不影响“主流程真实可走通”的本轮结论。
