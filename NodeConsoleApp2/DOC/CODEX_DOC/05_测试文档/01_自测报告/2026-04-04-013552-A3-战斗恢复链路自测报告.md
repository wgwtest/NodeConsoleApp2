# A3 战斗恢复链路自测报告

记录时间：2026-04-04 01:35:52 +0800

状态：`已自测` / `待人工验收`

## 1. 本轮目标

围绕阶段 A 的 `#22 / #23 / #24`，验证以下三件事是否已经形成可交付的恢复链路：

1. 战斗运行时关键快照会在稳定节点自动落盘。
2. 读档恢复后会重建 BuffManager、planning budget、规划状态与时间线预览。
3. `mock_ui_v11.html` 中已提交规划可走通“保存 -> 刷新 -> 恢复 -> 执行 -> 下一回合”。

## 2. 测试环境

- 工程目录：`NodeConsoleApp2/NodeConsoleApp2`
- 本地服务：`python3 -m http.server 8123`
- 浏览器自动化：`puppeteer-core + /usr/bin/google-chrome-stable`
- 结构化结果文件：`/tmp/codex-browser-test/a3_recovery_selftest_20260404.json`

## 3. 测试范围

本轮执行了两组自动化验证：

1. `test/codex_regression_runner.html`
   - 新增 3 条 A3 回归：
   - `CoreEngine.startTurn 会持久化最新 runtime snapshot`
   - `CoreEngine.commitPlanning 会持久化已提交规划快照`
   - `CoreEngine.resumeBattle 会重建 Buff/规划/时间线状态`
2. `mock_ui_v11.html`
   - 主流程进入战斗
   - 选择 `重锤` 并提交规划
   - 使用右上角齿轮进入 `存档 / 读档`
   - 保存后刷新页面
   - 重新 `开始冒险` 并点 `继续游戏`
   - 直接点击 `执行`，验证进入下一回合

## 4. 总体结果

### 4.1 回归页

- 结果：PASS
- 通过数：`43`
- 失败数：`0`
- `pageerror / requestfailed / HTTP 4xx/5xx`：`0`

### 4.2 主页面恢复链路

- 结果：PASS
- 保存前时间线：
  - `敌方 ⚔️ skill_bi… 速度 8 · PENDING`
  - `我方 ⚔️ skill_he… 速度 1 · PENDING`
- 保存前快照：
  - `runtime.turn = 1`
  - `runtime.phase = PLANNING`
  - `runtime.battleState.timelinePhase = READY`
  - `runtime.queues.player.length = 1`
  - `runtime.queues.enemy.length = 1`
- 恢复后：
  - `turnNumberLabel = 1`
  - 时间线与保存前一致
- 恢复后执行：
  - 出现 2 条执行日志：
    - `[enemy] skill_bite -> DONE`
    - `[self] skill_heavy_swing -> DONE`
  - 成功进入回合 `2`
  - 自动保存后的 `save_game.runtime.turn = 2`

## 5. 关键结论

### 5.1 落盘问题已收口

上一轮完整自测中的风险是：

- `save_game.runtime.turn` 不随 UI 回合推进同步

本轮验证结果表明：

1. `startTurn()` 后当前回合会自动写入 `save_game.runtime.turn`
2. `commitPlanning()` 后已提交规划、敌我队列、`timelinePhase=READY` 会自动写入存档
3. 执行完成进入下一回合后，`save_game.runtime.turn` 会继续推进

### 5.2 恢复重建问题已收口

本轮验证结果表明：

1. `resumeBattle()` 后可以恢复：
   - 玩家/敌方 BuffManager
   - planning budget
   - 已提交规划
   - 时间线 READY 预览
2. 恢复后无需重新进入选关，可直接继续当前战斗
3. 恢复后点击 `执行` 可以正常进入下一回合

## 6. 边界与观察

1. 当前恢复口径针对“已提交规划”的正式运行态。
2. `UI_SkillPanel` 在点击 `提交规划` 之前仍是 UI draft；未提交草稿不会进入引擎 runtime snapshot，这不属于本轮恢复承诺范围。
3. 当前战斗内用于打开主菜单/存档页的稳定入口是右上角齿轮按钮 `systemMenuBtn`。
4. 战斗面板中的 `设置` 按钮当前只发出 `UI:OPEN_MODAL { view: 'MAIN_MENU' }`，但 `UI_SystemModal.handleOpenModal()` 还没有处理该 view；这不阻塞 A3 恢复主链路，但属于可继续收口的 UI 一致性问题。

## 7. 结论

本轮可得出以下结论：

1. A3 恢复链路的核心实现已经自测通过。
2. 回归页新增的 3 条恢复专项回归均已转绿。
3. `mock_ui_v11.html` 中“已提交规划保存 -> 刷新 -> 恢复 -> 执行 -> 下一回合”已形成真实闭环。

因此本轮结论为：

- `A3 战斗恢复链路已完成自测，可进入人工验收`
