# mock_ui_v11 测试入口更正说明

创建时间：2026-03-28 00:00

## 1. 更正内容

上一轮深度自测使用的入口是：

- `test/engine_test_v2.html`

该入口适合做引擎级验证，但不是当前工程更完整的主流程验收入口。

当前应更正为：

- `mock_ui_v11.html`

原因：

- `mock_ui_v11.html` 直接实例化 `CoreEngine`
- 同时接入了：
  - `UI_BattleRow`
  - `UI_SystemModal`
  - `UI_SkillPanel`
  - `UI_TurnPanel`
  - `UI_TimelineBlock`
  - `UI_AttentionGuide`
- 具备更完整的登录、菜单、选关、规划、执行、时间线和系统模态交互链路

## 2. 对上一轮报告的影响

以下文档仍然有效，但应重新定义范围：

- `DOC/CODEX_DOC/05_测试文档/01_自测报告/2026-03-27-2328-战斗主流程深度自测报告.md`

更准确的含义应为：

- `引擎测试入口深度自测报告`

不应直接视为：

- `mock_ui_v11 主 UI 验收报告`

## 3. 当前阶段新的测试基线

从本说明开始，涉及“主流程 UI 验收”时，默认入口应为：

- `http://127.0.0.1:3101/mock_ui_v11.html`

只有在以下场景下才继续使用 `engine_test_v2.html`：

- 引擎底层行为隔离验证
- 快速对照验证
- UI 干扰较大时的运行时剥离验证

## 4. 当前已确认的 mock_ui_v11 初步事实

- 页面确实接入真实引擎和 UI 模块
- 技能选择 -> 槽位放置 -> 提交规划 -> 执行回合可以贯通
- 但页面同时暴露出新的 UI 层问题，不能沿用旧测试结论直接替代

## 5. 下一步要求

后续深度自测应按以下顺序：

1. 以 `mock_ui_v11.html` 为主入口重跑主流程自测
2. 将 `engine_test_v2.html` 降级为辅助验证入口
3. 对两类入口的结论分别记录，避免混淆
4. 主流程点击顺序以：
   - `DOC/CODEX_DOC/01_需求分析/05-mock_ui_v11主流程测试基线.md`
   为准
